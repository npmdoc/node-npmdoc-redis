# api documentation for  [redis (v2.7.1)](https://github.com/NodeRedis/node_redis)  [![travis-ci.org build-status](https://api.travis-ci.org/npmdoc/node-npmdoc-redis.svg)](https://travis-ci.org/npmdoc/node-npmdoc-redis)
#### Redis client library

[![NPM](https://nodei.co/npm/redis.png?downloads=true)](https://www.npmjs.com/package/redis)

[![apidoc](https://npmdoc.github.io/node-npmdoc-redis/build/screen-capture.buildNpmdoc.browser._2Fhome_2Ftravis_2Fbuild_2Fnpmdoc_2Fnode-npmdoc-redis_2Ftmp_2Fbuild_2Fapidoc.html.png)](https://npmdoc.github.io/node-npmdoc-redis/build..beta..travis-ci.org/apidoc.html)

![package-listing](https://npmdoc.github.io/node-npmdoc-redis/build/screen-capture.npmPackageListing.svg)



# package.json

```json

{
    "author": {
        "name": "Matt Ranney",
        "email": "mjr@ranney.com"
    },
    "bugs": {
        "url": "https://github.com/NodeRedis/node_redis/issues"
    },
    "dependencies": {
        "double-ended-queue": "^2.1.0-0",
        "redis-commands": "^1.2.0",
        "redis-parser": "^2.5.0"
    },
    "description": "Redis client library",
    "devDependencies": {
        "bluebird": "^3.0.2",
        "coveralls": "^2.11.2",
        "eslint": "^3.5.0",
        "intercept-stdout": "~0.1.2",
        "metrics": "^0.1.9",
        "mocha": "^3.1.2",
        "nyc": "^8.3.0",
        "tcp-port-used": "^0.1.2",
        "uuid": "^2.0.1",
        "win-spawn": "^2.0.0"
    },
    "directories": {
        "example": "examples",
        "test": "test"
    },
    "dist": {
        "shasum": "7d56f7875b98b20410b71539f1d878ed58ebf46a",
        "tarball": "https://registry.npmjs.org/redis/-/redis-2.7.1.tgz"
    },
    "engines": {
        "node": ">=0.10.0"
    },
    "gitHead": "5d73f5efa246f5f16f0e6ce2480fe4cdad8b0cb8",
    "homepage": "https://github.com/NodeRedis/node_redis",
    "keywords": [
        "database",
        "redis",
        "transaction",
        "pipelining",
        "performance",
        "queue",
        "nodejs",
        "pubsub",
        "backpressure"
    ],
    "license": "MIT",
    "main": "./index.js",
    "maintainers": [
        {
            "name": "bcoe",
            "email": "ben@npmjs.com"
        },
        {
            "name": "bridgear",
            "email": "ruben@bridgewater.de"
        },
        {
            "name": "bryce",
            "email": "bryce@ravenwall.com"
        },
        {
            "name": "mjr",
            "email": "mjr@ranney.com"
        }
    ],
    "name": "redis",
    "optionalDependencies": {},
    "readme": "ERROR: No README data found!",
    "repository": {
        "type": "git",
        "url": "git://github.com/NodeRedis/node_redis.git"
    },
    "scripts": {
        "benchmark": "node benchmarks/multi_bench.js",
        "compare": "node benchmarks/diff_multi_bench_output.js beforeBench.txt afterBench.txt",
        "coverage": "nyc report --reporter=html",
        "coveralls": "nyc report --reporter=text-lcov | coveralls",
        "lint": "eslint . --fix && npm run coverage",
        "test": "nyc --cache mocha ./test/*.js ./test/commands/*.js --timeout=8000"
    },
    "version": "2.7.1"
}
```



# <a name="apidoc.tableOfContents"></a>[table of contents](#apidoc.tableOfContents)

#### [module redis](#apidoc.module.redis)
1.  boolean <span class="apidocSignatureSpan">redis.</span>debug_mode
1.  [function <span class="apidocSignatureSpan">redis.</span>AbortError (obj, stack)](#apidoc.element.redis.AbortError)
1.  [function <span class="apidocSignatureSpan">redis.</span>AggregateError (obj)](#apidoc.element.redis.AggregateError)
1.  [function <span class="apidocSignatureSpan">redis.</span>Multi (client, args)](#apidoc.element.redis.Multi)
1.  [function <span class="apidocSignatureSpan">redis.</span>ParserError (message, stack)](#apidoc.element.redis.ParserError)
1.  [function <span class="apidocSignatureSpan">redis.</span>RedisClient (options, stream)](#apidoc.element.redis.RedisClient)
1.  [function <span class="apidocSignatureSpan">redis.</span>RedisError (message, stack)](#apidoc.element.redis.RedisError)
1.  [function <span class="apidocSignatureSpan">redis.</span>RedisError.super_ ()](#apidoc.element.redis.RedisError.super_)
1.  [function <span class="apidocSignatureSpan">redis.</span>ReplyError (message)](#apidoc.element.redis.ReplyError)
1.  [function <span class="apidocSignatureSpan">redis.</span>createClient ()](#apidoc.element.redis.createClient)
1.  [function <span class="apidocSignatureSpan">redis.</span>print (err, reply)](#apidoc.element.redis.print)
1.  object <span class="apidocSignatureSpan">redis.</span>Multi.prototype
1.  object <span class="apidocSignatureSpan">redis.</span>RedisClient.prototype
1.  object <span class="apidocSignatureSpan">redis.</span>customErrors
1.  object <span class="apidocSignatureSpan">redis.</span>utils

#### [module redis.AbortError](#apidoc.module.redis.AbortError)
1.  [function <span class="apidocSignatureSpan">redis.</span>AbortError (obj, stack)](#apidoc.element.redis.AbortError.AbortError)
1.  [function <span class="apidocSignatureSpan">redis.AbortError.</span>super_ (message, stack)](#apidoc.element.redis.AbortError.super_)

#### [module redis.AggregateError](#apidoc.module.redis.AggregateError)
1.  [function <span class="apidocSignatureSpan">redis.</span>AggregateError (obj)](#apidoc.element.redis.AggregateError.AggregateError)
1.  [function <span class="apidocSignatureSpan">redis.AggregateError.</span>super_ (obj, stack)](#apidoc.element.redis.AggregateError.super_)

#### [module redis.Multi](#apidoc.module.redis.Multi)
1.  [function <span class="apidocSignatureSpan">redis.</span>Multi (client, args)](#apidoc.element.redis.Multi.Multi)

#### [module redis.Multi.prototype](#apidoc.module.redis.Multi.prototype)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>APPEND ()](#apidoc.element.redis.Multi.prototype.APPEND)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ASKING ()](#apidoc.element.redis.Multi.prototype.ASKING)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>AUTH (pass, callback)](#apidoc.element.redis.Multi.prototype.AUTH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BGREWRITEAOF ()](#apidoc.element.redis.Multi.prototype.BGREWRITEAOF)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BGSAVE ()](#apidoc.element.redis.Multi.prototype.BGSAVE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITCOUNT ()](#apidoc.element.redis.Multi.prototype.BITCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITFIELD ()](#apidoc.element.redis.Multi.prototype.BITFIELD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITOP ()](#apidoc.element.redis.Multi.prototype.BITOP)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITPOS ()](#apidoc.element.redis.Multi.prototype.BITPOS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BLPOP ()](#apidoc.element.redis.Multi.prototype.BLPOP)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BRPOP ()](#apidoc.element.redis.Multi.prototype.BRPOP)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BRPOPLPUSH ()](#apidoc.element.redis.Multi.prototype.BRPOPLPUSH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>CLIENT ()](#apidoc.element.redis.Multi.prototype.CLIENT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>CLUSTER ()](#apidoc.element.redis.Multi.prototype.CLUSTER)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>COMMAND ()](#apidoc.element.redis.Multi.prototype.COMMAND)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>CONFIG ()](#apidoc.element.redis.Multi.prototype.CONFIG)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DBSIZE ()](#apidoc.element.redis.Multi.prototype.DBSIZE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DEBUG ()](#apidoc.element.redis.Multi.prototype.DEBUG)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DECR ()](#apidoc.element.redis.Multi.prototype.DECR)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DECRBY ()](#apidoc.element.redis.Multi.prototype.DECRBY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DEL ()](#apidoc.element.redis.Multi.prototype.DEL)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DISCARD ()](#apidoc.element.redis.Multi.prototype.DISCARD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DUMP ()](#apidoc.element.redis.Multi.prototype.DUMP)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ECHO ()](#apidoc.element.redis.Multi.prototype.ECHO)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EVAL ()](#apidoc.element.redis.Multi.prototype.EVAL)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EVALSHA ()](#apidoc.element.redis.Multi.prototype.EVALSHA)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXEC (callback)](#apidoc.element.redis.Multi.prototype.EXEC)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXEC_ATOMIC (callback)](#apidoc.element.redis.Multi.prototype.EXEC_ATOMIC)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXISTS ()](#apidoc.element.redis.Multi.prototype.EXISTS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXPIRE ()](#apidoc.element.redis.Multi.prototype.EXPIRE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXPIREAT ()](#apidoc.element.redis.Multi.prototype.EXPIREAT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>FLUSHALL ()](#apidoc.element.redis.Multi.prototype.FLUSHALL)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>FLUSHDB ()](#apidoc.element.redis.Multi.prototype.FLUSHDB)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEOADD ()](#apidoc.element.redis.Multi.prototype.GEOADD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEODIST ()](#apidoc.element.redis.Multi.prototype.GEODIST)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEOHASH ()](#apidoc.element.redis.Multi.prototype.GEOHASH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEOPOS ()](#apidoc.element.redis.Multi.prototype.GEOPOS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEORADIUS ()](#apidoc.element.redis.Multi.prototype.GEORADIUS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEORADIUSBYMEMBER ()](#apidoc.element.redis.Multi.prototype.GEORADIUSBYMEMBER)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GET ()](#apidoc.element.redis.Multi.prototype.GET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GETBIT ()](#apidoc.element.redis.Multi.prototype.GETBIT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GETRANGE ()](#apidoc.element.redis.Multi.prototype.GETRANGE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GETSET ()](#apidoc.element.redis.Multi.prototype.GETSET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HDEL ()](#apidoc.element.redis.Multi.prototype.HDEL)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HEXISTS ()](#apidoc.element.redis.Multi.prototype.HEXISTS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HGET ()](#apidoc.element.redis.Multi.prototype.HGET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HGETALL ()](#apidoc.element.redis.Multi.prototype.HGETALL)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HINCRBY ()](#apidoc.element.redis.Multi.prototype.HINCRBY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HINCRBYFLOAT ()](#apidoc.element.redis.Multi.prototype.HINCRBYFLOAT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HKEYS ()](#apidoc.element.redis.Multi.prototype.HKEYS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HLEN ()](#apidoc.element.redis.Multi.prototype.HLEN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HMGET ()](#apidoc.element.redis.Multi.prototype.HMGET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HMSET ()](#apidoc.element.redis.Multi.prototype.HMSET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSCAN ()](#apidoc.element.redis.Multi.prototype.HSCAN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSET ()](#apidoc.element.redis.Multi.prototype.HSET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSETNX ()](#apidoc.element.redis.Multi.prototype.HSETNX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSTRLEN ()](#apidoc.element.redis.Multi.prototype.HSTRLEN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HVALS ()](#apidoc.element.redis.Multi.prototype.HVALS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INCR ()](#apidoc.element.redis.Multi.prototype.INCR)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INCRBY ()](#apidoc.element.redis.Multi.prototype.INCRBY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INCRBYFLOAT ()](#apidoc.element.redis.Multi.prototype.INCRBYFLOAT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INFO (section, callback)](#apidoc.element.redis.Multi.prototype.INFO)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>KEYS ()](#apidoc.element.redis.Multi.prototype.KEYS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LASTSAVE ()](#apidoc.element.redis.Multi.prototype.LASTSAVE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LATENCY ()](#apidoc.element.redis.Multi.prototype.LATENCY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LINDEX ()](#apidoc.element.redis.Multi.prototype.LINDEX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LINSERT ()](#apidoc.element.redis.Multi.prototype.LINSERT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LLEN ()](#apidoc.element.redis.Multi.prototype.LLEN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LPOP ()](#apidoc.element.redis.Multi.prototype.LPOP)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LPUSH ()](#apidoc.element.redis.Multi.prototype.LPUSH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LPUSHX ()](#apidoc.element.redis.Multi.prototype.LPUSHX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LRANGE ()](#apidoc.element.redis.Multi.prototype.LRANGE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LREM ()](#apidoc.element.redis.Multi.prototype.LREM)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LSET ()](#apidoc.element.redis.Multi.prototype.LSET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LTRIM ()](#apidoc.element.redis.Multi.prototype.LTRIM)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MEMORY ()](#apidoc.element.redis.Multi.prototype.MEMORY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MGET ()](#apidoc.element.redis.Multi.prototype.MGET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MIGRATE ()](#apidoc.element.redis.Multi.prototype.MIGRATE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MODULE ()](#apidoc.element.redis.Multi.prototype.MODULE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MONITOR (callback)](#apidoc.element.redis.Multi.prototype.MONITOR)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MOVE ()](#apidoc.element.redis.Multi.prototype.MOVE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MSET ()](#apidoc.element.redis.Multi.prototype.MSET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MSETNX ()](#apidoc.element.redis.Multi.prototype.MSETNX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MULTI ()](#apidoc.element.redis.Multi.prototype.MULTI)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>OBJECT ()](#apidoc.element.redis.Multi.prototype.OBJECT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PERSIST ()](#apidoc.element.redis.Multi.prototype.PERSIST)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PEXPIRE ()](#apidoc.element.redis.Multi.prototype.PEXPIRE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PEXPIREAT ()](#apidoc.element.redis.Multi.prototype.PEXPIREAT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFADD ()](#apidoc.element.redis.Multi.prototype.PFADD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFCOUNT ()](#apidoc.element.redis.Multi.prototype.PFCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFDEBUG ()](#apidoc.element.redis.Multi.prototype.PFDEBUG)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFMERGE ()](#apidoc.element.redis.Multi.prototype.PFMERGE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFSELFTEST ()](#apidoc.element.redis.Multi.prototype.PFSELFTEST)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PING ()](#apidoc.element.redis.Multi.prototype.PING)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>POST ()](#apidoc.element.redis.Multi.prototype.POST)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PSETEX ()](#apidoc.element.redis.Multi.prototype.PSETEX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PSUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.PSUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PSYNC ()](#apidoc.element.redis.Multi.prototype.PSYNC)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PTTL ()](#apidoc.element.redis.Multi.prototype.PTTL)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PUBLISH ()](#apidoc.element.redis.Multi.prototype.PUBLISH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PUBSUB ()](#apidoc.element.redis.Multi.prototype.PUBSUB)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PUNSUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.PUNSUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>QUIT (callback)](#apidoc.element.redis.Multi.prototype.QUIT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RANDOMKEY ()](#apidoc.element.redis.Multi.prototype.RANDOMKEY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>READONLY ()](#apidoc.element.redis.Multi.prototype.READONLY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>READWRITE ()](#apidoc.element.redis.Multi.prototype.READWRITE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RENAME ()](#apidoc.element.redis.Multi.prototype.RENAME)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RENAMENX ()](#apidoc.element.redis.Multi.prototype.RENAMENX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>REPLCONF ()](#apidoc.element.redis.Multi.prototype.REPLCONF)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RESTORE ()](#apidoc.element.redis.Multi.prototype.RESTORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RESTORE-ASKING ()](#apidoc.element.redis.Multi.prototype.RESTORE-ASKING)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ROLE ()](#apidoc.element.redis.Multi.prototype.ROLE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPOP ()](#apidoc.element.redis.Multi.prototype.RPOP)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPOPLPUSH ()](#apidoc.element.redis.Multi.prototype.RPOPLPUSH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPUSH ()](#apidoc.element.redis.Multi.prototype.RPUSH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPUSHX ()](#apidoc.element.redis.Multi.prototype.RPUSHX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SADD ()](#apidoc.element.redis.Multi.prototype.SADD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SAVE ()](#apidoc.element.redis.Multi.prototype.SAVE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SCAN ()](#apidoc.element.redis.Multi.prototype.SCAN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SCARD ()](#apidoc.element.redis.Multi.prototype.SCARD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SCRIPT ()](#apidoc.element.redis.Multi.prototype.SCRIPT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SDIFF ()](#apidoc.element.redis.Multi.prototype.SDIFF)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SDIFFSTORE ()](#apidoc.element.redis.Multi.prototype.SDIFFSTORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SELECT (db, callback)](#apidoc.element.redis.Multi.prototype.SELECT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SET ()](#apidoc.element.redis.Multi.prototype.SET)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETBIT ()](#apidoc.element.redis.Multi.prototype.SETBIT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETEX ()](#apidoc.element.redis.Multi.prototype.SETEX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETNX ()](#apidoc.element.redis.Multi.prototype.SETNX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETRANGE ()](#apidoc.element.redis.Multi.prototype.SETRANGE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SHUTDOWN ()](#apidoc.element.redis.Multi.prototype.SHUTDOWN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SINTER ()](#apidoc.element.redis.Multi.prototype.SINTER)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SINTERSTORE ()](#apidoc.element.redis.Multi.prototype.SINTERSTORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SISMEMBER ()](#apidoc.element.redis.Multi.prototype.SISMEMBER)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SLAVEOF ()](#apidoc.element.redis.Multi.prototype.SLAVEOF)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SLOWLOG ()](#apidoc.element.redis.Multi.prototype.SLOWLOG)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SMEMBERS ()](#apidoc.element.redis.Multi.prototype.SMEMBERS)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SMOVE ()](#apidoc.element.redis.Multi.prototype.SMOVE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SORT ()](#apidoc.element.redis.Multi.prototype.SORT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SPOP ()](#apidoc.element.redis.Multi.prototype.SPOP)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SRANDMEMBER ()](#apidoc.element.redis.Multi.prototype.SRANDMEMBER)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SREM ()](#apidoc.element.redis.Multi.prototype.SREM)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SSCAN ()](#apidoc.element.redis.Multi.prototype.SSCAN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>STRLEN ()](#apidoc.element.redis.Multi.prototype.STRLEN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.SUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUBSTR ()](#apidoc.element.redis.Multi.prototype.SUBSTR)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUNION ()](#apidoc.element.redis.Multi.prototype.SUNION)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUNIONSTORE ()](#apidoc.element.redis.Multi.prototype.SUNIONSTORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SWAPDB ()](#apidoc.element.redis.Multi.prototype.SWAPDB)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SYNC ()](#apidoc.element.redis.Multi.prototype.SYNC)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TIME ()](#apidoc.element.redis.Multi.prototype.TIME)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TOUCH ()](#apidoc.element.redis.Multi.prototype.TOUCH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TTL ()](#apidoc.element.redis.Multi.prototype.TTL)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TYPE ()](#apidoc.element.redis.Multi.prototype.TYPE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>UNLINK ()](#apidoc.element.redis.Multi.prototype.UNLINK)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>UNSUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.UNSUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>UNWATCH ()](#apidoc.element.redis.Multi.prototype.UNWATCH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>WAIT ()](#apidoc.element.redis.Multi.prototype.WAIT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>WATCH ()](#apidoc.element.redis.Multi.prototype.WATCH)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZADD ()](#apidoc.element.redis.Multi.prototype.ZADD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZCARD ()](#apidoc.element.redis.Multi.prototype.ZCARD)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZCOUNT ()](#apidoc.element.redis.Multi.prototype.ZCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZINCRBY ()](#apidoc.element.redis.Multi.prototype.ZINCRBY)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZINTERSTORE ()](#apidoc.element.redis.Multi.prototype.ZINTERSTORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZLEXCOUNT ()](#apidoc.element.redis.Multi.prototype.ZLEXCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANGE ()](#apidoc.element.redis.Multi.prototype.ZRANGE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANGEBYLEX ()](#apidoc.element.redis.Multi.prototype.ZRANGEBYLEX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANGEBYSCORE ()](#apidoc.element.redis.Multi.prototype.ZRANGEBYSCORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANK ()](#apidoc.element.redis.Multi.prototype.ZRANK)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREM ()](#apidoc.element.redis.Multi.prototype.ZREM)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREMRANGEBYLEX ()](#apidoc.element.redis.Multi.prototype.ZREMRANGEBYLEX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREMRANGEBYRANK ()](#apidoc.element.redis.Multi.prototype.ZREMRANGEBYRANK)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREMRANGEBYSCORE ()](#apidoc.element.redis.Multi.prototype.ZREMRANGEBYSCORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANGE ()](#apidoc.element.redis.Multi.prototype.ZREVRANGE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANGEBYLEX ()](#apidoc.element.redis.Multi.prototype.ZREVRANGEBYLEX)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANGEBYSCORE ()](#apidoc.element.redis.Multi.prototype.ZREVRANGEBYSCORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANK ()](#apidoc.element.redis.Multi.prototype.ZREVRANK)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZSCAN ()](#apidoc.element.redis.Multi.prototype.ZSCAN)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZSCORE ()](#apidoc.element.redis.Multi.prototype.ZSCORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZUNIONSTORE ()](#apidoc.element.redis.Multi.prototype.ZUNIONSTORE)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>append ()](#apidoc.element.redis.Multi.prototype.append)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>asking ()](#apidoc.element.redis.Multi.prototype.asking)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>auth (pass, callback)](#apidoc.element.redis.Multi.prototype.auth)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bgrewriteaof ()](#apidoc.element.redis.Multi.prototype.bgrewriteaof)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bgsave ()](#apidoc.element.redis.Multi.prototype.bgsave)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitcount ()](#apidoc.element.redis.Multi.prototype.bitcount)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitfield ()](#apidoc.element.redis.Multi.prototype.bitfield)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitop ()](#apidoc.element.redis.Multi.prototype.bitop)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitpos ()](#apidoc.element.redis.Multi.prototype.bitpos)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>blpop ()](#apidoc.element.redis.Multi.prototype.blpop)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>brpop ()](#apidoc.element.redis.Multi.prototype.brpop)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>brpoplpush ()](#apidoc.element.redis.Multi.prototype.brpoplpush)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>client ()](#apidoc.element.redis.Multi.prototype.client)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>cluster ()](#apidoc.element.redis.Multi.prototype.cluster)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>command ()](#apidoc.element.redis.Multi.prototype.command)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>config ()](#apidoc.element.redis.Multi.prototype.config)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>dbsize ()](#apidoc.element.redis.Multi.prototype.dbsize)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>debug ()](#apidoc.element.redis.Multi.prototype.debug)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>decr ()](#apidoc.element.redis.Multi.prototype.decr)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>decrby ()](#apidoc.element.redis.Multi.prototype.decrby)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>del ()](#apidoc.element.redis.Multi.prototype.del)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>discard ()](#apidoc.element.redis.Multi.prototype.discard)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>dump ()](#apidoc.element.redis.Multi.prototype.dump)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>echo ()](#apidoc.element.redis.Multi.prototype.echo)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>eval ()](#apidoc.element.redis.Multi.prototype.eval)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>evalsha ()](#apidoc.element.redis.Multi.prototype.evalsha)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec (callback)](#apidoc.element.redis.Multi.prototype.exec)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>execAtomic (callback)](#apidoc.element.redis.Multi.prototype.execAtomic)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec_atomic (callback)](#apidoc.element.redis.Multi.prototype.exec_atomic)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec_batch (callback)](#apidoc.element.redis.Multi.prototype.exec_batch)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec_transaction (callback)](#apidoc.element.redis.Multi.prototype.exec_transaction)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exists ()](#apidoc.element.redis.Multi.prototype.exists)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>expire ()](#apidoc.element.redis.Multi.prototype.expire)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>expireat ()](#apidoc.element.redis.Multi.prototype.expireat)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>flushall ()](#apidoc.element.redis.Multi.prototype.flushall)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>flushdb ()](#apidoc.element.redis.Multi.prototype.flushdb)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geoadd ()](#apidoc.element.redis.Multi.prototype.geoadd)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geodist ()](#apidoc.element.redis.Multi.prototype.geodist)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geohash ()](#apidoc.element.redis.Multi.prototype.geohash)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geopos ()](#apidoc.element.redis.Multi.prototype.geopos)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>georadius ()](#apidoc.element.redis.Multi.prototype.georadius)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>georadiusbymember ()](#apidoc.element.redis.Multi.prototype.georadiusbymember)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>get ()](#apidoc.element.redis.Multi.prototype.get)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>getbit ()](#apidoc.element.redis.Multi.prototype.getbit)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>getrange ()](#apidoc.element.redis.Multi.prototype.getrange)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>getset ()](#apidoc.element.redis.Multi.prototype.getset)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hdel ()](#apidoc.element.redis.Multi.prototype.hdel)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hexists ()](#apidoc.element.redis.Multi.prototype.hexists)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hget ()](#apidoc.element.redis.Multi.prototype.hget)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hgetall ()](#apidoc.element.redis.Multi.prototype.hgetall)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hincrby ()](#apidoc.element.redis.Multi.prototype.hincrby)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hincrbyfloat ()](#apidoc.element.redis.Multi.prototype.hincrbyfloat)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hkeys ()](#apidoc.element.redis.Multi.prototype.hkeys)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hlen ()](#apidoc.element.redis.Multi.prototype.hlen)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hmget ()](#apidoc.element.redis.Multi.prototype.hmget)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hmset ()](#apidoc.element.redis.Multi.prototype.hmset)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hscan ()](#apidoc.element.redis.Multi.prototype.hscan)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hset ()](#apidoc.element.redis.Multi.prototype.hset)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hsetnx ()](#apidoc.element.redis.Multi.prototype.hsetnx)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hstrlen ()](#apidoc.element.redis.Multi.prototype.hstrlen)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hvals ()](#apidoc.element.redis.Multi.prototype.hvals)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>incr ()](#apidoc.element.redis.Multi.prototype.incr)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>incrby ()](#apidoc.element.redis.Multi.prototype.incrby)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>incrbyfloat ()](#apidoc.element.redis.Multi.prototype.incrbyfloat)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>info (section, callback)](#apidoc.element.redis.Multi.prototype.info)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>keys ()](#apidoc.element.redis.Multi.prototype.keys)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lastsave ()](#apidoc.element.redis.Multi.prototype.lastsave)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>latency ()](#apidoc.element.redis.Multi.prototype.latency)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lindex ()](#apidoc.element.redis.Multi.prototype.lindex)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>linsert ()](#apidoc.element.redis.Multi.prototype.linsert)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>llen ()](#apidoc.element.redis.Multi.prototype.llen)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lpop ()](#apidoc.element.redis.Multi.prototype.lpop)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lpush ()](#apidoc.element.redis.Multi.prototype.lpush)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lpushx ()](#apidoc.element.redis.Multi.prototype.lpushx)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lrange ()](#apidoc.element.redis.Multi.prototype.lrange)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lrem ()](#apidoc.element.redis.Multi.prototype.lrem)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lset ()](#apidoc.element.redis.Multi.prototype.lset)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ltrim ()](#apidoc.element.redis.Multi.prototype.ltrim)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>memory ()](#apidoc.element.redis.Multi.prototype.memory)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>mget ()](#apidoc.element.redis.Multi.prototype.mget)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>migrate ()](#apidoc.element.redis.Multi.prototype.migrate)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>module ()](#apidoc.element.redis.Multi.prototype.module)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>monitor (callback)](#apidoc.element.redis.Multi.prototype.monitor)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>move ()](#apidoc.element.redis.Multi.prototype.move)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>mset ()](#apidoc.element.redis.Multi.prototype.mset)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>msetnx ()](#apidoc.element.redis.Multi.prototype.msetnx)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>multi ()](#apidoc.element.redis.Multi.prototype.multi)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>object ()](#apidoc.element.redis.Multi.prototype.object)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>persist ()](#apidoc.element.redis.Multi.prototype.persist)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pexpire ()](#apidoc.element.redis.Multi.prototype.pexpire)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pexpireat ()](#apidoc.element.redis.Multi.prototype.pexpireat)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfadd ()](#apidoc.element.redis.Multi.prototype.pfadd)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfcount ()](#apidoc.element.redis.Multi.prototype.pfcount)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfdebug ()](#apidoc.element.redis.Multi.prototype.pfdebug)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfmerge ()](#apidoc.element.redis.Multi.prototype.pfmerge)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfselftest ()](#apidoc.element.redis.Multi.prototype.pfselftest)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ping ()](#apidoc.element.redis.Multi.prototype.ping)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>post ()](#apidoc.element.redis.Multi.prototype.post)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>psetex ()](#apidoc.element.redis.Multi.prototype.psetex)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>psubscribe ()](#apidoc.element.redis.Multi.prototype.psubscribe)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>psync ()](#apidoc.element.redis.Multi.prototype.psync)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pttl ()](#apidoc.element.redis.Multi.prototype.pttl)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>publish ()](#apidoc.element.redis.Multi.prototype.publish)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pubsub ()](#apidoc.element.redis.Multi.prototype.pubsub)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>punsubscribe ()](#apidoc.element.redis.Multi.prototype.punsubscribe)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>quit (callback)](#apidoc.element.redis.Multi.prototype.quit)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>randomkey ()](#apidoc.element.redis.Multi.prototype.randomkey)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>readonly ()](#apidoc.element.redis.Multi.prototype.readonly)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>readwrite ()](#apidoc.element.redis.Multi.prototype.readwrite)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rename ()](#apidoc.element.redis.Multi.prototype.rename)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>renamenx ()](#apidoc.element.redis.Multi.prototype.renamenx)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>replconf ()](#apidoc.element.redis.Multi.prototype.replconf)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>restore ()](#apidoc.element.redis.Multi.prototype.restore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>restore-asking ()](#apidoc.element.redis.Multi.prototype.restore-asking)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>role ()](#apidoc.element.redis.Multi.prototype.role)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpop ()](#apidoc.element.redis.Multi.prototype.rpop)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpoplpush ()](#apidoc.element.redis.Multi.prototype.rpoplpush)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpush ()](#apidoc.element.redis.Multi.prototype.rpush)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpushx ()](#apidoc.element.redis.Multi.prototype.rpushx)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sadd ()](#apidoc.element.redis.Multi.prototype.sadd)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>save ()](#apidoc.element.redis.Multi.prototype.save)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>scan ()](#apidoc.element.redis.Multi.prototype.scan)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>scard ()](#apidoc.element.redis.Multi.prototype.scard)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>script ()](#apidoc.element.redis.Multi.prototype.script)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sdiff ()](#apidoc.element.redis.Multi.prototype.sdiff)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sdiffstore ()](#apidoc.element.redis.Multi.prototype.sdiffstore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>select (db, callback)](#apidoc.element.redis.Multi.prototype.select)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>set ()](#apidoc.element.redis.Multi.prototype.set)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setbit ()](#apidoc.element.redis.Multi.prototype.setbit)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setex ()](#apidoc.element.redis.Multi.prototype.setex)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setnx ()](#apidoc.element.redis.Multi.prototype.setnx)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setrange ()](#apidoc.element.redis.Multi.prototype.setrange)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>shutdown ()](#apidoc.element.redis.Multi.prototype.shutdown)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sinter ()](#apidoc.element.redis.Multi.prototype.sinter)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sinterstore ()](#apidoc.element.redis.Multi.prototype.sinterstore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sismember ()](#apidoc.element.redis.Multi.prototype.sismember)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>slaveof ()](#apidoc.element.redis.Multi.prototype.slaveof)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>slowlog ()](#apidoc.element.redis.Multi.prototype.slowlog)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>smembers ()](#apidoc.element.redis.Multi.prototype.smembers)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>smove ()](#apidoc.element.redis.Multi.prototype.smove)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sort ()](#apidoc.element.redis.Multi.prototype.sort)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>spop ()](#apidoc.element.redis.Multi.prototype.spop)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>srandmember ()](#apidoc.element.redis.Multi.prototype.srandmember)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>srem ()](#apidoc.element.redis.Multi.prototype.srem)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sscan ()](#apidoc.element.redis.Multi.prototype.sscan)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>strlen ()](#apidoc.element.redis.Multi.prototype.strlen)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>subscribe ()](#apidoc.element.redis.Multi.prototype.subscribe)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>substr ()](#apidoc.element.redis.Multi.prototype.substr)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sunion ()](#apidoc.element.redis.Multi.prototype.sunion)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sunionstore ()](#apidoc.element.redis.Multi.prototype.sunionstore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>swapdb ()](#apidoc.element.redis.Multi.prototype.swapdb)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sync ()](#apidoc.element.redis.Multi.prototype.sync)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>time ()](#apidoc.element.redis.Multi.prototype.time)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>touch ()](#apidoc.element.redis.Multi.prototype.touch)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ttl ()](#apidoc.element.redis.Multi.prototype.ttl)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>type ()](#apidoc.element.redis.Multi.prototype.type)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>unlink ()](#apidoc.element.redis.Multi.prototype.unlink)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>unsubscribe ()](#apidoc.element.redis.Multi.prototype.unsubscribe)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>unwatch ()](#apidoc.element.redis.Multi.prototype.unwatch)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>wait ()](#apidoc.element.redis.Multi.prototype.wait)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>watch ()](#apidoc.element.redis.Multi.prototype.watch)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zadd ()](#apidoc.element.redis.Multi.prototype.zadd)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zcard ()](#apidoc.element.redis.Multi.prototype.zcard)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zcount ()](#apidoc.element.redis.Multi.prototype.zcount)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zincrby ()](#apidoc.element.redis.Multi.prototype.zincrby)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zinterstore ()](#apidoc.element.redis.Multi.prototype.zinterstore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zlexcount ()](#apidoc.element.redis.Multi.prototype.zlexcount)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrange ()](#apidoc.element.redis.Multi.prototype.zrange)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrangebylex ()](#apidoc.element.redis.Multi.prototype.zrangebylex)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrangebyscore ()](#apidoc.element.redis.Multi.prototype.zrangebyscore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrank ()](#apidoc.element.redis.Multi.prototype.zrank)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrem ()](#apidoc.element.redis.Multi.prototype.zrem)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zremrangebylex ()](#apidoc.element.redis.Multi.prototype.zremrangebylex)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zremrangebyrank ()](#apidoc.element.redis.Multi.prototype.zremrangebyrank)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zremrangebyscore ()](#apidoc.element.redis.Multi.prototype.zremrangebyscore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrange ()](#apidoc.element.redis.Multi.prototype.zrevrange)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrangebylex ()](#apidoc.element.redis.Multi.prototype.zrevrangebylex)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrangebyscore ()](#apidoc.element.redis.Multi.prototype.zrevrangebyscore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrank ()](#apidoc.element.redis.Multi.prototype.zrevrank)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zscan ()](#apidoc.element.redis.Multi.prototype.zscan)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zscore ()](#apidoc.element.redis.Multi.prototype.zscore)
1.  [function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zunionstore ()](#apidoc.element.redis.Multi.prototype.zunionstore)

#### [module redis.RedisClient](#apidoc.module.redis.RedisClient)
1.  [function <span class="apidocSignatureSpan">redis.</span>RedisClient (options, stream)](#apidoc.element.redis.RedisClient.RedisClient)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.</span>super_ ()](#apidoc.element.redis.RedisClient.super_)
1.  number <span class="apidocSignatureSpan">redis.RedisClient.</span>connection_id

#### [module redis.RedisClient.prototype](#apidoc.module.redis.RedisClient.prototype)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>APPEND ()](#apidoc.element.redis.RedisClient.prototype.APPEND)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ASKING ()](#apidoc.element.redis.RedisClient.prototype.ASKING)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>AUTH (pass, callback)](#apidoc.element.redis.RedisClient.prototype.AUTH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BATCH (args)](#apidoc.element.redis.RedisClient.prototype.BATCH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BGREWRITEAOF ()](#apidoc.element.redis.RedisClient.prototype.BGREWRITEAOF)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BGSAVE ()](#apidoc.element.redis.RedisClient.prototype.BGSAVE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITCOUNT ()](#apidoc.element.redis.RedisClient.prototype.BITCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITFIELD ()](#apidoc.element.redis.RedisClient.prototype.BITFIELD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITOP ()](#apidoc.element.redis.RedisClient.prototype.BITOP)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITPOS ()](#apidoc.element.redis.RedisClient.prototype.BITPOS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BLPOP ()](#apidoc.element.redis.RedisClient.prototype.BLPOP)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BRPOP ()](#apidoc.element.redis.RedisClient.prototype.BRPOP)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BRPOPLPUSH ()](#apidoc.element.redis.RedisClient.prototype.BRPOPLPUSH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>CLIENT ()](#apidoc.element.redis.RedisClient.prototype.CLIENT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>CLUSTER ()](#apidoc.element.redis.RedisClient.prototype.CLUSTER)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>COMMAND ()](#apidoc.element.redis.RedisClient.prototype.COMMAND)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>CONFIG ()](#apidoc.element.redis.RedisClient.prototype.CONFIG)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DBSIZE ()](#apidoc.element.redis.RedisClient.prototype.DBSIZE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DEBUG ()](#apidoc.element.redis.RedisClient.prototype.DEBUG)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DECR ()](#apidoc.element.redis.RedisClient.prototype.DECR)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DECRBY ()](#apidoc.element.redis.RedisClient.prototype.DECRBY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DEL ()](#apidoc.element.redis.RedisClient.prototype.DEL)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DISCARD ()](#apidoc.element.redis.RedisClient.prototype.DISCARD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DUMP ()](#apidoc.element.redis.RedisClient.prototype.DUMP)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ECHO ()](#apidoc.element.redis.RedisClient.prototype.ECHO)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EVAL ()](#apidoc.element.redis.RedisClient.prototype.EVAL)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EVALSHA ()](#apidoc.element.redis.RedisClient.prototype.EVALSHA)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXEC ()](#apidoc.element.redis.RedisClient.prototype.EXEC)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXISTS ()](#apidoc.element.redis.RedisClient.prototype.EXISTS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXPIRE ()](#apidoc.element.redis.RedisClient.prototype.EXPIRE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXPIREAT ()](#apidoc.element.redis.RedisClient.prototype.EXPIREAT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>FLUSHALL ()](#apidoc.element.redis.RedisClient.prototype.FLUSHALL)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>FLUSHDB ()](#apidoc.element.redis.RedisClient.prototype.FLUSHDB)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEOADD ()](#apidoc.element.redis.RedisClient.prototype.GEOADD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEODIST ()](#apidoc.element.redis.RedisClient.prototype.GEODIST)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEOHASH ()](#apidoc.element.redis.RedisClient.prototype.GEOHASH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEOPOS ()](#apidoc.element.redis.RedisClient.prototype.GEOPOS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEORADIUS ()](#apidoc.element.redis.RedisClient.prototype.GEORADIUS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEORADIUSBYMEMBER ()](#apidoc.element.redis.RedisClient.prototype.GEORADIUSBYMEMBER)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GET ()](#apidoc.element.redis.RedisClient.prototype.GET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GETBIT ()](#apidoc.element.redis.RedisClient.prototype.GETBIT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GETRANGE ()](#apidoc.element.redis.RedisClient.prototype.GETRANGE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GETSET ()](#apidoc.element.redis.RedisClient.prototype.GETSET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HDEL ()](#apidoc.element.redis.RedisClient.prototype.HDEL)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HEXISTS ()](#apidoc.element.redis.RedisClient.prototype.HEXISTS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HGET ()](#apidoc.element.redis.RedisClient.prototype.HGET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HGETALL ()](#apidoc.element.redis.RedisClient.prototype.HGETALL)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HINCRBY ()](#apidoc.element.redis.RedisClient.prototype.HINCRBY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HINCRBYFLOAT ()](#apidoc.element.redis.RedisClient.prototype.HINCRBYFLOAT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HKEYS ()](#apidoc.element.redis.RedisClient.prototype.HKEYS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HLEN ()](#apidoc.element.redis.RedisClient.prototype.HLEN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HMGET ()](#apidoc.element.redis.RedisClient.prototype.HMGET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HMSET ()](#apidoc.element.redis.RedisClient.prototype.HMSET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSCAN ()](#apidoc.element.redis.RedisClient.prototype.HSCAN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSET ()](#apidoc.element.redis.RedisClient.prototype.HSET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSETNX ()](#apidoc.element.redis.RedisClient.prototype.HSETNX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSTRLEN ()](#apidoc.element.redis.RedisClient.prototype.HSTRLEN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HVALS ()](#apidoc.element.redis.RedisClient.prototype.HVALS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INCR ()](#apidoc.element.redis.RedisClient.prototype.INCR)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INCRBY ()](#apidoc.element.redis.RedisClient.prototype.INCRBY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INCRBYFLOAT ()](#apidoc.element.redis.RedisClient.prototype.INCRBYFLOAT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INFO (section, callback)](#apidoc.element.redis.RedisClient.prototype.INFO)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>KEYS ()](#apidoc.element.redis.RedisClient.prototype.KEYS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LASTSAVE ()](#apidoc.element.redis.RedisClient.prototype.LASTSAVE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LATENCY ()](#apidoc.element.redis.RedisClient.prototype.LATENCY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LINDEX ()](#apidoc.element.redis.RedisClient.prototype.LINDEX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LINSERT ()](#apidoc.element.redis.RedisClient.prototype.LINSERT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LLEN ()](#apidoc.element.redis.RedisClient.prototype.LLEN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LPOP ()](#apidoc.element.redis.RedisClient.prototype.LPOP)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LPUSH ()](#apidoc.element.redis.RedisClient.prototype.LPUSH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LPUSHX ()](#apidoc.element.redis.RedisClient.prototype.LPUSHX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LRANGE ()](#apidoc.element.redis.RedisClient.prototype.LRANGE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LREM ()](#apidoc.element.redis.RedisClient.prototype.LREM)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LSET ()](#apidoc.element.redis.RedisClient.prototype.LSET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LTRIM ()](#apidoc.element.redis.RedisClient.prototype.LTRIM)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MEMORY ()](#apidoc.element.redis.RedisClient.prototype.MEMORY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MGET ()](#apidoc.element.redis.RedisClient.prototype.MGET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MIGRATE ()](#apidoc.element.redis.RedisClient.prototype.MIGRATE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MODULE ()](#apidoc.element.redis.RedisClient.prototype.MODULE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MONITOR (callback)](#apidoc.element.redis.RedisClient.prototype.MONITOR)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MOVE ()](#apidoc.element.redis.RedisClient.prototype.MOVE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MSET ()](#apidoc.element.redis.RedisClient.prototype.MSET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MSETNX ()](#apidoc.element.redis.RedisClient.prototype.MSETNX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MULTI (args)](#apidoc.element.redis.RedisClient.prototype.MULTI)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>OBJECT ()](#apidoc.element.redis.RedisClient.prototype.OBJECT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PERSIST ()](#apidoc.element.redis.RedisClient.prototype.PERSIST)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PEXPIRE ()](#apidoc.element.redis.RedisClient.prototype.PEXPIRE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PEXPIREAT ()](#apidoc.element.redis.RedisClient.prototype.PEXPIREAT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFADD ()](#apidoc.element.redis.RedisClient.prototype.PFADD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFCOUNT ()](#apidoc.element.redis.RedisClient.prototype.PFCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFDEBUG ()](#apidoc.element.redis.RedisClient.prototype.PFDEBUG)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFMERGE ()](#apidoc.element.redis.RedisClient.prototype.PFMERGE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFSELFTEST ()](#apidoc.element.redis.RedisClient.prototype.PFSELFTEST)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PING ()](#apidoc.element.redis.RedisClient.prototype.PING)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>POST ()](#apidoc.element.redis.RedisClient.prototype.POST)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PSETEX ()](#apidoc.element.redis.RedisClient.prototype.PSETEX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PSUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.PSUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PSYNC ()](#apidoc.element.redis.RedisClient.prototype.PSYNC)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PTTL ()](#apidoc.element.redis.RedisClient.prototype.PTTL)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PUBLISH ()](#apidoc.element.redis.RedisClient.prototype.PUBLISH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PUBSUB ()](#apidoc.element.redis.RedisClient.prototype.PUBSUB)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PUNSUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.PUNSUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>QUIT (callback)](#apidoc.element.redis.RedisClient.prototype.QUIT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RANDOMKEY ()](#apidoc.element.redis.RedisClient.prototype.RANDOMKEY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>READONLY ()](#apidoc.element.redis.RedisClient.prototype.READONLY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>READWRITE ()](#apidoc.element.redis.RedisClient.prototype.READWRITE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RENAME ()](#apidoc.element.redis.RedisClient.prototype.RENAME)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RENAMENX ()](#apidoc.element.redis.RedisClient.prototype.RENAMENX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>REPLCONF ()](#apidoc.element.redis.RedisClient.prototype.REPLCONF)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RESTORE ()](#apidoc.element.redis.RedisClient.prototype.RESTORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RESTORE-ASKING ()](#apidoc.element.redis.RedisClient.prototype.RESTORE-ASKING)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ROLE ()](#apidoc.element.redis.RedisClient.prototype.ROLE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPOP ()](#apidoc.element.redis.RedisClient.prototype.RPOP)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPOPLPUSH ()](#apidoc.element.redis.RedisClient.prototype.RPOPLPUSH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPUSH ()](#apidoc.element.redis.RedisClient.prototype.RPUSH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPUSHX ()](#apidoc.element.redis.RedisClient.prototype.RPUSHX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SADD ()](#apidoc.element.redis.RedisClient.prototype.SADD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SAVE ()](#apidoc.element.redis.RedisClient.prototype.SAVE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SCAN ()](#apidoc.element.redis.RedisClient.prototype.SCAN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SCARD ()](#apidoc.element.redis.RedisClient.prototype.SCARD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SCRIPT ()](#apidoc.element.redis.RedisClient.prototype.SCRIPT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SDIFF ()](#apidoc.element.redis.RedisClient.prototype.SDIFF)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SDIFFSTORE ()](#apidoc.element.redis.RedisClient.prototype.SDIFFSTORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SELECT (db, callback)](#apidoc.element.redis.RedisClient.prototype.SELECT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SET ()](#apidoc.element.redis.RedisClient.prototype.SET)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETBIT ()](#apidoc.element.redis.RedisClient.prototype.SETBIT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETEX ()](#apidoc.element.redis.RedisClient.prototype.SETEX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETNX ()](#apidoc.element.redis.RedisClient.prototype.SETNX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETRANGE ()](#apidoc.element.redis.RedisClient.prototype.SETRANGE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SHUTDOWN ()](#apidoc.element.redis.RedisClient.prototype.SHUTDOWN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SINTER ()](#apidoc.element.redis.RedisClient.prototype.SINTER)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SINTERSTORE ()](#apidoc.element.redis.RedisClient.prototype.SINTERSTORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SISMEMBER ()](#apidoc.element.redis.RedisClient.prototype.SISMEMBER)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SLAVEOF ()](#apidoc.element.redis.RedisClient.prototype.SLAVEOF)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SLOWLOG ()](#apidoc.element.redis.RedisClient.prototype.SLOWLOG)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SMEMBERS ()](#apidoc.element.redis.RedisClient.prototype.SMEMBERS)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SMOVE ()](#apidoc.element.redis.RedisClient.prototype.SMOVE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SORT ()](#apidoc.element.redis.RedisClient.prototype.SORT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SPOP ()](#apidoc.element.redis.RedisClient.prototype.SPOP)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SRANDMEMBER ()](#apidoc.element.redis.RedisClient.prototype.SRANDMEMBER)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SREM ()](#apidoc.element.redis.RedisClient.prototype.SREM)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SSCAN ()](#apidoc.element.redis.RedisClient.prototype.SSCAN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>STRLEN ()](#apidoc.element.redis.RedisClient.prototype.STRLEN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.SUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUBSTR ()](#apidoc.element.redis.RedisClient.prototype.SUBSTR)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUNION ()](#apidoc.element.redis.RedisClient.prototype.SUNION)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUNIONSTORE ()](#apidoc.element.redis.RedisClient.prototype.SUNIONSTORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SWAPDB ()](#apidoc.element.redis.RedisClient.prototype.SWAPDB)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SYNC ()](#apidoc.element.redis.RedisClient.prototype.SYNC)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TIME ()](#apidoc.element.redis.RedisClient.prototype.TIME)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TOUCH ()](#apidoc.element.redis.RedisClient.prototype.TOUCH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TTL ()](#apidoc.element.redis.RedisClient.prototype.TTL)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TYPE ()](#apidoc.element.redis.RedisClient.prototype.TYPE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>UNLINK ()](#apidoc.element.redis.RedisClient.prototype.UNLINK)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>UNSUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.UNSUBSCRIBE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>UNWATCH ()](#apidoc.element.redis.RedisClient.prototype.UNWATCH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>WAIT ()](#apidoc.element.redis.RedisClient.prototype.WAIT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>WATCH ()](#apidoc.element.redis.RedisClient.prototype.WATCH)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZADD ()](#apidoc.element.redis.RedisClient.prototype.ZADD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZCARD ()](#apidoc.element.redis.RedisClient.prototype.ZCARD)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZCOUNT ()](#apidoc.element.redis.RedisClient.prototype.ZCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZINCRBY ()](#apidoc.element.redis.RedisClient.prototype.ZINCRBY)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZINTERSTORE ()](#apidoc.element.redis.RedisClient.prototype.ZINTERSTORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZLEXCOUNT ()](#apidoc.element.redis.RedisClient.prototype.ZLEXCOUNT)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANGE ()](#apidoc.element.redis.RedisClient.prototype.ZRANGE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANGEBYLEX ()](#apidoc.element.redis.RedisClient.prototype.ZRANGEBYLEX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANGEBYSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZRANGEBYSCORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANK ()](#apidoc.element.redis.RedisClient.prototype.ZRANK)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREM ()](#apidoc.element.redis.RedisClient.prototype.ZREM)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREMRANGEBYLEX ()](#apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYLEX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREMRANGEBYRANK ()](#apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYRANK)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREMRANGEBYSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYSCORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANGE ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANGE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANGEBYLEX ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANGEBYLEX)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANGEBYSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANGEBYSCORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANK ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANK)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZSCAN ()](#apidoc.element.redis.RedisClient.prototype.ZSCAN)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZSCORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZUNIONSTORE ()](#apidoc.element.redis.RedisClient.prototype.ZUNIONSTORE)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>append ()](#apidoc.element.redis.RedisClient.prototype.append)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>asking ()](#apidoc.element.redis.RedisClient.prototype.asking)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>auth (pass, callback)](#apidoc.element.redis.RedisClient.prototype.auth)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>batch (args)](#apidoc.element.redis.RedisClient.prototype.batch)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bgrewriteaof ()](#apidoc.element.redis.RedisClient.prototype.bgrewriteaof)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bgsave ()](#apidoc.element.redis.RedisClient.prototype.bgsave)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitcount ()](#apidoc.element.redis.RedisClient.prototype.bitcount)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitfield ()](#apidoc.element.redis.RedisClient.prototype.bitfield)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitop ()](#apidoc.element.redis.RedisClient.prototype.bitop)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitpos ()](#apidoc.element.redis.RedisClient.prototype.bitpos)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>blpop ()](#apidoc.element.redis.RedisClient.prototype.blpop)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>brpop ()](#apidoc.element.redis.RedisClient.prototype.brpop)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>brpoplpush ()](#apidoc.element.redis.RedisClient.prototype.brpoplpush)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>client ()](#apidoc.element.redis.RedisClient.prototype.client)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>cluster ()](#apidoc.element.redis.RedisClient.prototype.cluster)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>command ()](#apidoc.element.redis.RedisClient.prototype.command)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>config ()](#apidoc.element.redis.RedisClient.prototype.config)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>connection_gone (why, error)](#apidoc.element.redis.RedisClient.prototype.connection_gone)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>cork ()](#apidoc.element.redis.RedisClient.prototype.cork)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>create_stream ()](#apidoc.element.redis.RedisClient.prototype.create_stream)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>dbsize ()](#apidoc.element.redis.RedisClient.prototype.dbsize)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>debug ()](#apidoc.element.redis.RedisClient.prototype.debug)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>decr ()](#apidoc.element.redis.RedisClient.prototype.decr)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>decrby ()](#apidoc.element.redis.RedisClient.prototype.decrby)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>del ()](#apidoc.element.redis.RedisClient.prototype.del)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>discard ()](#apidoc.element.redis.RedisClient.prototype.discard)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>drain ()](#apidoc.element.redis.RedisClient.prototype.drain)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>dump ()](#apidoc.element.redis.RedisClient.prototype.dump)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>duplicate (options, callback)](#apidoc.element.redis.RedisClient.prototype.duplicate)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>echo ()](#apidoc.element.redis.RedisClient.prototype.echo)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>emit_idle ()](#apidoc.element.redis.RedisClient.prototype.emit_idle)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>end (flush)](#apidoc.element.redis.RedisClient.prototype.end)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>eval ()](#apidoc.element.redis.RedisClient.prototype.eval)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>evalsha ()](#apidoc.element.redis.RedisClient.prototype.evalsha)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>exec ()](#apidoc.element.redis.RedisClient.prototype.exec)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>exists ()](#apidoc.element.redis.RedisClient.prototype.exists)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>expire ()](#apidoc.element.redis.RedisClient.prototype.expire)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>expireat ()](#apidoc.element.redis.RedisClient.prototype.expireat)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>flush_and_error (error_attributes, options)](#apidoc.element.redis.RedisClient.prototype.flush_and_error)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>flushall ()](#apidoc.element.redis.RedisClient.prototype.flushall)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>flushdb ()](#apidoc.element.redis.RedisClient.prototype.flushdb)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geoadd ()](#apidoc.element.redis.RedisClient.prototype.geoadd)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geodist ()](#apidoc.element.redis.RedisClient.prototype.geodist)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geohash ()](#apidoc.element.redis.RedisClient.prototype.geohash)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geopos ()](#apidoc.element.redis.RedisClient.prototype.geopos)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>georadius ()](#apidoc.element.redis.RedisClient.prototype.georadius)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>georadiusbymember ()](#apidoc.element.redis.RedisClient.prototype.georadiusbymember)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>get ()](#apidoc.element.redis.RedisClient.prototype.get)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>getbit ()](#apidoc.element.redis.RedisClient.prototype.getbit)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>getrange ()](#apidoc.element.redis.RedisClient.prototype.getrange)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>getset ()](#apidoc.element.redis.RedisClient.prototype.getset)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>handle_reply (reply, command)](#apidoc.element.redis.RedisClient.prototype.handle_reply)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hdel ()](#apidoc.element.redis.RedisClient.prototype.hdel)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hexists ()](#apidoc.element.redis.RedisClient.prototype.hexists)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hget ()](#apidoc.element.redis.RedisClient.prototype.hget)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hgetall ()](#apidoc.element.redis.RedisClient.prototype.hgetall)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hincrby ()](#apidoc.element.redis.RedisClient.prototype.hincrby)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hincrbyfloat ()](#apidoc.element.redis.RedisClient.prototype.hincrbyfloat)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hkeys ()](#apidoc.element.redis.RedisClient.prototype.hkeys)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hlen ()](#apidoc.element.redis.RedisClient.prototype.hlen)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hmget ()](#apidoc.element.redis.RedisClient.prototype.hmget)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hmset ()](#apidoc.element.redis.RedisClient.prototype.hmset)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hscan ()](#apidoc.element.redis.RedisClient.prototype.hscan)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hset ()](#apidoc.element.redis.RedisClient.prototype.hset)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hsetnx ()](#apidoc.element.redis.RedisClient.prototype.hsetnx)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hstrlen ()](#apidoc.element.redis.RedisClient.prototype.hstrlen)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hvals ()](#apidoc.element.redis.RedisClient.prototype.hvals)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>incr ()](#apidoc.element.redis.RedisClient.prototype.incr)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>incrby ()](#apidoc.element.redis.RedisClient.prototype.incrby)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>incrbyfloat ()](#apidoc.element.redis.RedisClient.prototype.incrbyfloat)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>info (section, callback)](#apidoc.element.redis.RedisClient.prototype.info)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>initialize_retry_vars ()](#apidoc.element.redis.RedisClient.prototype.initialize_retry_vars)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>internal_send_command (command_obj)](#apidoc.element.redis.RedisClient.prototype.internal_send_command)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>keys ()](#apidoc.element.redis.RedisClient.prototype.keys)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lastsave ()](#apidoc.element.redis.RedisClient.prototype.lastsave)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>latency ()](#apidoc.element.redis.RedisClient.prototype.latency)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lindex ()](#apidoc.element.redis.RedisClient.prototype.lindex)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>linsert ()](#apidoc.element.redis.RedisClient.prototype.linsert)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>llen ()](#apidoc.element.redis.RedisClient.prototype.llen)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lpop ()](#apidoc.element.redis.RedisClient.prototype.lpop)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lpush ()](#apidoc.element.redis.RedisClient.prototype.lpush)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lpushx ()](#apidoc.element.redis.RedisClient.prototype.lpushx)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lrange ()](#apidoc.element.redis.RedisClient.prototype.lrange)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lrem ()](#apidoc.element.redis.RedisClient.prototype.lrem)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lset ()](#apidoc.element.redis.RedisClient.prototype.lset)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ltrim ()](#apidoc.element.redis.RedisClient.prototype.ltrim)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>memory ()](#apidoc.element.redis.RedisClient.prototype.memory)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>mget ()](#apidoc.element.redis.RedisClient.prototype.mget)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>migrate ()](#apidoc.element.redis.RedisClient.prototype.migrate)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>module ()](#apidoc.element.redis.RedisClient.prototype.module)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>monitor (callback)](#apidoc.element.redis.RedisClient.prototype.monitor)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>move ()](#apidoc.element.redis.RedisClient.prototype.move)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>mset ()](#apidoc.element.redis.RedisClient.prototype.mset)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>msetnx ()](#apidoc.element.redis.RedisClient.prototype.msetnx)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>multi (args)](#apidoc.element.redis.RedisClient.prototype.multi)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>object ()](#apidoc.element.redis.RedisClient.prototype.object)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_connect ()](#apidoc.element.redis.RedisClient.prototype.on_connect)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_error (err)](#apidoc.element.redis.RedisClient.prototype.on_error)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_info_cmd (err, res)](#apidoc.element.redis.RedisClient.prototype.on_info_cmd)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_ready ()](#apidoc.element.redis.RedisClient.prototype.on_ready)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>persist ()](#apidoc.element.redis.RedisClient.prototype.persist)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pexpire ()](#apidoc.element.redis.RedisClient.prototype.pexpire)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pexpireat ()](#apidoc.element.redis.RedisClient.prototype.pexpireat)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfadd ()](#apidoc.element.redis.RedisClient.prototype.pfadd)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfcount ()](#apidoc.element.redis.RedisClient.prototype.pfcount)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfdebug ()](#apidoc.element.redis.RedisClient.prototype.pfdebug)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfmerge ()](#apidoc.element.redis.RedisClient.prototype.pfmerge)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfselftest ()](#apidoc.element.redis.RedisClient.prototype.pfselftest)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ping ()](#apidoc.element.redis.RedisClient.prototype.ping)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>post ()](#apidoc.element.redis.RedisClient.prototype.post)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>psetex ()](#apidoc.element.redis.RedisClient.prototype.psetex)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>psubscribe ()](#apidoc.element.redis.RedisClient.prototype.psubscribe)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>psync ()](#apidoc.element.redis.RedisClient.prototype.psync)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pttl ()](#apidoc.element.redis.RedisClient.prototype.pttl)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>publish ()](#apidoc.element.redis.RedisClient.prototype.publish)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pubsub ()](#apidoc.element.redis.RedisClient.prototype.pubsub)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>punsubscribe ()](#apidoc.element.redis.RedisClient.prototype.punsubscribe)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>quit (callback)](#apidoc.element.redis.RedisClient.prototype.quit)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>randomkey ()](#apidoc.element.redis.RedisClient.prototype.randomkey)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>readonly ()](#apidoc.element.redis.RedisClient.prototype.readonly)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>readwrite ()](#apidoc.element.redis.RedisClient.prototype.readwrite)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ready_check ()](#apidoc.element.redis.RedisClient.prototype.ready_check)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rename ()](#apidoc.element.redis.RedisClient.prototype.rename)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>renamenx ()](#apidoc.element.redis.RedisClient.prototype.renamenx)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>replconf ()](#apidoc.element.redis.RedisClient.prototype.replconf)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>restore ()](#apidoc.element.redis.RedisClient.prototype.restore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>restore-asking ()](#apidoc.element.redis.RedisClient.prototype.restore-asking)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>return_error (err)](#apidoc.element.redis.RedisClient.prototype.return_error)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>return_reply (reply)](#apidoc.element.redis.RedisClient.prototype.return_reply)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>role ()](#apidoc.element.redis.RedisClient.prototype.role)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpop ()](#apidoc.element.redis.RedisClient.prototype.rpop)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpoplpush ()](#apidoc.element.redis.RedisClient.prototype.rpoplpush)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpush ()](#apidoc.element.redis.RedisClient.prototype.rpush)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpushx ()](#apidoc.element.redis.RedisClient.prototype.rpushx)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sadd ()](#apidoc.element.redis.RedisClient.prototype.sadd)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>save ()](#apidoc.element.redis.RedisClient.prototype.save)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>scan ()](#apidoc.element.redis.RedisClient.prototype.scan)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>scard ()](#apidoc.element.redis.RedisClient.prototype.scard)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>script ()](#apidoc.element.redis.RedisClient.prototype.script)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sdiff ()](#apidoc.element.redis.RedisClient.prototype.sdiff)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sdiffstore ()](#apidoc.element.redis.RedisClient.prototype.sdiffstore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>select (db, callback)](#apidoc.element.redis.RedisClient.prototype.select)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sendCommand (command, args, callback)](#apidoc.element.redis.RedisClient.prototype.sendCommand)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>send_command (command, args, callback)](#apidoc.element.redis.RedisClient.prototype.send_command)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>send_offline_queue ()](#apidoc.element.redis.RedisClient.prototype.send_offline_queue)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>set ()](#apidoc.element.redis.RedisClient.prototype.set)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setbit ()](#apidoc.element.redis.RedisClient.prototype.setbit)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setex ()](#apidoc.element.redis.RedisClient.prototype.setex)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setnx ()](#apidoc.element.redis.RedisClient.prototype.setnx)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setrange ()](#apidoc.element.redis.RedisClient.prototype.setrange)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>shutdown ()](#apidoc.element.redis.RedisClient.prototype.shutdown)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sinter ()](#apidoc.element.redis.RedisClient.prototype.sinter)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sinterstore ()](#apidoc.element.redis.RedisClient.prototype.sinterstore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sismember ()](#apidoc.element.redis.RedisClient.prototype.sismember)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>slaveof ()](#apidoc.element.redis.RedisClient.prototype.slaveof)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>slowlog ()](#apidoc.element.redis.RedisClient.prototype.slowlog)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>smembers ()](#apidoc.element.redis.RedisClient.prototype.smembers)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>smove ()](#apidoc.element.redis.RedisClient.prototype.smove)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sort ()](#apidoc.element.redis.RedisClient.prototype.sort)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>spop ()](#apidoc.element.redis.RedisClient.prototype.spop)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>srandmember ()](#apidoc.element.redis.RedisClient.prototype.srandmember)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>srem ()](#apidoc.element.redis.RedisClient.prototype.srem)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sscan ()](#apidoc.element.redis.RedisClient.prototype.sscan)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>strlen ()](#apidoc.element.redis.RedisClient.prototype.strlen)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>subscribe ()](#apidoc.element.redis.RedisClient.prototype.subscribe)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>substr ()](#apidoc.element.redis.RedisClient.prototype.substr)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sunion ()](#apidoc.element.redis.RedisClient.prototype.sunion)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sunionstore ()](#apidoc.element.redis.RedisClient.prototype.sunionstore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>swapdb ()](#apidoc.element.redis.RedisClient.prototype.swapdb)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sync ()](#apidoc.element.redis.RedisClient.prototype.sync)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>time ()](#apidoc.element.redis.RedisClient.prototype.time)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>touch ()](#apidoc.element.redis.RedisClient.prototype.touch)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ttl ()](#apidoc.element.redis.RedisClient.prototype.ttl)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>type ()](#apidoc.element.redis.RedisClient.prototype.type)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>uncork ()](#apidoc.element.redis.RedisClient.prototype.uncork)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unlink ()](#apidoc.element.redis.RedisClient.prototype.unlink)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unref ()](#apidoc.element.redis.RedisClient.prototype.unref)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unsubscribe ()](#apidoc.element.redis.RedisClient.prototype.unsubscribe)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unwatch ()](#apidoc.element.redis.RedisClient.prototype.unwatch)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>wait ()](#apidoc.element.redis.RedisClient.prototype.wait)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>warn (msg)](#apidoc.element.redis.RedisClient.prototype.warn)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>watch ()](#apidoc.element.redis.RedisClient.prototype.watch)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>write (data)](#apidoc.element.redis.RedisClient.prototype.write)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>write_buffers ()](#apidoc.element.redis.RedisClient.prototype.write_buffers)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>write_strings ()](#apidoc.element.redis.RedisClient.prototype.write_strings)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zadd ()](#apidoc.element.redis.RedisClient.prototype.zadd)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zcard ()](#apidoc.element.redis.RedisClient.prototype.zcard)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zcount ()](#apidoc.element.redis.RedisClient.prototype.zcount)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zincrby ()](#apidoc.element.redis.RedisClient.prototype.zincrby)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zinterstore ()](#apidoc.element.redis.RedisClient.prototype.zinterstore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zlexcount ()](#apidoc.element.redis.RedisClient.prototype.zlexcount)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrange ()](#apidoc.element.redis.RedisClient.prototype.zrange)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrangebylex ()](#apidoc.element.redis.RedisClient.prototype.zrangebylex)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrangebyscore ()](#apidoc.element.redis.RedisClient.prototype.zrangebyscore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrank ()](#apidoc.element.redis.RedisClient.prototype.zrank)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrem ()](#apidoc.element.redis.RedisClient.prototype.zrem)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zremrangebylex ()](#apidoc.element.redis.RedisClient.prototype.zremrangebylex)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zremrangebyrank ()](#apidoc.element.redis.RedisClient.prototype.zremrangebyrank)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zremrangebyscore ()](#apidoc.element.redis.RedisClient.prototype.zremrangebyscore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrange ()](#apidoc.element.redis.RedisClient.prototype.zrevrange)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrangebylex ()](#apidoc.element.redis.RedisClient.prototype.zrevrangebylex)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrangebyscore ()](#apidoc.element.redis.RedisClient.prototype.zrevrangebyscore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrank ()](#apidoc.element.redis.RedisClient.prototype.zrevrank)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zscan ()](#apidoc.element.redis.RedisClient.prototype.zscan)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zscore ()](#apidoc.element.redis.RedisClient.prototype.zscore)
1.  [function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zunionstore ()](#apidoc.element.redis.RedisClient.prototype.zunionstore)

#### [module redis.RedisError](#apidoc.module.redis.RedisError)
1.  [function <span class="apidocSignatureSpan">redis.</span>RedisError (message, stack)](#apidoc.element.redis.RedisError.RedisError)
1.  [function <span class="apidocSignatureSpan">redis.RedisError.</span>super_ ()](#apidoc.element.redis.RedisError.super_)

#### [module redis.RedisError.super_](#apidoc.module.redis.RedisError.super_)
1.  [function <span class="apidocSignatureSpan">redis.RedisError.</span>super_ ()](#apidoc.element.redis.RedisError.super_.super_)
1.  [function <span class="apidocSignatureSpan">redis.RedisError.super_.</span>captureStackTrace ()](#apidoc.element.redis.RedisError.super_.captureStackTrace)
1.  number <span class="apidocSignatureSpan">redis.RedisError.super_.</span>stackTraceLimit

#### [module redis.ReplyError](#apidoc.module.redis.ReplyError)
1.  [function <span class="apidocSignatureSpan">redis.</span>ReplyError (message)](#apidoc.element.redis.ReplyError.ReplyError)
1.  [function <span class="apidocSignatureSpan">redis.ReplyError.</span>super_ (message, stack)](#apidoc.element.redis.ReplyError.super_)

#### [module redis.customErrors](#apidoc.module.redis.customErrors)
1.  [function <span class="apidocSignatureSpan">redis.customErrors.</span>AbortError (obj, stack)](#apidoc.element.redis.customErrors.AbortError)
1.  [function <span class="apidocSignatureSpan">redis.customErrors.</span>AggregateError (obj)](#apidoc.element.redis.customErrors.AggregateError)

#### [module redis.utils](#apidoc.module.redis.utils)
1.  [function <span class="apidocSignatureSpan">redis.utils.</span>callback_or_emit (self, callback, err, res)](#apidoc.element.redis.utils.callback_or_emit)
1.  [function <span class="apidocSignatureSpan">redis.utils.</span>clone (obj)](#apidoc.element.redis.utils.clone)
1.  [function <span class="apidocSignatureSpan">redis.utils.</span>print (err, reply)](#apidoc.element.redis.utils.print)
1.  [function <span class="apidocSignatureSpan">redis.utils.</span>reply_in_order (self, callback, err, res, queue)](#apidoc.element.redis.utils.reply_in_order)
1.  [function <span class="apidocSignatureSpan">redis.utils.</span>reply_to_object (reply)](#apidoc.element.redis.utils.reply_to_object)
1.  [function <span class="apidocSignatureSpan">redis.utils.</span>reply_to_strings (reply)](#apidoc.element.redis.utils.reply_to_strings)
1.  object <span class="apidocSignatureSpan">redis.utils.</span>err_code
1.  object <span class="apidocSignatureSpan">redis.utils.</span>monitor_regex



# <a name="apidoc.module.redis"></a>[module redis](#apidoc.module.redis)

#### <a name="apidoc.element.redis.AbortError"></a>[function <span class="apidocSignatureSpan">redis.</span>AbortError (obj, stack)](#apidoc.element.redis.AbortError)
- description and source-code
```javascript
function AbortError(obj, stack) {
    assert(obj, 'The options argument is required');
    assert.strictEqual(typeof obj, 'object', 'The options argument has to be of type object');

    RedisError.call(this, obj.message, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    if (stack || stack === undefined) {
        Error.captureStackTrace(this, AbortError);
    }
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}
```
- example usage
```shell
...
if (queue_names[i] === 'command_queue') {
    error_attributes.message += ' It might have been processed.';
} else { // As the command_queue is flushed first, remove this for the offline queue
    error_attributes.message = error_attributes.message.replace(' It might have been processed.', '');
}
// Don't flush everything from the queue
for (var command_obj = this[queue_names[i]].shift(); command_obj; command_obj = this[queue_names[i]].shift()) {
    var err = new errorClasses.AbortError(error_attributes);
    if (command_obj.error) {
        err.stack = err.stack + command_obj.error.stack.replace(/^Error.*?\n/, '\n');
    }
    err.command = command_obj.command.toUpperCase();
    if (command_obj.args && command_obj.args.length) {
        err.args = command_obj.args;
    }
...
```

#### <a name="apidoc.element.redis.AggregateError"></a>[function <span class="apidocSignatureSpan">redis.</span>AggregateError (obj)](#apidoc.element.redis.AggregateError)
- description and source-code
```javascript
function AggregateError(obj) {
    assert(obj, 'The options argument is required');
    assert.strictEqual(typeof obj, 'object', 'The options argument has to be of type object');

    AbortError.call(this, obj, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    Error.captureStackTrace(this, AggregateError);
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}
```
- example usage
```shell
...
    // Currently this would be a breaking change, therefore it's only emitted in debug_mode
    if (exports.debug_mode && aggregated_errors.length) {
        var error;
        if (aggregated_errors.length === 1) {
            error = aggregated_errors[0];
        } else {
            error_attributes.message = error_attributes.message.replace('It', 'They').replace(/command/i, '$&s');
            error = new errorClasses.AggregateError(error_attributes);
            error.errors = aggregated_errors;
        }
        this.emit('error', error);
    }
};

RedisClient.prototype.on_error = function (err) {
...
```

#### <a name="apidoc.element.redis.Multi"></a>[function <span class="apidocSignatureSpan">redis.</span>Multi (client, args)](#apidoc.element.redis.Multi)
- description and source-code
```javascript
function Multi(client, args) {
    this._client = client;
    this.queue = new Queue();
    var command, tmp_args;
    if (args) { // Either undefined or an array. Fail hard if it's not an array
        for (var i = 0; i < args.length; i++) {
            command = args[i][0];
            tmp_args = args[i].slice(1);
            if (Array.isArray(command)) {
                this[command[0]].apply(this, command.slice(1).concat(tmp_args));
            } else {
                this[command].apply(this, tmp_args);
            }
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.ParserError"></a>[function <span class="apidocSignatureSpan">redis.</span>ParserError (message, stack)](#apidoc.element.redis.ParserError)
- description and source-code
```javascript
function RedisError(message, stack) {
  Object.defineProperty(this, 'message', {
    value: message || '',
    configurable: true,
    writable: true
  })
  if (stack || stack === undefined) {
    Error.captureStackTrace(this, RedisError)
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient"></a>[function <span class="apidocSignatureSpan">redis.</span>RedisClient (options, stream)](#apidoc.element.redis.RedisClient)
- description and source-code
```javascript
function RedisClient(options, stream) {
    // Copy the options so they are not mutated
    options = utils.clone(options);
    EventEmitter.call(this);
    var cnx_options = {};
    var self = this;
<span class="apidocCodeCommentSpan">    /* istanbul ignore next: travis does not work with stunnel atm. Therefore the tls tests are skipped on travis */
</span>    for (var tls_option in options.tls) {
        cnx_options[tls_option] = options.tls[tls_option];
        // Copy the tls options into the general options to make sure the address is set right
        if (tls_option === 'port' || tls_option === 'host' || tls_option === 'path' || tls_option === 'family') {
            options[tls_option] = options.tls[tls_option];
        }
    }
    if (stream) {
        // The stream from the outside is used so no connection from this side is triggered but from the server this client should
 talk to
        // Reconnect etc won't work with this. This requires monkey patching to work, so it is not officially supported
        options.stream = stream;
        this.address = '"Private stream"';
    } else if (options.path) {
        cnx_options.path = options.path;
        this.address = options.path;
    } else {
        cnx_options.port = +options.port || 6379;
        cnx_options.host = options.host || '127.0.0.1';
        cnx_options.family = (!options.family && net.isIP(cnx_options.host)) || (options.family === 'IPv6' ? 6 : 4);
        this.address = cnx_options.host + ':' + cnx_options.port;
    }
    // Warn on misusing deprecated functions
    if (typeof options.retry_strategy === 'function') {
        if ('max_attempts' in options) {
            self.warn('WARNING: You activated the retry_strategy and max_attempts at the same time. This is not possible and max_attempts
 will be ignored.');
            // Do not print deprecation warnings twice
            delete options.max_attempts;
        }
        if ('retry_max_delay' in options) {
            self.warn('WARNING: You activated the retry_strategy and retry_max_delay at the same time. This is not possible and
retry_max_delay will be ignored.');
            // Do not print deprecation warnings twice
            delete options.retry_max_delay;
        }
    }

    this.connection_options = cnx_options;
    this.connection_id = RedisClient.connection_id++;
    this.connected = false;
    this.ready = false;
    if (options.socket_nodelay === undefined) {
        options.socket_nodelay = true;
    } else if (!options.socket_nodelay) { // Only warn users with this set to false
        self.warn(
            'socket_nodelay is deprecated and will be removed in v.3.0.0.\n' +
            'Setting socket_nodelay to false likely results in a reduced throughput. Please use .batch for pipelining instead.\n
' +
            'If you are sure you rely on the NAGLE-algorithm you can activate it by calling client.stream.setNoDelay(false) instead
.'
        );
    }
    if (options.socket_keepalive === undefined) {
        options.socket_keepalive = true;
    }
    for (var command in options.rename_commands) {
        options.rename_commands[command.toLowerCase()] = options.rename_commands[command];
    }
    options.return_buffers = !!options.return_buffers;
    options.detect_buffers = !!options.detect_buffers;
    // Override the detect_buffers setting if return_buffers is active and print a warning
    if (options.return_buffers && options.detect_buffers) {
        self.warn('WARNING: You activated return_buffers and detect_buffers at the same time. The return value is always going to
 be a buffer.');
        options.detect_buffers = false;
    }
    if (options.detect_buffers) {
        // We only need to look at the arguments if we do not know what we have to return
        this.handle_reply = handle_detect_buffers_reply;
    }
    this.should_buffer = false;
    this.max_attempts = options.max_attempts | 0;
    if ('max_attempts' in options) {
        self.warn(
            'max_attempts is deprecated and will be removed in v.3.0.0.\n' +
            'To reduce the amount of options and the improve the reconnection handling ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisError"></a>[function <span class="apidocSignatureSpan">redis.</span>RedisError (message, stack)](#apidoc.element.redis.RedisError)
- description and source-code
```javascript
function RedisError(message, stack) {
  Object.defineProperty(this, 'message', {
    value: message || '',
    configurable: true,
    writable: true
  })
  if (stack || stack === undefined) {
    Error.captureStackTrace(this, RedisError)
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisError.super_"></a>[function <span class="apidocSignatureSpan">redis.</span>RedisError.super_ ()](#apidoc.element.redis.RedisError.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.ReplyError"></a>[function <span class="apidocSignatureSpan">redis.</span>ReplyError (message)](#apidoc.element.redis.ReplyError)
- description and source-code
```javascript
function ReplyError(message) {
  var tmp = Error.stackTraceLimit
  Error.stackTraceLimit = 2
  RedisError.call(this, message, ADD_STACKTRACE)
  Error.captureStackTrace(this, ReplyError)
  Error.stackTraceLimit = tmp
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.createClient"></a>[function <span class="apidocSignatureSpan">redis.</span>createClient ()](#apidoc.element.redis.createClient)
- description and source-code
```javascript
createClient = function () {
    return new RedisClient(unifyOptions.apply(null, arguments));
}
```
- example usage
```shell
...

    npm install redis

## Usage Example

'''js
var redis = require("redis"),
    client = redis.createClient();

// if you'd like to select database 3, instead of 0 (default), call
// client.select(3, function() { /* ... */ });

client.on("error", function (err) {
    console.log("Error " + err);
});
...
```

#### <a name="apidoc.element.redis.print"></a>[function <span class="apidocSignatureSpan">redis.</span>print (err, reply)](#apidoc.element.redis.print)
- description and source-code
```javascript
function print(err, reply) {
    if (err) {
        // A error always begins with Error:
        console.log(err.toString());
    } else {
        console.log('Reply: ' + reply);
    }
}
```
- example usage
```shell
...
The 'versions' key contains an array of the elements of the version string for easy comparison.

> client.server_info.redis_version
'2.3.0'
> client.server_info.versions
[ 2, 3, 0 ]

## redis.print()

A handy callback function for displaying return values when testing. Example:

'''js
var redis = require("redis"),
client = redis.createClient();
...
```



# <a name="apidoc.module.redis.AbortError"></a>[module redis.AbortError](#apidoc.module.redis.AbortError)

#### <a name="apidoc.element.redis.AbortError.AbortError"></a>[function <span class="apidocSignatureSpan">redis.</span>AbortError (obj, stack)](#apidoc.element.redis.AbortError.AbortError)
- description and source-code
```javascript
function AbortError(obj, stack) {
    assert(obj, 'The options argument is required');
    assert.strictEqual(typeof obj, 'object', 'The options argument has to be of type object');

    RedisError.call(this, obj.message, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    if (stack || stack === undefined) {
        Error.captureStackTrace(this, AbortError);
    }
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}
```
- example usage
```shell
...
if (queue_names[i] === 'command_queue') {
    error_attributes.message += ' It might have been processed.';
} else { // As the command_queue is flushed first, remove this for the offline queue
    error_attributes.message = error_attributes.message.replace(' It might have been processed.', '');
}
// Don't flush everything from the queue
for (var command_obj = this[queue_names[i]].shift(); command_obj; command_obj = this[queue_names[i]].shift()) {
    var err = new errorClasses.AbortError(error_attributes);
    if (command_obj.error) {
        err.stack = err.stack + command_obj.error.stack.replace(/^Error.*?\n/, '\n');
    }
    err.command = command_obj.command.toUpperCase();
    if (command_obj.args && command_obj.args.length) {
        err.args = command_obj.args;
    }
...
```

#### <a name="apidoc.element.redis.AbortError.super_"></a>[function <span class="apidocSignatureSpan">redis.AbortError.</span>super_ (message, stack)](#apidoc.element.redis.AbortError.super_)
- description and source-code
```javascript
function RedisError(message, stack) {
  Object.defineProperty(this, 'message', {
    value: message || '',
    configurable: true,
    writable: true
  })
  if (stack || stack === undefined) {
    Error.captureStackTrace(this, RedisError)
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.AggregateError"></a>[module redis.AggregateError](#apidoc.module.redis.AggregateError)

#### <a name="apidoc.element.redis.AggregateError.AggregateError"></a>[function <span class="apidocSignatureSpan">redis.</span>AggregateError (obj)](#apidoc.element.redis.AggregateError.AggregateError)
- description and source-code
```javascript
function AggregateError(obj) {
    assert(obj, 'The options argument is required');
    assert.strictEqual(typeof obj, 'object', 'The options argument has to be of type object');

    AbortError.call(this, obj, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    Error.captureStackTrace(this, AggregateError);
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}
```
- example usage
```shell
...
    // Currently this would be a breaking change, therefore it's only emitted in debug_mode
    if (exports.debug_mode && aggregated_errors.length) {
        var error;
        if (aggregated_errors.length === 1) {
            error = aggregated_errors[0];
        } else {
            error_attributes.message = error_attributes.message.replace('It', 'They').replace(/command/i, '$&s');
            error = new errorClasses.AggregateError(error_attributes);
            error.errors = aggregated_errors;
        }
        this.emit('error', error);
    }
};

RedisClient.prototype.on_error = function (err) {
...
```

#### <a name="apidoc.element.redis.AggregateError.super_"></a>[function <span class="apidocSignatureSpan">redis.AggregateError.</span>super_ (obj, stack)](#apidoc.element.redis.AggregateError.super_)
- description and source-code
```javascript
function AbortError(obj, stack) {
    assert(obj, 'The options argument is required');
    assert.strictEqual(typeof obj, 'object', 'The options argument has to be of type object');

    RedisError.call(this, obj.message, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    if (stack || stack === undefined) {
        Error.captureStackTrace(this, AbortError);
    }
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.Multi"></a>[module redis.Multi](#apidoc.module.redis.Multi)

#### <a name="apidoc.element.redis.Multi.Multi"></a>[function <span class="apidocSignatureSpan">redis.</span>Multi (client, args)](#apidoc.element.redis.Multi.Multi)
- description and source-code
```javascript
function Multi(client, args) {
    this._client = client;
    this.queue = new Queue();
    var command, tmp_args;
    if (args) { // Either undefined or an array. Fail hard if it's not an array
        for (var i = 0; i < args.length; i++) {
            command = args[i][0];
            tmp_args = args[i].slice(1);
            if (Array.isArray(command)) {
                this[command[0]].apply(this, command.slice(1).concat(tmp_args));
            } else {
                this[command].apply(this, tmp_args);
            }
        }
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.Multi.prototype"></a>[module redis.Multi.prototype](#apidoc.module.redis.Multi.prototype)

#### <a name="apidoc.element.redis.Multi.prototype.APPEND"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>APPEND ()](#apidoc.element.redis.Multi.prototype.APPEND)
- description and source-code
```javascript
APPEND = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ASKING"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ASKING ()](#apidoc.element.redis.Multi.prototype.ASKING)
- description and source-code
```javascript
ASKING = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.AUTH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>AUTH (pass, callback)](#apidoc.element.redis.Multi.prototype.AUTH)
- description and source-code
```javascript
function auth(pass, callback) {
    debug('Sending auth to ' + this.address + ' id ' + this.connection_id);

    // Stash auth for connect and reconnect.
    this.auth_pass = pass;
    this.queue.push(new Command('auth', [pass], auth_callback(this._client, callback)));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BGREWRITEAOF"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BGREWRITEAOF ()](#apidoc.element.redis.Multi.prototype.BGREWRITEAOF)
- description and source-code
```javascript
BGREWRITEAOF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BGSAVE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BGSAVE ()](#apidoc.element.redis.Multi.prototype.BGSAVE)
- description and source-code
```javascript
BGSAVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BITCOUNT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITCOUNT ()](#apidoc.element.redis.Multi.prototype.BITCOUNT)
- description and source-code
```javascript
BITCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BITFIELD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITFIELD ()](#apidoc.element.redis.Multi.prototype.BITFIELD)
- description and source-code
```javascript
BITFIELD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BITOP"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITOP ()](#apidoc.element.redis.Multi.prototype.BITOP)
- description and source-code
```javascript
BITOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BITPOS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BITPOS ()](#apidoc.element.redis.Multi.prototype.BITPOS)
- description and source-code
```javascript
BITPOS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BLPOP"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BLPOP ()](#apidoc.element.redis.Multi.prototype.BLPOP)
- description and source-code
```javascript
BLPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BRPOP"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BRPOP ()](#apidoc.element.redis.Multi.prototype.BRPOP)
- description and source-code
```javascript
BRPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.BRPOPLPUSH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>BRPOPLPUSH ()](#apidoc.element.redis.Multi.prototype.BRPOPLPUSH)
- description and source-code
```javascript
BRPOPLPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.CLIENT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>CLIENT ()](#apidoc.element.redis.Multi.prototype.CLIENT)
- description and source-code
```javascript
function client() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = undefined;
    // CLIENT REPLY ON|OFF|SKIP
<span class="apidocCodeCommentSpan">    /* istanbul ignore next: TODO: Remove this as soon as Travis runs Redis 3.2 */
</span>    if (arr.length === 2 && arr[0].toString().toUpperCase() === 'REPLY') {
        var reply_on_off = arr[1].toString().toUpperCase();
        if (reply_on_off === 'ON' || reply_on_off === 'OFF' || reply_on_off === 'SKIP') {
            call_on_write = function () {
                self.reply = reply_on_off;
            };
        }
    }
    this.queue.push(new Command('client', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.CLUSTER"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>CLUSTER ()](#apidoc.element.redis.Multi.prototype.CLUSTER)
- description and source-code
```javascript
CLUSTER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.COMMAND"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>COMMAND ()](#apidoc.element.redis.Multi.prototype.COMMAND)
- description and source-code
```javascript
COMMAND = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.CONFIG"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>CONFIG ()](#apidoc.element.redis.Multi.prototype.CONFIG)
- description and source-code
```javascript
CONFIG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.DBSIZE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DBSIZE ()](#apidoc.element.redis.Multi.prototype.DBSIZE)
- description and source-code
```javascript
DBSIZE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.DEBUG"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DEBUG ()](#apidoc.element.redis.Multi.prototype.DEBUG)
- description and source-code
```javascript
DEBUG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.DECR"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DECR ()](#apidoc.element.redis.Multi.prototype.DECR)
- description and source-code
```javascript
DECR = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.DECRBY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DECRBY ()](#apidoc.element.redis.Multi.prototype.DECRBY)
- description and source-code
```javascript
DECRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.DEL"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DEL ()](#apidoc.element.redis.Multi.prototype.DEL)
- description and source-code
```javascript
DEL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.DISCARD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DISCARD ()](#apidoc.element.redis.Multi.prototype.DISCARD)
- description and source-code
```javascript
DISCARD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.DUMP"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>DUMP ()](#apidoc.element.redis.Multi.prototype.DUMP)
- description and source-code
```javascript
DUMP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ECHO"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ECHO ()](#apidoc.element.redis.Multi.prototype.ECHO)
- description and source-code
```javascript
ECHO = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.EVAL"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EVAL ()](#apidoc.element.redis.Multi.prototype.EVAL)
- description and source-code
```javascript
EVAL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.EVALSHA"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EVALSHA ()](#apidoc.element.redis.Multi.prototype.EVALSHA)
- description and source-code
```javascript
EVALSHA = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.EXEC"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXEC (callback)](#apidoc.element.redis.Multi.prototype.EXEC)
- description and source-code
```javascript
function exec_batch(callback) {
    var self = this;
    var len = self.queue.length;
    var index = 0;
    var command_obj;
    if (len === 0) {
        utils.reply_in_order(self._client, callback, null, []);
        return !self._client.should_buffer;
    }
    self._client.cork();
    if (!callback) {
        while (command_obj = self.queue.shift()) {
            self._client.internal_send_command(command_obj);
        }
        self._client.uncork();
        return !self._client.should_buffer;
    }
    var callback_without_own_cb = function (err, res) {
        if (err) {
            self.results.push(err);
            // Add the position to the error
            var i = self.results.length - 1;
            self.results[i].position = i;
        } else {
            self.results.push(res);
        }
        // Do not emit an error here. Otherwise each error would result in one emit.
        // The errors will be returned in the result anyway
    };
    var last_callback = function (cb) {
        return function (err, res) {
            cb(err, res);
            callback(null, self.results);
        };
    };
    self.results = [];
    while (command_obj = self.queue.shift()) {
        if (typeof command_obj.callback === 'function') {
            command_obj.callback = batch_callback(self, command_obj.callback, index);
        } else {
            command_obj.callback = callback_without_own_cb;
        }
        if (typeof callback === 'function' && index === len - 1) {
            command_obj.callback = last_callback(command_obj.callback);
        }
        this._client.internal_send_command(command_obj);
        index++;
    }
    self._client.uncork();
    return !self._client.should_buffer;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.EXEC_ATOMIC"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXEC_ATOMIC (callback)](#apidoc.element.redis.Multi.prototype.EXEC_ATOMIC)
- description and source-code
```javascript
function exec_atomic(callback) {
    if (this.queue.length < 2) {
        return this.exec_batch(callback);
    }
    return this.exec(callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.EXISTS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXISTS ()](#apidoc.element.redis.Multi.prototype.EXISTS)
- description and source-code
```javascript
EXISTS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.EXPIRE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXPIRE ()](#apidoc.element.redis.Multi.prototype.EXPIRE)
- description and source-code
```javascript
EXPIRE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.EXPIREAT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>EXPIREAT ()](#apidoc.element.redis.Multi.prototype.EXPIREAT)
- description and source-code
```javascript
EXPIREAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.FLUSHALL"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>FLUSHALL ()](#apidoc.element.redis.Multi.prototype.FLUSHALL)
- description and source-code
```javascript
FLUSHALL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.FLUSHDB"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>FLUSHDB ()](#apidoc.element.redis.Multi.prototype.FLUSHDB)
- description and source-code
```javascript
FLUSHDB = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GEOADD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEOADD ()](#apidoc.element.redis.Multi.prototype.GEOADD)
- description and source-code
```javascript
GEOADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GEODIST"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEODIST ()](#apidoc.element.redis.Multi.prototype.GEODIST)
- description and source-code
```javascript
GEODIST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GEOHASH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEOHASH ()](#apidoc.element.redis.Multi.prototype.GEOHASH)
- description and source-code
```javascript
GEOHASH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GEOPOS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEOPOS ()](#apidoc.element.redis.Multi.prototype.GEOPOS)
- description and source-code
```javascript
GEOPOS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GEORADIUS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEORADIUS ()](#apidoc.element.redis.Multi.prototype.GEORADIUS)
- description and source-code
```javascript
GEORADIUS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GEORADIUSBYMEMBER"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GEORADIUSBYMEMBER ()](#apidoc.element.redis.Multi.prototype.GEORADIUSBYMEMBER)
- description and source-code
```javascript
GEORADIUSBYMEMBER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GET ()](#apidoc.element.redis.Multi.prototype.GET)
- description and source-code
```javascript
GET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GETBIT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GETBIT ()](#apidoc.element.redis.Multi.prototype.GETBIT)
- description and source-code
```javascript
GETBIT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GETRANGE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GETRANGE ()](#apidoc.element.redis.Multi.prototype.GETRANGE)
- description and source-code
```javascript
GETRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.GETSET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>GETSET ()](#apidoc.element.redis.Multi.prototype.GETSET)
- description and source-code
```javascript
GETSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HDEL"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HDEL ()](#apidoc.element.redis.Multi.prototype.HDEL)
- description and source-code
```javascript
HDEL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HEXISTS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HEXISTS ()](#apidoc.element.redis.Multi.prototype.HEXISTS)
- description and source-code
```javascript
HEXISTS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HGET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HGET ()](#apidoc.element.redis.Multi.prototype.HGET)
- description and source-code
```javascript
HGET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HGETALL"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HGETALL ()](#apidoc.element.redis.Multi.prototype.HGETALL)
- description and source-code
```javascript
HGETALL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HINCRBY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HINCRBY ()](#apidoc.element.redis.Multi.prototype.HINCRBY)
- description and source-code
```javascript
HINCRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HINCRBYFLOAT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HINCRBYFLOAT ()](#apidoc.element.redis.Multi.prototype.HINCRBYFLOAT)
- description and source-code
```javascript
HINCRBYFLOAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HKEYS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HKEYS ()](#apidoc.element.redis.Multi.prototype.HKEYS)
- description and source-code
```javascript
HKEYS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HLEN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HLEN ()](#apidoc.element.redis.Multi.prototype.HLEN)
- description and source-code
```javascript
HLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HMGET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HMGET ()](#apidoc.element.redis.Multi.prototype.HMGET)
- description and source-code
```javascript
HMGET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HMSET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HMSET ()](#apidoc.element.redis.Multi.prototype.HMSET)
- description and source-code
```javascript
function hmset() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else if (typeof arguments[1] === 'object' && (arguments.length === 2 || arguments.length === 3 && (typeof arguments[2] === '
function' || typeof arguments[2] === 'undefined'))) {
        arr = [arguments[0]];
        for (var field in arguments[1]) {
            arr.push(field, arguments[1][field]);
        }
        callback = arguments[2];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command('hmset', arr, callback));
    return this;
}
```
- example usage
```shell
...
'''

### client.hmset(hash, obj[, callback])

Multiple values in a hash can be set by supplying an object:

'''js
client.HMSET(key2, {
    "0123456789": "abcdefghij", // NOTE: key and value will be coerced to strings
    "some manner of key": "a type of value"
});
'''

The properties and values of this Object will be set as keys and values in the Redis hash.
...
```

#### <a name="apidoc.element.redis.Multi.prototype.HSCAN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSCAN ()](#apidoc.element.redis.Multi.prototype.HSCAN)
- description and source-code
```javascript
HSCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HSET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSET ()](#apidoc.element.redis.Multi.prototype.HSET)
- description and source-code
```javascript
HSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HSETNX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSETNX ()](#apidoc.element.redis.Multi.prototype.HSETNX)
- description and source-code
```javascript
HSETNX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HSTRLEN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HSTRLEN ()](#apidoc.element.redis.Multi.prototype.HSTRLEN)
- description and source-code
```javascript
HSTRLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.HVALS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>HVALS ()](#apidoc.element.redis.Multi.prototype.HVALS)
- description and source-code
```javascript
HVALS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.INCR"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INCR ()](#apidoc.element.redis.Multi.prototype.INCR)
- description and source-code
```javascript
INCR = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.INCRBY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INCRBY ()](#apidoc.element.redis.Multi.prototype.INCRBY)
- description and source-code
```javascript
INCRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.INCRBYFLOAT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INCRBYFLOAT ()](#apidoc.element.redis.Multi.prototype.INCRBYFLOAT)
- description and source-code
```javascript
INCRBYFLOAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.INFO"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>INFO (section, callback)](#apidoc.element.redis.Multi.prototype.INFO)
- description and source-code
```javascript
function info(section, callback) {
    var args = [];
    if (typeof section === 'function') {
        callback = section;
    } else if (section !== undefined) {
        args = Array.isArray(section) ? section : [section];
    }
    this.queue.push(new Command('info', args, info_callback(this._client, callback)));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.KEYS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>KEYS ()](#apidoc.element.redis.Multi.prototype.KEYS)
- description and source-code
```javascript
KEYS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LASTSAVE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LASTSAVE ()](#apidoc.element.redis.Multi.prototype.LASTSAVE)
- description and source-code
```javascript
LASTSAVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LATENCY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LATENCY ()](#apidoc.element.redis.Multi.prototype.LATENCY)
- description and source-code
```javascript
LATENCY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LINDEX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LINDEX ()](#apidoc.element.redis.Multi.prototype.LINDEX)
- description and source-code
```javascript
LINDEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LINSERT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LINSERT ()](#apidoc.element.redis.Multi.prototype.LINSERT)
- description and source-code
```javascript
LINSERT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LLEN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LLEN ()](#apidoc.element.redis.Multi.prototype.LLEN)
- description and source-code
```javascript
LLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LPOP"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LPOP ()](#apidoc.element.redis.Multi.prototype.LPOP)
- description and source-code
```javascript
LPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LPUSH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LPUSH ()](#apidoc.element.redis.Multi.prototype.LPUSH)
- description and source-code
```javascript
LPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LPUSHX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LPUSHX ()](#apidoc.element.redis.Multi.prototype.LPUSHX)
- description and source-code
```javascript
LPUSHX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LRANGE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LRANGE ()](#apidoc.element.redis.Multi.prototype.LRANGE)
- description and source-code
```javascript
LRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LREM"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LREM ()](#apidoc.element.redis.Multi.prototype.LREM)
- description and source-code
```javascript
LREM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LSET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LSET ()](#apidoc.element.redis.Multi.prototype.LSET)
- description and source-code
```javascript
LSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.LTRIM"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>LTRIM ()](#apidoc.element.redis.Multi.prototype.LTRIM)
- description and source-code
```javascript
LTRIM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MEMORY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MEMORY ()](#apidoc.element.redis.Multi.prototype.MEMORY)
- description and source-code
```javascript
MEMORY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MGET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MGET ()](#apidoc.element.redis.Multi.prototype.MGET)
- description and source-code
```javascript
MGET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MIGRATE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MIGRATE ()](#apidoc.element.redis.Multi.prototype.MIGRATE)
- description and source-code
```javascript
MIGRATE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MODULE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MODULE ()](#apidoc.element.redis.Multi.prototype.MODULE)
- description and source-code
```javascript
MODULE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MONITOR"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MONITOR (callback)](#apidoc.element.redis.Multi.prototype.MONITOR)
- description and source-code
```javascript
function monitor(callback) {
    // Use a individual command, as this is a special case that does not has to be checked for any other command
    if (this.exec !== this.exec_transaction) {
        var self = this;
        var call_on_write = function () {
            self._client.monitoring = true;
        };
        this.queue.push(new Command('monitor', [], callback, call_on_write));
        return this;
    }
    // Set multi monitoring to indicate the exec that it should abort
    // Remove this "hack" as soon as Redis might fix this
    this.monitoring = true;
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MOVE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MOVE ()](#apidoc.element.redis.Multi.prototype.MOVE)
- description and source-code
```javascript
MOVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MSET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MSET ()](#apidoc.element.redis.Multi.prototype.MSET)
- description and source-code
```javascript
MSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MSETNX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MSETNX ()](#apidoc.element.redis.Multi.prototype.MSETNX)
- description and source-code
```javascript
MSETNX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.MULTI"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>MULTI ()](#apidoc.element.redis.Multi.prototype.MULTI)
- description and source-code
```javascript
MULTI = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.OBJECT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>OBJECT ()](#apidoc.element.redis.Multi.prototype.OBJECT)
- description and source-code
```javascript
OBJECT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PERSIST"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PERSIST ()](#apidoc.element.redis.Multi.prototype.PERSIST)
- description and source-code
```javascript
PERSIST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PEXPIRE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PEXPIRE ()](#apidoc.element.redis.Multi.prototype.PEXPIRE)
- description and source-code
```javascript
PEXPIRE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PEXPIREAT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PEXPIREAT ()](#apidoc.element.redis.Multi.prototype.PEXPIREAT)
- description and source-code
```javascript
PEXPIREAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PFADD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFADD ()](#apidoc.element.redis.Multi.prototype.PFADD)
- description and source-code
```javascript
PFADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PFCOUNT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFCOUNT ()](#apidoc.element.redis.Multi.prototype.PFCOUNT)
- description and source-code
```javascript
PFCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PFDEBUG"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFDEBUG ()](#apidoc.element.redis.Multi.prototype.PFDEBUG)
- description and source-code
```javascript
PFDEBUG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PFMERGE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFMERGE ()](#apidoc.element.redis.Multi.prototype.PFMERGE)
- description and source-code
```javascript
PFMERGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PFSELFTEST"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PFSELFTEST ()](#apidoc.element.redis.Multi.prototype.PFSELFTEST)
- description and source-code
```javascript
PFSELFTEST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PING"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PING ()](#apidoc.element.redis.Multi.prototype.PING)
- description and source-code
```javascript
PING = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.POST"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>POST ()](#apidoc.element.redis.Multi.prototype.POST)
- description and source-code
```javascript
POST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PSETEX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PSETEX ()](#apidoc.element.redis.Multi.prototype.PSETEX)
- description and source-code
```javascript
PSETEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PSUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PSUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.PSUBSCRIBE)
- description and source-code
```javascript
function psubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('psubscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PSYNC"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PSYNC ()](#apidoc.element.redis.Multi.prototype.PSYNC)
- description and source-code
```javascript
PSYNC = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PTTL"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PTTL ()](#apidoc.element.redis.Multi.prototype.PTTL)
- description and source-code
```javascript
PTTL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PUBLISH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PUBLISH ()](#apidoc.element.redis.Multi.prototype.PUBLISH)
- description and source-code
```javascript
PUBLISH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PUBSUB"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PUBSUB ()](#apidoc.element.redis.Multi.prototype.PUBSUB)
- description and source-code
```javascript
PUBSUB = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.PUNSUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>PUNSUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.PUNSUBSCRIBE)
- description and source-code
```javascript
function punsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('punsubscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.QUIT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>QUIT (callback)](#apidoc.element.redis.Multi.prototype.QUIT)
- description and source-code
```javascript
function quit(callback) {
    var self = this._client;
    var call_on_write = function () {
        // If called in a multi context, we expect redis is available
        self.closing = true;
        self.ready = false;
    };
    this.queue.push(new Command('quit', [], quit_callback(self, callback), call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RANDOMKEY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RANDOMKEY ()](#apidoc.element.redis.Multi.prototype.RANDOMKEY)
- description and source-code
```javascript
RANDOMKEY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.READONLY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>READONLY ()](#apidoc.element.redis.Multi.prototype.READONLY)
- description and source-code
```javascript
READONLY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.READWRITE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>READWRITE ()](#apidoc.element.redis.Multi.prototype.READWRITE)
- description and source-code
```javascript
READWRITE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RENAME"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RENAME ()](#apidoc.element.redis.Multi.prototype.RENAME)
- description and source-code
```javascript
RENAME = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RENAMENX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RENAMENX ()](#apidoc.element.redis.Multi.prototype.RENAMENX)
- description and source-code
```javascript
RENAMENX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.REPLCONF"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>REPLCONF ()](#apidoc.element.redis.Multi.prototype.REPLCONF)
- description and source-code
```javascript
REPLCONF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RESTORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RESTORE ()](#apidoc.element.redis.Multi.prototype.RESTORE)
- description and source-code
```javascript
RESTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RESTORE-ASKING"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RESTORE-ASKING ()](#apidoc.element.redis.Multi.prototype.RESTORE-ASKING)
- description and source-code
```javascript
RESTORE-ASKING = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ROLE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ROLE ()](#apidoc.element.redis.Multi.prototype.ROLE)
- description and source-code
```javascript
ROLE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RPOP"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPOP ()](#apidoc.element.redis.Multi.prototype.RPOP)
- description and source-code
```javascript
RPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RPOPLPUSH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPOPLPUSH ()](#apidoc.element.redis.Multi.prototype.RPOPLPUSH)
- description and source-code
```javascript
RPOPLPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RPUSH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPUSH ()](#apidoc.element.redis.Multi.prototype.RPUSH)
- description and source-code
```javascript
RPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.RPUSHX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>RPUSHX ()](#apidoc.element.redis.Multi.prototype.RPUSHX)
- description and source-code
```javascript
RPUSHX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SADD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SADD ()](#apidoc.element.redis.Multi.prototype.SADD)
- description and source-code
```javascript
SADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SAVE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SAVE ()](#apidoc.element.redis.Multi.prototype.SAVE)
- description and source-code
```javascript
SAVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SCAN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SCAN ()](#apidoc.element.redis.Multi.prototype.SCAN)
- description and source-code
```javascript
SCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SCARD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SCARD ()](#apidoc.element.redis.Multi.prototype.SCARD)
- description and source-code
```javascript
SCARD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SCRIPT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SCRIPT ()](#apidoc.element.redis.Multi.prototype.SCRIPT)
- description and source-code
```javascript
SCRIPT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SDIFF"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SDIFF ()](#apidoc.element.redis.Multi.prototype.SDIFF)
- description and source-code
```javascript
SDIFF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SDIFFSTORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SDIFFSTORE ()](#apidoc.element.redis.Multi.prototype.SDIFFSTORE)
- description and source-code
```javascript
SDIFFSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SELECT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SELECT (db, callback)](#apidoc.element.redis.Multi.prototype.SELECT)
- description and source-code
```javascript
function select(db, callback) {
    this.queue.push(new Command('select', [db], select_callback(this._client, db, callback)));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SET"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SET ()](#apidoc.element.redis.Multi.prototype.SET)
- description and source-code
```javascript
SET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SETBIT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETBIT ()](#apidoc.element.redis.Multi.prototype.SETBIT)
- description and source-code
```javascript
SETBIT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SETEX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETEX ()](#apidoc.element.redis.Multi.prototype.SETEX)
- description and source-code
```javascript
SETEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SETNX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETNX ()](#apidoc.element.redis.Multi.prototype.SETNX)
- description and source-code
```javascript
SETNX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SETRANGE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SETRANGE ()](#apidoc.element.redis.Multi.prototype.SETRANGE)
- description and source-code
```javascript
SETRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SHUTDOWN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SHUTDOWN ()](#apidoc.element.redis.Multi.prototype.SHUTDOWN)
- description and source-code
```javascript
SHUTDOWN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SINTER"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SINTER ()](#apidoc.element.redis.Multi.prototype.SINTER)
- description and source-code
```javascript
SINTER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SINTERSTORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SINTERSTORE ()](#apidoc.element.redis.Multi.prototype.SINTERSTORE)
- description and source-code
```javascript
SINTERSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SISMEMBER"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SISMEMBER ()](#apidoc.element.redis.Multi.prototype.SISMEMBER)
- description and source-code
```javascript
SISMEMBER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SLAVEOF"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SLAVEOF ()](#apidoc.element.redis.Multi.prototype.SLAVEOF)
- description and source-code
```javascript
SLAVEOF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SLOWLOG"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SLOWLOG ()](#apidoc.element.redis.Multi.prototype.SLOWLOG)
- description and source-code
```javascript
SLOWLOG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SMEMBERS"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SMEMBERS ()](#apidoc.element.redis.Multi.prototype.SMEMBERS)
- description and source-code
```javascript
SMEMBERS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SMOVE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SMOVE ()](#apidoc.element.redis.Multi.prototype.SMOVE)
- description and source-code
```javascript
SMOVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SORT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SORT ()](#apidoc.element.redis.Multi.prototype.SORT)
- description and source-code
```javascript
SORT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SPOP"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SPOP ()](#apidoc.element.redis.Multi.prototype.SPOP)
- description and source-code
```javascript
SPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SRANDMEMBER"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SRANDMEMBER ()](#apidoc.element.redis.Multi.prototype.SRANDMEMBER)
- description and source-code
```javascript
SRANDMEMBER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SREM"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SREM ()](#apidoc.element.redis.Multi.prototype.SREM)
- description and source-code
```javascript
SREM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SSCAN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SSCAN ()](#apidoc.element.redis.Multi.prototype.SSCAN)
- description and source-code
```javascript
SSCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.STRLEN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>STRLEN ()](#apidoc.element.redis.Multi.prototype.STRLEN)
- description and source-code
```javascript
STRLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.SUBSCRIBE)
- description and source-code
```javascript
function subscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('subscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SUBSTR"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUBSTR ()](#apidoc.element.redis.Multi.prototype.SUBSTR)
- description and source-code
```javascript
SUBSTR = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SUNION"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUNION ()](#apidoc.element.redis.Multi.prototype.SUNION)
- description and source-code
```javascript
SUNION = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SUNIONSTORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SUNIONSTORE ()](#apidoc.element.redis.Multi.prototype.SUNIONSTORE)
- description and source-code
```javascript
SUNIONSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SWAPDB"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SWAPDB ()](#apidoc.element.redis.Multi.prototype.SWAPDB)
- description and source-code
```javascript
SWAPDB = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.SYNC"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>SYNC ()](#apidoc.element.redis.Multi.prototype.SYNC)
- description and source-code
```javascript
SYNC = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.TIME"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TIME ()](#apidoc.element.redis.Multi.prototype.TIME)
- description and source-code
```javascript
TIME = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.TOUCH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TOUCH ()](#apidoc.element.redis.Multi.prototype.TOUCH)
- description and source-code
```javascript
TOUCH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.TTL"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TTL ()](#apidoc.element.redis.Multi.prototype.TTL)
- description and source-code
```javascript
TTL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.TYPE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>TYPE ()](#apidoc.element.redis.Multi.prototype.TYPE)
- description and source-code
```javascript
TYPE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.UNLINK"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>UNLINK ()](#apidoc.element.redis.Multi.prototype.UNLINK)
- description and source-code
```javascript
UNLINK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.UNSUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>UNSUBSCRIBE ()](#apidoc.element.redis.Multi.prototype.UNSUBSCRIBE)
- description and source-code
```javascript
function unsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('unsubscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.UNWATCH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>UNWATCH ()](#apidoc.element.redis.Multi.prototype.UNWATCH)
- description and source-code
```javascript
UNWATCH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.WAIT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>WAIT ()](#apidoc.element.redis.Multi.prototype.WAIT)
- description and source-code
```javascript
WAIT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.WATCH"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>WATCH ()](#apidoc.element.redis.Multi.prototype.WATCH)
- description and source-code
```javascript
WATCH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZADD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZADD ()](#apidoc.element.redis.Multi.prototype.ZADD)
- description and source-code
```javascript
ZADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZCARD"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZCARD ()](#apidoc.element.redis.Multi.prototype.ZCARD)
- description and source-code
```javascript
ZCARD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZCOUNT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZCOUNT ()](#apidoc.element.redis.Multi.prototype.ZCOUNT)
- description and source-code
```javascript
ZCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZINCRBY"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZINCRBY ()](#apidoc.element.redis.Multi.prototype.ZINCRBY)
- description and source-code
```javascript
ZINCRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZINTERSTORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZINTERSTORE ()](#apidoc.element.redis.Multi.prototype.ZINTERSTORE)
- description and source-code
```javascript
ZINTERSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZLEXCOUNT"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZLEXCOUNT ()](#apidoc.element.redis.Multi.prototype.ZLEXCOUNT)
- description and source-code
```javascript
ZLEXCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZRANGE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANGE ()](#apidoc.element.redis.Multi.prototype.ZRANGE)
- description and source-code
```javascript
ZRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZRANGEBYLEX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANGEBYLEX ()](#apidoc.element.redis.Multi.prototype.ZRANGEBYLEX)
- description and source-code
```javascript
ZRANGEBYLEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZRANGEBYSCORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANGEBYSCORE ()](#apidoc.element.redis.Multi.prototype.ZRANGEBYSCORE)
- description and source-code
```javascript
ZRANGEBYSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZRANK"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZRANK ()](#apidoc.element.redis.Multi.prototype.ZRANK)
- description and source-code
```javascript
ZRANK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREM"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREM ()](#apidoc.element.redis.Multi.prototype.ZREM)
- description and source-code
```javascript
ZREM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREMRANGEBYLEX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREMRANGEBYLEX ()](#apidoc.element.redis.Multi.prototype.ZREMRANGEBYLEX)
- description and source-code
```javascript
ZREMRANGEBYLEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREMRANGEBYRANK"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREMRANGEBYRANK ()](#apidoc.element.redis.Multi.prototype.ZREMRANGEBYRANK)
- description and source-code
```javascript
ZREMRANGEBYRANK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREMRANGEBYSCORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREMRANGEBYSCORE ()](#apidoc.element.redis.Multi.prototype.ZREMRANGEBYSCORE)
- description and source-code
```javascript
ZREMRANGEBYSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREVRANGE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANGE ()](#apidoc.element.redis.Multi.prototype.ZREVRANGE)
- description and source-code
```javascript
ZREVRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREVRANGEBYLEX"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANGEBYLEX ()](#apidoc.element.redis.Multi.prototype.ZREVRANGEBYLEX)
- description and source-code
```javascript
ZREVRANGEBYLEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREVRANGEBYSCORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANGEBYSCORE ()](#apidoc.element.redis.Multi.prototype.ZREVRANGEBYSCORE)
- description and source-code
```javascript
ZREVRANGEBYSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZREVRANK"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZREVRANK ()](#apidoc.element.redis.Multi.prototype.ZREVRANK)
- description and source-code
```javascript
ZREVRANK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZSCAN"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZSCAN ()](#apidoc.element.redis.Multi.prototype.ZSCAN)
- description and source-code
```javascript
ZSCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZSCORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZSCORE ()](#apidoc.element.redis.Multi.prototype.ZSCORE)
- description and source-code
```javascript
ZSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ZUNIONSTORE"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ZUNIONSTORE ()](#apidoc.element.redis.Multi.prototype.ZUNIONSTORE)
- description and source-code
```javascript
ZUNIONSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.append"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>append ()](#apidoc.element.redis.Multi.prototype.append)
- description and source-code
```javascript
append = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.asking"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>asking ()](#apidoc.element.redis.Multi.prototype.asking)
- description and source-code
```javascript
asking = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.auth"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>auth (pass, callback)](#apidoc.element.redis.Multi.prototype.auth)
- description and source-code
```javascript
function auth(pass, callback) {
    debug('Sending auth to ' + this.address + ' id ' + this.connection_id);

    // Stash auth for connect and reconnect.
    this.auth_pass = pass;
    this.queue.push(new Command('auth', [pass], auth_callback(this._client, callback)));
    return this;
}
```
- example usage
```shell
...
        }
        // reconnect after
        return Math.min(options.attempt * 100, 3000);
    }
});
'''

## client.auth(password[, callback])

When connecting to a Redis server that requires authentication, the 'AUTH' command must be sent as the
first command after connecting. This can be tricky to coordinate with reconnections, the ready check,
etc. To make this easier, 'client.auth()' stashes 'password' and will send it after each connection,
including reconnections. 'callback' is invoked only once, after the response to the very first
'AUTH' command sent.
NOTE: Your call to 'client.auth()' should not be inside the ready handler. If
...
```

#### <a name="apidoc.element.redis.Multi.prototype.bgrewriteaof"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bgrewriteaof ()](#apidoc.element.redis.Multi.prototype.bgrewriteaof)
- description and source-code
```javascript
bgrewriteaof = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.bgsave"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bgsave ()](#apidoc.element.redis.Multi.prototype.bgsave)
- description and source-code
```javascript
bgsave = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.bitcount"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitcount ()](#apidoc.element.redis.Multi.prototype.bitcount)
- description and source-code
```javascript
bitcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.bitfield"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitfield ()](#apidoc.element.redis.Multi.prototype.bitfield)
- description and source-code
```javascript
bitfield = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.bitop"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitop ()](#apidoc.element.redis.Multi.prototype.bitop)
- description and source-code
```javascript
bitop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.bitpos"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>bitpos ()](#apidoc.element.redis.Multi.prototype.bitpos)
- description and source-code
```javascript
bitpos = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.blpop"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>blpop ()](#apidoc.element.redis.Multi.prototype.blpop)
- description and source-code
```javascript
blpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.brpop"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>brpop ()](#apidoc.element.redis.Multi.prototype.brpop)
- description and source-code
```javascript
brpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
var get = function() {
    console.log("get called");
    client.get("any_key",function() { console.log("get returned"); });
    setTimeout( get, 1000 );
};
var brpop = function() {
    console.log("brpop called");
    clientBlocking.brpop("nonexistent", 5, function() {
        console.log("brpop return");
        setTimeout( brpop, 1000 );
    });
};
get();
brpop();
...
```

#### <a name="apidoc.element.redis.Multi.prototype.brpoplpush"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>brpoplpush ()](#apidoc.element.redis.Multi.prototype.brpoplpush)
- description and source-code
```javascript
brpoplpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.client"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>client ()](#apidoc.element.redis.Multi.prototype.client)
- description and source-code
```javascript
function client() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = undefined;
    // CLIENT REPLY ON|OFF|SKIP
<span class="apidocCodeCommentSpan">    /* istanbul ignore next: TODO: Remove this as soon as Travis runs Redis 3.2 */
</span>    if (arr.length === 2 && arr[0].toString().toUpperCase() === 'REPLY') {
        var reply_on_off = arr[1].toString().toUpperCase();
        if (reply_on_off === 'ON' || reply_on_off === 'OFF' || reply_on_off === 'SKIP') {
            call_on_write = function () {
                self.reply = reply_on_off;
            };
        }
    }
    this.queue.push(new Command('client', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.cluster"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>cluster ()](#apidoc.element.redis.Multi.prototype.cluster)
- description and source-code
```javascript
cluster = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.command"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>command ()](#apidoc.element.redis.Multi.prototype.command)
- description and source-code
```javascript
command = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.config"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>config ()](#apidoc.element.redis.Multi.prototype.config)
- description and source-code
```javascript
config = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.dbsize"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>dbsize ()](#apidoc.element.redis.Multi.prototype.dbsize)
- description and source-code
```javascript
dbsize = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
    .scard("bigset")
    .smembers("bigset")
    .keys("*", function (err, replies) {
        // NOTE: code in this callback is NOT atomic
        // this only happens after the the .exec call finishes.
        client.mget(replies, redis.print);
    })
    .dbsize()
    .exec(function (err, replies) {
        console.log("MULTI got " + replies.length + " replies");
        replies.forEach(function (reply, index) {
            console.log("Reply " + index + ": " + reply.toString());
        });
    });
'''
...
```

#### <a name="apidoc.element.redis.Multi.prototype.debug"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>debug ()](#apidoc.element.redis.Multi.prototype.debug)
- description and source-code
```javascript
debug = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.decr"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>decr ()](#apidoc.element.redis.Multi.prototype.decr)
- description and source-code
```javascript
decr = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.decrby"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>decrby ()](#apidoc.element.redis.Multi.prototype.decrby)
- description and source-code
```javascript
decrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.del"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>del ()](#apidoc.element.redis.Multi.prototype.del)
- description and source-code
```javascript
del = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.discard"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>discard ()](#apidoc.element.redis.Multi.prototype.discard)
- description and source-code
```javascript
discard = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.dump"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>dump ()](#apidoc.element.redis.Multi.prototype.dump)
- description and source-code
```javascript
dump = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.echo"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>echo ()](#apidoc.element.redis.Multi.prototype.echo)
- description and source-code
```javascript
echo = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.eval"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>eval ()](#apidoc.element.redis.Multi.prototype.eval)
- description and source-code
```javascript
eval = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.evalsha"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>evalsha ()](#apidoc.element.redis.Multi.prototype.evalsha)
- description and source-code
```javascript
evalsha = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.exec"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec (callback)](#apidoc.element.redis.Multi.prototype.exec)
- description and source-code
```javascript
function exec_batch(callback) {
    var self = this;
    var len = self.queue.length;
    var index = 0;
    var command_obj;
    if (len === 0) {
        utils.reply_in_order(self._client, callback, null, []);
        return !self._client.should_buffer;
    }
    self._client.cork();
    if (!callback) {
        while (command_obj = self.queue.shift()) {
            self._client.internal_send_command(command_obj);
        }
        self._client.uncork();
        return !self._client.should_buffer;
    }
    var callback_without_own_cb = function (err, res) {
        if (err) {
            self.results.push(err);
            // Add the position to the error
            var i = self.results.length - 1;
            self.results[i].position = i;
        } else {
            self.results.push(res);
        }
        // Do not emit an error here. Otherwise each error would result in one emit.
        // The errors will be returned in the result anyway
    };
    var last_callback = function (cb) {
        return function (err, res) {
            cb(err, res);
            callback(null, self.results);
        };
    };
    self.results = [];
    while (command_obj = self.queue.shift()) {
        if (typeof command_obj.callback === 'function') {
            command_obj.callback = batch_callback(self, command_obj.callback, index);
        } else {
            command_obj.callback = callback_without_own_cb;
        }
        if (typeof callback === 'function' && index === len - 1) {
            command_obj.callback = last_callback(command_obj.callback);
        }
        this._client.internal_send_command(command_obj);
        index++;
    }
    self._client.uncork();
    return !self._client.should_buffer;
}
```
- example usage
```shell
...
    .smembers("bigset")
    .keys("*", function (err, replies) {
        // NOTE: code in this callback is NOT atomic
        // this only happens after the the .exec call finishes.
        client.mget(replies, redis.print);
    })
    .dbsize()
    .exec(function (err, replies) {
        console.log("MULTI got " + replies.length + " replies");
        replies.forEach(function (reply, index) {
            console.log("Reply " + index + ": " + reply.toString());
        });
    });
'''
...
```

#### <a name="apidoc.element.redis.Multi.prototype.execAtomic"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>execAtomic (callback)](#apidoc.element.redis.Multi.prototype.execAtomic)
- description and source-code
```javascript
function exec_atomic(callback) {
    if (this.queue.length < 2) {
        return this.exec_batch(callback);
    }
    return this.exec(callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.exec_atomic"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec_atomic (callback)](#apidoc.element.redis.Multi.prototype.exec_atomic)
- description and source-code
```javascript
function exec_atomic(callback) {
    if (this.queue.length < 2) {
        return this.exec_batch(callback);
    }
    return this.exec(callback);
}
```
- example usage
```shell
...
    ["incr", "multifoo"],
    ["incr", "multibar"]
]).exec(function (err, replies) {
    console.log(replies);
});
'''

### Multi.exec_atomic([callback])

Identical to Multi.exec but with the difference that executing a single command will not use transactions.

## client.batch([commands])

Identical to .multi without transactions. This is recommended if you want to execute many commands at once but don't have to rely
 on transactions.
...
```

#### <a name="apidoc.element.redis.Multi.prototype.exec_batch"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec_batch (callback)](#apidoc.element.redis.Multi.prototype.exec_batch)
- description and source-code
```javascript
function exec_batch(callback) {
    var self = this;
    var len = self.queue.length;
    var index = 0;
    var command_obj;
    if (len === 0) {
        utils.reply_in_order(self._client, callback, null, []);
        return !self._client.should_buffer;
    }
    self._client.cork();
    if (!callback) {
        while (command_obj = self.queue.shift()) {
            self._client.internal_send_command(command_obj);
        }
        self._client.uncork();
        return !self._client.should_buffer;
    }
    var callback_without_own_cb = function (err, res) {
        if (err) {
            self.results.push(err);
            // Add the position to the error
            var i = self.results.length - 1;
            self.results[i].position = i;
        } else {
            self.results.push(res);
        }
        // Do not emit an error here. Otherwise each error would result in one emit.
        // The errors will be returned in the result anyway
    };
    var last_callback = function (cb) {
        return function (err, res) {
            cb(err, res);
            callback(null, self.results);
        };
    };
    self.results = [];
    while (command_obj = self.queue.shift()) {
        if (typeof command_obj.callback === 'function') {
            command_obj.callback = batch_callback(self, command_obj.callback, index);
        } else {
            command_obj.callback = callback_without_own_cb;
        }
        if (typeof callback === 'function' && index === len - 1) {
            command_obj.callback = last_callback(command_obj.callback);
        }
        this._client.internal_send_command(command_obj);
        index++;
    }
    self._client.uncork();
    return !self._client.should_buffer;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.exec_transaction"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exec_transaction (callback)](#apidoc.element.redis.Multi.prototype.exec_transaction)
- description and source-code
```javascript
function exec_transaction(callback) {
    if (this.monitoring || this._client.monitoring) {
        var err = new RangeError(
            'Using transaction with a client that is in monitor mode does not work due to faulty return values of Redis.'
        );
        err.command = 'EXEC';
        err.code = 'EXECABORT';
        return utils.reply_in_order(this._client, callback, err);
    }
    var self = this;
    var len = self.queue.length;
    self.errors = [];
    self.callback = callback;
    self._client.cork();
    self.wants_buffers = new Array(len);
    pipeline_transaction_command(self, new Command('multi', []), -1);
    // Drain queue, callback will catch 'QUEUED' or error
    for (var index = 0; index < len; index++) {
        // The commands may not be shifted off, since they are needed in the result handler
        pipeline_transaction_command(self, self.queue.get(index), index);
    }

    self._client.internal_send_command(new Command('exec', [], function (err, replies) {
        multi_callback(self, err, replies);
    }));
    self._client.uncork();
    return !self._client.should_buffer;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.exists"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>exists ()](#apidoc.element.redis.Multi.prototype.exists)
- description and source-code
```javascript
exists = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.expire"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>expire ()](#apidoc.element.redis.Multi.prototype.expire)
- description and source-code
```javascript
expire = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.expireat"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>expireat ()](#apidoc.element.redis.Multi.prototype.expireat)
- description and source-code
```javascript
expireat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.flushall"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>flushall ()](#apidoc.element.redis.Multi.prototype.flushall)
- description and source-code
```javascript
flushall = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.flushdb"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>flushdb ()](#apidoc.element.redis.Multi.prototype.flushdb)
- description and source-code
```javascript
flushdb = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.geoadd"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geoadd ()](#apidoc.element.redis.Multi.prototype.geoadd)
- description and source-code
```javascript
geoadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.geodist"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geodist ()](#apidoc.element.redis.Multi.prototype.geodist)
- description and source-code
```javascript
geodist = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.geohash"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geohash ()](#apidoc.element.redis.Multi.prototype.geohash)
- description and source-code
```javascript
geohash = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.geopos"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>geopos ()](#apidoc.element.redis.Multi.prototype.geopos)
- description and source-code
```javascript
geopos = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.georadius"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>georadius ()](#apidoc.element.redis.Multi.prototype.georadius)
- description and source-code
```javascript
georadius = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.georadiusbymember"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>georadiusbymember ()](#apidoc.element.redis.Multi.prototype.georadiusbymember)
- description and source-code
```javascript
georadiusbymember = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.get"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>get ()](#apidoc.element.redis.Multi.prototype.get)
- description and source-code
```javascript
get = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
bluebird.promisifyAll(redis.Multi.prototype);
'''

It'll add a *Async* to all node_redis functions (e.g. return client.getAsync().then())

'''js
// We expect a value 'foo': 'bar' to be present
// So instead of writing client.get('foo', cb); you have to write:
return client.getAsync('foo').then(function(res) {
    console.log(res); // => 'bar'
});

// Using multi with promises looks like:

return client.multi().get('foo').execAsync().then(function(res) {
...
```

#### <a name="apidoc.element.redis.Multi.prototype.getbit"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>getbit ()](#apidoc.element.redis.Multi.prototype.getbit)
- description and source-code
```javascript
getbit = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.getrange"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>getrange ()](#apidoc.element.redis.Multi.prototype.getrange)
- description and source-code
```javascript
getrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.getset"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>getset ()](#apidoc.element.redis.Multi.prototype.getset)
- description and source-code
```javascript
getset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hdel"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hdel ()](#apidoc.element.redis.Multi.prototype.hdel)
- description and source-code
```javascript
hdel = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hexists"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hexists ()](#apidoc.element.redis.Multi.prototype.hexists)
- description and source-code
```javascript
hexists = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hget"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hget ()](#apidoc.element.redis.Multi.prototype.hget)
- description and source-code
```javascript
hget = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hgetall"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hgetall ()](#apidoc.element.redis.Multi.prototype.hgetall)
- description and source-code
```javascript
hgetall = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
'''

## Friendlier hash commands

Most Redis commands take a single String or an Array of Strings as arguments, and replies are sent back as a single String or an
 Array of Strings.
When dealing with hash values, there are a couple of useful exceptions to this.

### client.hgetall(hash, callback)

The reply from an HGETALL command will be converted into a JavaScript Object by 'node_redis'. That way you can interact
with the responses using JavaScript syntax.

Example:

'''js
...
```

#### <a name="apidoc.element.redis.Multi.prototype.hincrby"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hincrby ()](#apidoc.element.redis.Multi.prototype.hincrby)
- description and source-code
```javascript
hincrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hincrbyfloat"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hincrbyfloat ()](#apidoc.element.redis.Multi.prototype.hincrbyfloat)
- description and source-code
```javascript
hincrbyfloat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hkeys"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hkeys ()](#apidoc.element.redis.Multi.prototype.hkeys)
- description and source-code
```javascript
hkeys = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
client.on("error", function (err) {
    console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
    console.log(replies.length + " replies:");
    replies.forEach(function (reply, i) {
        console.log("    " + i + ": " + reply);
    });
    client.quit();
});
'''
...
```

#### <a name="apidoc.element.redis.Multi.prototype.hlen"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hlen ()](#apidoc.element.redis.Multi.prototype.hlen)
- description and source-code
```javascript
hlen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hmget"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hmget ()](#apidoc.element.redis.Multi.prototype.hmget)
- description and source-code
```javascript
hmget = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hmset"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hmset ()](#apidoc.element.redis.Multi.prototype.hmset)
- description and source-code
```javascript
function hmset() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else if (typeof arguments[1] === 'object' && (arguments.length === 2 || arguments.length === 3 && (typeof arguments[2] === '
function' || typeof arguments[2] === 'undefined'))) {
        arr = [arguments[0]];
        for (var field in arguments[1]) {
            arr.push(field, arguments[1][field]);
        }
        callback = arguments[2];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command('hmset', arr, callback));
    return this;
}
```
- example usage
```shell
...

Each Redis command is exposed as a function on the 'client' object.
All functions take either an 'args' Array plus optional 'callback' Function or
a variable number of individual arguments followed by an optional callback.
Examples:

'''js
client.hmset(["key", "test keys 1", "test val 1", "test keys 2", "test val 2"], function (err, res) {});
// Works the same as
client.hmset("key", ["test keys 1", "test val 1", "test keys 2", "test val 2"], function (err, res) {});
// Or
client.hmset("key", "test keys 1", "test val 1", "test keys 2", "test val 2", function (err, res) {});
'''

Note that in either form the 'callback' is optional:
...
```

#### <a name="apidoc.element.redis.Multi.prototype.hscan"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hscan ()](#apidoc.element.redis.Multi.prototype.hscan)
- description and source-code
```javascript
hscan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hset"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hset ()](#apidoc.element.redis.Multi.prototype.hset)
- description and source-code
```javascript
hset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
// client.select(3, function() { /* ... */ });

client.on("error", function (err) {
console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
console.log(replies.length + " replies:");
replies.forEach(function (reply, i) {
    console.log("    " + i + ": " + reply);
});
client.quit();
...
```

#### <a name="apidoc.element.redis.Multi.prototype.hsetnx"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hsetnx ()](#apidoc.element.redis.Multi.prototype.hsetnx)
- description and source-code
```javascript
hsetnx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hstrlen"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hstrlen ()](#apidoc.element.redis.Multi.prototype.hstrlen)
- description and source-code
```javascript
hstrlen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.hvals"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>hvals ()](#apidoc.element.redis.Multi.prototype.hvals)
- description and source-code
```javascript
hvals = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.incr"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>incr ()](#apidoc.element.redis.Multi.prototype.incr)
- description and source-code
```javascript
incr = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...

'''js
var redis  = require("redis"),
    client = redis.createClient(), multi;

// start a separate multi command queue
multi = client.multi();
multi.incr("incr thing", redis.print);
multi.incr("incr other thing", redis.print);

// runs immediately
client.mset("incr thing", 100, "incr other thing", 1, redis.print);

// drains multi queue and runs atomically
multi.exec(function (err, replies) {
...
```

#### <a name="apidoc.element.redis.Multi.prototype.incrby"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>incrby ()](#apidoc.element.redis.Multi.prototype.incrby)
- description and source-code
```javascript
incrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.incrbyfloat"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>incrbyfloat ()](#apidoc.element.redis.Multi.prototype.incrbyfloat)
- description and source-code
```javascript
incrbyfloat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.info"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>info (section, callback)](#apidoc.element.redis.Multi.prototype.info)
- description and source-code
```javascript
function info(section, callback) {
    var args = [];
    if (typeof section === 'function') {
        callback = section;
    } else if (section !== undefined) {
        args = Array.isArray(section) ? section : [section];
    }
    this.queue.push(new Command('info', args, info_callback(this._client, callback)));
    return this;
}
```
- example usage
```shell
...
};

RedisClient.prototype.ready_check = function () {
var self = this;
debug('Checking server ready state...');
// Always fire this info command as first command even if other commands are already queued up
this.ready = true;
this.info(function (err, res) {
    self.on_info_cmd(err, res);
});
this.ready = false;
};

RedisClient.prototype.send_offline_queue = function () {
for (var command_obj = this.offline_queue.shift(); command_obj; command_obj = this.offline_queue.shift()) {
...
```

#### <a name="apidoc.element.redis.Multi.prototype.keys"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>keys ()](#apidoc.element.redis.Multi.prototype.keys)
- description and source-code
```javascript
keys = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
set_size -= 1;
}

// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
.exec(function (err, replies) {
    console.log("MULTI got " + replies.length + " replies");
...
```

#### <a name="apidoc.element.redis.Multi.prototype.lastsave"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lastsave ()](#apidoc.element.redis.Multi.prototype.lastsave)
- description and source-code
```javascript
lastsave = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.latency"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>latency ()](#apidoc.element.redis.Multi.prototype.latency)
- description and source-code
```javascript
latency = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.lindex"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lindex ()](#apidoc.element.redis.Multi.prototype.lindex)
- description and source-code
```javascript
lindex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.linsert"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>linsert ()](#apidoc.element.redis.Multi.prototype.linsert)
- description and source-code
```javascript
linsert = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.llen"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>llen ()](#apidoc.element.redis.Multi.prototype.llen)
- description and source-code
```javascript
llen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.lpop"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lpop ()](#apidoc.element.redis.Multi.prototype.lpop)
- description and source-code
```javascript
lpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.lpush"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lpush ()](#apidoc.element.redis.Multi.prototype.lpush)
- description and source-code
```javascript
lpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.lpushx"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lpushx ()](#apidoc.element.redis.Multi.prototype.lpushx)
- description and source-code
```javascript
lpushx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.lrange"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lrange ()](#apidoc.element.redis.Multi.prototype.lrange)
- description and source-code
```javascript
lrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.lrem"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lrem ()](#apidoc.element.redis.Multi.prototype.lrem)
- description and source-code
```javascript
lrem = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.lset"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>lset ()](#apidoc.element.redis.Multi.prototype.lset)
- description and source-code
```javascript
lset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ltrim"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ltrim ()](#apidoc.element.redis.Multi.prototype.ltrim)
- description and source-code
```javascript
ltrim = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.memory"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>memory ()](#apidoc.element.redis.Multi.prototype.memory)
- description and source-code
```javascript
memory = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.mget"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>mget ()](#apidoc.element.redis.Multi.prototype.mget)
- description and source-code
```javascript
mget = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
.exec(function (err, replies) {
    console.log("MULTI got " + replies.length + " replies");
    replies.forEach(function (reply, index) {
        console.log("Reply " + index + ": " + reply.toString());
    });
...
```

#### <a name="apidoc.element.redis.Multi.prototype.migrate"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>migrate ()](#apidoc.element.redis.Multi.prototype.migrate)
- description and source-code
```javascript
migrate = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.module"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>module ()](#apidoc.element.redis.Multi.prototype.module)
- description and source-code
```javascript
module = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.monitor"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>monitor (callback)](#apidoc.element.redis.Multi.prototype.monitor)
- description and source-code
```javascript
function monitor(callback) {
    // Use a individual command, as this is a special case that does not has to be checked for any other command
    if (this.exec !== this.exec_transaction) {
        var self = this;
        var call_on_write = function () {
            self._client.monitoring = true;
        };
        this.queue.push(new Command('monitor', [], callback, call_on_write));
        return this;
    }
    // Set multi monitoring to indicate the exec that it should abort
    // Remove this "hack" as soon as Redis might fix this
    this.monitoring = true;
    return this;
}
```
- example usage
```shell
...
A 'monitor' event is going to be emitted for every command fired from any client connected to the server including the monitoring
 client itself.
The callback for the 'monitor' event takes a timestamp from the Redis server, an array of command arguments and the raw monitoring
 string.

Example:

'''js
var client  = require("redis").createClient();
client.monitor(function (err, res) {
    console.log("Entering monitoring mode.");
});
client.set('foo', 'bar');

client.on("monitor", function (time, args, raw_reply) {
    console.log(time + ": " + args); // 1458910076.446514:['set', 'foo', 'bar']
});
...
```

#### <a name="apidoc.element.redis.Multi.prototype.move"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>move ()](#apidoc.element.redis.Multi.prototype.move)
- description and source-code
```javascript
move = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.mset"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>mset ()](#apidoc.element.redis.Multi.prototype.mset)
- description and source-code
```javascript
mset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...

// start a separate multi command queue
multi = client.multi();
multi.incr("incr thing", redis.print);
multi.incr("incr other thing", redis.print);

// runs immediately
client.mset("incr thing", 100, "incr other thing", 1, redis.print);

// drains multi queue and runs atomically
multi.exec(function (err, replies) {
    console.log(replies); // 101, 2
});
'''
...
```

#### <a name="apidoc.element.redis.Multi.prototype.msetnx"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>msetnx ()](#apidoc.element.redis.Multi.prototype.msetnx)
- description and source-code
```javascript
msetnx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.multi"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>multi ()](#apidoc.element.redis.Multi.prototype.multi)
- description and source-code
```javascript
multi = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
// So instead of writing client.get('foo', cb); you have to write:
return client.getAsync('foo').then(function(res) {
    console.log(res); // => 'bar'
});

// Using multi with promises looks like:

return client.multi().get('foo').execAsync().then(function(res) {
    console.log(res); // => 'bar'
});
'''

### Sending Commands

Each Redis command is exposed as a function on the 'client' object.
...
```

#### <a name="apidoc.element.redis.Multi.prototype.object"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>object ()](#apidoc.element.redis.Multi.prototype.object)
- description and source-code
```javascript
object = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.persist"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>persist ()](#apidoc.element.redis.Multi.prototype.persist)
- description and source-code
```javascript
persist = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pexpire"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pexpire ()](#apidoc.element.redis.Multi.prototype.pexpire)
- description and source-code
```javascript
pexpire = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pexpireat"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pexpireat ()](#apidoc.element.redis.Multi.prototype.pexpireat)
- description and source-code
```javascript
pexpireat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pfadd"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfadd ()](#apidoc.element.redis.Multi.prototype.pfadd)
- description and source-code
```javascript
pfadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pfcount"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfcount ()](#apidoc.element.redis.Multi.prototype.pfcount)
- description and source-code
```javascript
pfcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pfdebug"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfdebug ()](#apidoc.element.redis.Multi.prototype.pfdebug)
- description and source-code
```javascript
pfdebug = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pfmerge"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfmerge ()](#apidoc.element.redis.Multi.prototype.pfmerge)
- description and source-code
```javascript
pfmerge = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pfselftest"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pfselftest ()](#apidoc.element.redis.Multi.prototype.pfselftest)
- description and source-code
```javascript
pfselftest = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ping"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ping ()](#apidoc.element.redis.Multi.prototype.ping)
- description and source-code
```javascript
ping = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.post"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>post ()](#apidoc.element.redis.Multi.prototype.post)
- description and source-code
```javascript
post = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.psetex"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>psetex ()](#apidoc.element.redis.Multi.prototype.psetex)
- description and source-code
```javascript
psetex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.psubscribe"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>psubscribe ()](#apidoc.element.redis.Multi.prototype.psubscribe)
- description and source-code
```javascript
function psubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('psubscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.psync"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>psync ()](#apidoc.element.redis.Multi.prototype.psync)
- description and source-code
```javascript
psync = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.pttl"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pttl ()](#apidoc.element.redis.Multi.prototype.pttl)
- description and source-code
```javascript
pttl = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.publish"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>publish ()](#apidoc.element.redis.Multi.prototype.publish)
- description and source-code
```javascript
publish = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...

'''js
var redis = require("redis");
var sub = redis.createClient(), pub = redis.createClient();
var msg_count = 0;

sub.on("subscribe", function (channel, count) {
pub.publish("a nice channel", "I am sending a message.");
pub.publish("a nice channel", "I am sending a second message.");
pub.publish("a nice channel", "I am sending my last message.");
});

sub.on("message", function (channel, message) {
console.log("sub channel " + channel + ": " + message);
msg_count += 1;
...
```

#### <a name="apidoc.element.redis.Multi.prototype.pubsub"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>pubsub ()](#apidoc.element.redis.Multi.prototype.pubsub)
- description and source-code
```javascript
pubsub = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.punsubscribe"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>punsubscribe ()](#apidoc.element.redis.Multi.prototype.punsubscribe)
- description and source-code
```javascript
function punsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('punsubscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.quit"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>quit (callback)](#apidoc.element.redis.Multi.prototype.quit)
- description and source-code
```javascript
function quit(callback) {
    var self = this._client;
    var call_on_write = function () {
        // If called in a multi context, we expect redis is available
        self.closing = true;
        self.ready = false;
    };
    this.queue.push(new Command('quit', [], quit_callback(self, callback), call_on_write));
    return this;
}
```
- example usage
```shell
...
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
console.log(replies.length + " replies:");
replies.forEach(function (reply, i) {
    console.log("    " + i + ": " + reply);
});
client.quit();
});
'''

This will display:

mjr:~/work/node_redis (master)$ node example.js
Reply: OK
...
```

#### <a name="apidoc.element.redis.Multi.prototype.randomkey"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>randomkey ()](#apidoc.element.redis.Multi.prototype.randomkey)
- description and source-code
```javascript
randomkey = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.readonly"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>readonly ()](#apidoc.element.redis.Multi.prototype.readonly)
- description and source-code
```javascript
readonly = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.readwrite"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>readwrite ()](#apidoc.element.redis.Multi.prototype.readwrite)
- description and source-code
```javascript
readwrite = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.rename"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rename ()](#apidoc.element.redis.Multi.prototype.rename)
- description and source-code
```javascript
rename = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.renamenx"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>renamenx ()](#apidoc.element.redis.Multi.prototype.renamenx)
- description and source-code
```javascript
renamenx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.replconf"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>replconf ()](#apidoc.element.redis.Multi.prototype.replconf)
- description and source-code
```javascript
replconf = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.restore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>restore ()](#apidoc.element.redis.Multi.prototype.restore)
- description and source-code
```javascript
restore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.restore-asking"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>restore-asking ()](#apidoc.element.redis.Multi.prototype.restore-asking)
- description and source-code
```javascript
restore-asking = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.role"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>role ()](#apidoc.element.redis.Multi.prototype.role)
- description and source-code
```javascript
role = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.rpop"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpop ()](#apidoc.element.redis.Multi.prototype.rpop)
- description and source-code
```javascript
rpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.rpoplpush"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpoplpush ()](#apidoc.element.redis.Multi.prototype.rpoplpush)
- description and source-code
```javascript
rpoplpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.rpush"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpush ()](#apidoc.element.redis.Multi.prototype.rpush)
- description and source-code
```javascript
rpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.rpushx"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>rpushx ()](#apidoc.element.redis.Multi.prototype.rpushx)
- description and source-code
```javascript
rpushx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sadd"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sadd ()](#apidoc.element.redis.Multi.prototype.sadd)
- description and source-code
```javascript
sadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
Redis. The interface in 'node_redis' is to return an individual 'Multi' object by calling 'client.multi()'.
If any command fails to queue, all commands are rolled back and none is going to be executed (For further information look at [transactions
](http://redis.io/topics/transactions)).

'''js
var redis  = require("./index"),
    client = redis.createClient(), set_size = 20;

client.sadd("bigset", "a member");
client.sadd("bigset", "another member");

while (set_size > 0) {
    client.sadd("bigset", "member " + set_size);
    set_size -= 1;
}
...
```

#### <a name="apidoc.element.redis.Multi.prototype.save"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>save ()](#apidoc.element.redis.Multi.prototype.save)
- description and source-code
```javascript
save = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.scan"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>scan ()](#apidoc.element.redis.Multi.prototype.scan)
- description and source-code
```javascript
scan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.scard"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>scard ()](#apidoc.element.redis.Multi.prototype.scard)
- description and source-code
```javascript
scard = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
while (set_size > 0) {
client.sadd("bigset", "member " + set_size);
set_size -= 1;
}

// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
...
```

#### <a name="apidoc.element.redis.Multi.prototype.script"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>script ()](#apidoc.element.redis.Multi.prototype.script)
- description and source-code
```javascript
script = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
Note that this program will not exit cleanly because the client is still connected.

## Multi-word commands

To execute redis multi-word commands like 'SCRIPT LOAD' or 'CLIENT LIST' pass
the second word as first parameter:

    client.script('load', 'return 1');
    client.multi().script('load', 'return 1').exec(...);
    client.multi([['script', 'load', 'return 1']]).exec(...);

## client.duplicate([options][, callback])

Duplicate all current options and return a new redisClient instance. All options passed to the duplicate function are going to replace
 the original option.
If you pass a callback, duplicate is going to wait until the client is ready and returns it in the callback. If an error occurs
in the meanwhile, that is going to return an error instead in the callback.
...
```

#### <a name="apidoc.element.redis.Multi.prototype.sdiff"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sdiff ()](#apidoc.element.redis.Multi.prototype.sdiff)
- description and source-code
```javascript
sdiff = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sdiffstore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sdiffstore ()](#apidoc.element.redis.Multi.prototype.sdiffstore)
- description and source-code
```javascript
sdiffstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.select"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>select (db, callback)](#apidoc.element.redis.Multi.prototype.select)
- description and source-code
```javascript
function select(db, callback) {
    this.queue.push(new Command('select', [db], select_callback(this._client, db, callback)));
    return this;
}
```
- example usage
```shell
...
## Usage Example

'''js
var redis = require("redis"),
    client = redis.createClient();

// if you'd like to select database 3, instead of 0 (default), call
// client.select(3, function() { /* ... */ });

client.on("error", function (err) {
    console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
...
```

#### <a name="apidoc.element.redis.Multi.prototype.set"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>set ()](#apidoc.element.redis.Multi.prototype.set)
- description and source-code
```javascript
set = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
// if you'd like to select database 3, instead of 0 (default), call
// client.select(3, function() { /* ... */ });

client.on("error", function (err) {
console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
console.log(replies.length + " replies:");
replies.forEach(function (reply, i) {
    console.log("    " + i + ": " + reply);
});
...
```

#### <a name="apidoc.element.redis.Multi.prototype.setbit"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setbit ()](#apidoc.element.redis.Multi.prototype.setbit)
- description and source-code
```javascript
setbit = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.setex"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setex ()](#apidoc.element.redis.Multi.prototype.setex)
- description and source-code
```javascript
setex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.setnx"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setnx ()](#apidoc.element.redis.Multi.prototype.setnx)
- description and source-code
```javascript
setnx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.setrange"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>setrange ()](#apidoc.element.redis.Multi.prototype.setrange)
- description and source-code
```javascript
setrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.shutdown"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>shutdown ()](#apidoc.element.redis.Multi.prototype.shutdown)
- description and source-code
```javascript
shutdown = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sinter"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sinter ()](#apidoc.element.redis.Multi.prototype.sinter)
- description and source-code
```javascript
sinter = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sinterstore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sinterstore ()](#apidoc.element.redis.Multi.prototype.sinterstore)
- description and source-code
```javascript
sinterstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sismember"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sismember ()](#apidoc.element.redis.Multi.prototype.sismember)
- description and source-code
```javascript
sismember = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.slaveof"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>slaveof ()](#apidoc.element.redis.Multi.prototype.slaveof)
- description and source-code
```javascript
slaveof = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.slowlog"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>slowlog ()](#apidoc.element.redis.Multi.prototype.slowlog)
- description and source-code
```javascript
slowlog = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.smembers"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>smembers ()](#apidoc.element.redis.Multi.prototype.smembers)
- description and source-code
```javascript
smembers = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
client.sadd("bigset", "member " + set_size);
set_size -= 1;
}

// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
.exec(function (err, replies) {
...
```

#### <a name="apidoc.element.redis.Multi.prototype.smove"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>smove ()](#apidoc.element.redis.Multi.prototype.smove)
- description and source-code
```javascript
smove = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sort"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sort ()](#apidoc.element.redis.Multi.prototype.sort)
- description and source-code
```javascript
sort = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.spop"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>spop ()](#apidoc.element.redis.Multi.prototype.spop)
- description and source-code
```javascript
spop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.srandmember"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>srandmember ()](#apidoc.element.redis.Multi.prototype.srandmember)
- description and source-code
```javascript
srandmember = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.srem"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>srem ()](#apidoc.element.redis.Multi.prototype.srem)
- description and source-code
```javascript
srem = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sscan"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sscan ()](#apidoc.element.redis.Multi.prototype.sscan)
- description and source-code
```javascript
sscan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.strlen"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>strlen ()](#apidoc.element.redis.Multi.prototype.strlen)
- description and source-code
```javascript
strlen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.subscribe"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>subscribe ()](#apidoc.element.redis.Multi.prototype.subscribe)
- description and source-code
```javascript
function subscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('subscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
...
    if (msg_count === 3) {
        sub.unsubscribe();
        sub.quit();
        pub.quit();
    }
});

sub.subscribe("a nice channel");
'''

When a client issues a 'SUBSCRIBE' or 'PSUBSCRIBE', that connection is put into a "subscriber" mode.
At that point, only commands that modify the subscription set are valid and quit (and depending on the redis version ping as well
). When the subscription
set is empty, the connection is put back into regular mode.

If you need to send regular commands to Redis while in subscriber mode, just open another connection with a new client (hint: use
 'client.duplicate()').
...
```

#### <a name="apidoc.element.redis.Multi.prototype.substr"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>substr ()](#apidoc.element.redis.Multi.prototype.substr)
- description and source-code
```javascript
substr = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
if (parsed.auth) {
    options.password = parsed.auth.split(':')[1];
}
if (parsed.protocol && parsed.protocol !== 'redis:') {
    console.warn('node_redis: WARNING: You passed "' + parsed.protocol.substring(0, parsed.protocol.length - 1) + '" as protocol
 instead of the "redis" protocol!');
}
if (parsed.pathname && parsed.pathname !== '/') {
    options.db = parsed.pathname.substr(1);
}
if (parsed.hostname) {
    options.host = parsed.hostname;
}
if (parsed.port) {
    options.port = parsed.port;
}
...
```

#### <a name="apidoc.element.redis.Multi.prototype.sunion"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sunion ()](#apidoc.element.redis.Multi.prototype.sunion)
- description and source-code
```javascript
sunion = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sunionstore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sunionstore ()](#apidoc.element.redis.Multi.prototype.sunionstore)
- description and source-code
```javascript
sunionstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.swapdb"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>swapdb ()](#apidoc.element.redis.Multi.prototype.swapdb)
- description and source-code
```javascript
swapdb = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.sync"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>sync ()](#apidoc.element.redis.Multi.prototype.sync)
- description and source-code
```javascript
sync = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.time"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>time ()](#apidoc.element.redis.Multi.prototype.time)
- description and source-code
```javascript
time = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.touch"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>touch ()](#apidoc.element.redis.Multi.prototype.touch)
- description and source-code
```javascript
touch = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.ttl"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>ttl ()](#apidoc.element.redis.Multi.prototype.ttl)
- description and source-code
```javascript
ttl = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.type"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>type ()](#apidoc.element.redis.Multi.prototype.type)
- description and source-code
```javascript
type = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.unlink"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>unlink ()](#apidoc.element.redis.Multi.prototype.unlink)
- description and source-code
```javascript
unlink = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.unsubscribe"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>unsubscribe ()](#apidoc.element.redis.Multi.prototype.unsubscribe)
- description and source-code
```javascript
function unsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this._client;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    this.queue.push(new Command('unsubscribe', arr, callback, call_on_write));
    return this;
}
```
- example usage
```shell
...
    pub.publish("a nice channel", "I am sending my last message.");
});

sub.on("message", function (channel, message) {
    console.log("sub channel " + channel + ": " + message);
    msg_count += 1;
    if (msg_count === 3) {
        sub.unsubscribe();
        sub.quit();
        pub.quit();
    }
});

sub.subscribe("a nice channel");
'''
...
```

#### <a name="apidoc.element.redis.Multi.prototype.unwatch"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>unwatch ()](#apidoc.element.redis.Multi.prototype.unwatch)
- description and source-code
```javascript
unwatch = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.wait"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>wait ()](#apidoc.element.redis.Multi.prototype.wait)
- description and source-code
```javascript
wait = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.watch"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>watch ()](#apidoc.element.redis.Multi.prototype.watch)
- description and source-code
```javascript
watch = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zadd"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zadd ()](#apidoc.element.redis.Multi.prototype.zadd)
- description and source-code
```javascript
zadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
### Commands with Optional and Keyword arguments

This applies to anything that uses an optional '[WITHSCORES]' or '[LIMIT offset count]' in the [redis.io/commands](http://redis.
io/commands) documentation.

Example:
'''js
var args = [ 'myzset', 1, 'one', 2, 'two', 3, 'three', 99, 'ninety-nine' ];
client.zadd(args, function (err, response) {
if (err) throw err;
console.log('added '+response+' items.');

// -Infinity and +Infinity also work
var args1 = [ 'myzset', '+inf', '-inf' ];
client.zrevrangebyscore(args1, function (err, response) {
    if (err) throw err;
...
```

#### <a name="apidoc.element.redis.Multi.prototype.zcard"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zcard ()](#apidoc.element.redis.Multi.prototype.zcard)
- description and source-code
```javascript
zcard = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zcount"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zcount ()](#apidoc.element.redis.Multi.prototype.zcount)
- description and source-code
```javascript
zcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zincrby"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zincrby ()](#apidoc.element.redis.Multi.prototype.zincrby)
- description and source-code
```javascript
zincrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zinterstore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zinterstore ()](#apidoc.element.redis.Multi.prototype.zinterstore)
- description and source-code
```javascript
zinterstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zlexcount"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zlexcount ()](#apidoc.element.redis.Multi.prototype.zlexcount)
- description and source-code
```javascript
zlexcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrange"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrange ()](#apidoc.element.redis.Multi.prototype.zrange)
- description and source-code
```javascript
zrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrangebylex"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrangebylex ()](#apidoc.element.redis.Multi.prototype.zrangebylex)
- description and source-code
```javascript
zrangebylex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrangebyscore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrangebyscore ()](#apidoc.element.redis.Multi.prototype.zrangebyscore)
- description and source-code
```javascript
zrangebyscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrank"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrank ()](#apidoc.element.redis.Multi.prototype.zrank)
- description and source-code
```javascript
zrank = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrem"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrem ()](#apidoc.element.redis.Multi.prototype.zrem)
- description and source-code
```javascript
zrem = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zremrangebylex"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zremrangebylex ()](#apidoc.element.redis.Multi.prototype.zremrangebylex)
- description and source-code
```javascript
zremrangebylex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zremrangebyrank"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zremrangebyrank ()](#apidoc.element.redis.Multi.prototype.zremrangebyrank)
- description and source-code
```javascript
zremrangebyrank = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zremrangebyscore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zremrangebyscore ()](#apidoc.element.redis.Multi.prototype.zremrangebyscore)
- description and source-code
```javascript
zremrangebyscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrevrange"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrange ()](#apidoc.element.redis.Multi.prototype.zrevrange)
- description and source-code
```javascript
zrevrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrevrangebylex"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrangebylex ()](#apidoc.element.redis.Multi.prototype.zrevrangebylex)
- description and source-code
```javascript
zrevrangebylex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zrevrangebyscore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrangebyscore ()](#apidoc.element.redis.Multi.prototype.zrevrangebyscore)
- description and source-code
```javascript
zrevrangebyscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
...
var args = [ 'myzset', 1, 'one', 2, 'two', 3, 'three', 99, 'ninety-nine' ];
client.zadd(args, function (err, response) {
if (err) throw err;
console.log('added '+response+' items.');

// -Infinity and +Infinity also work
var args1 = [ 'myzset', '+inf', '-inf' ];
client.zrevrangebyscore(args1, function (err, response) {
    if (err) throw err;
    console.log('example1', response);
    // write your code here
});

var max = 3, min = 1, offset = 1, count = 2;
var args2 = [ 'myzset', max, min, 'WITHSCORES', 'LIMIT', offset, count ];
...
```

#### <a name="apidoc.element.redis.Multi.prototype.zrevrank"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zrevrank ()](#apidoc.element.redis.Multi.prototype.zrevrank)
- description and source-code
```javascript
zrevrank = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zscan"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zscan ()](#apidoc.element.redis.Multi.prototype.zscan)
- description and source-code
```javascript
zscan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zscore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zscore ()](#apidoc.element.redis.Multi.prototype.zscore)
- description and source-code
```javascript
zscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.Multi.prototype.zunionstore"></a>[function <span class="apidocSignatureSpan">redis.Multi.prototype.</span>zunionstore ()](#apidoc.element.redis.Multi.prototype.zunionstore)
- description and source-code
```javascript
zunionstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    this.queue.push(new Command(command, arr, callback));
    return this;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.RedisClient"></a>[module redis.RedisClient](#apidoc.module.redis.RedisClient)

#### <a name="apidoc.element.redis.RedisClient.RedisClient"></a>[function <span class="apidocSignatureSpan">redis.</span>RedisClient (options, stream)](#apidoc.element.redis.RedisClient.RedisClient)
- description and source-code
```javascript
function RedisClient(options, stream) {
    // Copy the options so they are not mutated
    options = utils.clone(options);
    EventEmitter.call(this);
    var cnx_options = {};
    var self = this;
<span class="apidocCodeCommentSpan">    /* istanbul ignore next: travis does not work with stunnel atm. Therefore the tls tests are skipped on travis */
</span>    for (var tls_option in options.tls) {
        cnx_options[tls_option] = options.tls[tls_option];
        // Copy the tls options into the general options to make sure the address is set right
        if (tls_option === 'port' || tls_option === 'host' || tls_option === 'path' || tls_option === 'family') {
            options[tls_option] = options.tls[tls_option];
        }
    }
    if (stream) {
        // The stream from the outside is used so no connection from this side is triggered but from the server this client should
 talk to
        // Reconnect etc won't work with this. This requires monkey patching to work, so it is not officially supported
        options.stream = stream;
        this.address = '"Private stream"';
    } else if (options.path) {
        cnx_options.path = options.path;
        this.address = options.path;
    } else {
        cnx_options.port = +options.port || 6379;
        cnx_options.host = options.host || '127.0.0.1';
        cnx_options.family = (!options.family && net.isIP(cnx_options.host)) || (options.family === 'IPv6' ? 6 : 4);
        this.address = cnx_options.host + ':' + cnx_options.port;
    }
    // Warn on misusing deprecated functions
    if (typeof options.retry_strategy === 'function') {
        if ('max_attempts' in options) {
            self.warn('WARNING: You activated the retry_strategy and max_attempts at the same time. This is not possible and max_attempts
 will be ignored.');
            // Do not print deprecation warnings twice
            delete options.max_attempts;
        }
        if ('retry_max_delay' in options) {
            self.warn('WARNING: You activated the retry_strategy and retry_max_delay at the same time. This is not possible and
retry_max_delay will be ignored.');
            // Do not print deprecation warnings twice
            delete options.retry_max_delay;
        }
    }

    this.connection_options = cnx_options;
    this.connection_id = RedisClient.connection_id++;
    this.connected = false;
    this.ready = false;
    if (options.socket_nodelay === undefined) {
        options.socket_nodelay = true;
    } else if (!options.socket_nodelay) { // Only warn users with this set to false
        self.warn(
            'socket_nodelay is deprecated and will be removed in v.3.0.0.\n' +
            'Setting socket_nodelay to false likely results in a reduced throughput. Please use .batch for pipelining instead.\n
' +
            'If you are sure you rely on the NAGLE-algorithm you can activate it by calling client.stream.setNoDelay(false) instead
.'
        );
    }
    if (options.socket_keepalive === undefined) {
        options.socket_keepalive = true;
    }
    for (var command in options.rename_commands) {
        options.rename_commands[command.toLowerCase()] = options.rename_commands[command];
    }
    options.return_buffers = !!options.return_buffers;
    options.detect_buffers = !!options.detect_buffers;
    // Override the detect_buffers setting if return_buffers is active and print a warning
    if (options.return_buffers && options.detect_buffers) {
        self.warn('WARNING: You activated return_buffers and detect_buffers at the same time. The return value is always going to
 be a buffer.');
        options.detect_buffers = false;
    }
    if (options.detect_buffers) {
        // We only need to look at the arguments if we do not know what we have to return
        this.handle_reply = handle_detect_buffers_reply;
    }
    this.should_buffer = false;
    this.max_attempts = options.max_attempts | 0;
    if ('max_attempts' in options) {
        self.warn(
            'max_attempts is deprecated and will be removed in v.3.0.0.\n' +
            'To reduce the amount of options and the improve the reconnection handling ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.super_"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.</span>super_ ()](#apidoc.element.redis.RedisClient.super_)
- description and source-code
```javascript
function EventEmitter() {
  EventEmitter.init.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.RedisClient.prototype"></a>[module redis.RedisClient.prototype](#apidoc.module.redis.RedisClient.prototype)

#### <a name="apidoc.element.redis.RedisClient.prototype.APPEND"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>APPEND ()](#apidoc.element.redis.RedisClient.prototype.APPEND)
- description and source-code
```javascript
APPEND = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ASKING"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ASKING ()](#apidoc.element.redis.RedisClient.prototype.ASKING)
- description and source-code
```javascript
ASKING = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.AUTH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>AUTH (pass, callback)](#apidoc.element.redis.RedisClient.prototype.AUTH)
- description and source-code
```javascript
function auth(pass, callback) {
    debug('Sending auth to ' + this.address + ' id ' + this.connection_id);

    // Stash auth for connect and reconnect.
    this.auth_pass = pass;
    var ready = this.ready;
    this.ready = ready || this.offline_queue.length === 0;
    var tmp = this.internal_send_command(new Command('auth', [pass], auth_callback(this, pass, callback)));
    this.ready = ready;
    return tmp;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BATCH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BATCH (args)](#apidoc.element.redis.RedisClient.prototype.BATCH)
- description and source-code
```javascript
function batch(args) {
    return new Multi(this, args);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BGREWRITEAOF"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BGREWRITEAOF ()](#apidoc.element.redis.RedisClient.prototype.BGREWRITEAOF)
- description and source-code
```javascript
BGREWRITEAOF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BGSAVE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BGSAVE ()](#apidoc.element.redis.RedisClient.prototype.BGSAVE)
- description and source-code
```javascript
BGSAVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BITCOUNT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITCOUNT ()](#apidoc.element.redis.RedisClient.prototype.BITCOUNT)
- description and source-code
```javascript
BITCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BITFIELD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITFIELD ()](#apidoc.element.redis.RedisClient.prototype.BITFIELD)
- description and source-code
```javascript
BITFIELD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BITOP"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITOP ()](#apidoc.element.redis.RedisClient.prototype.BITOP)
- description and source-code
```javascript
BITOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BITPOS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BITPOS ()](#apidoc.element.redis.RedisClient.prototype.BITPOS)
- description and source-code
```javascript
BITPOS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BLPOP"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BLPOP ()](#apidoc.element.redis.RedisClient.prototype.BLPOP)
- description and source-code
```javascript
BLPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BRPOP"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BRPOP ()](#apidoc.element.redis.RedisClient.prototype.BRPOP)
- description and source-code
```javascript
BRPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.BRPOPLPUSH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>BRPOPLPUSH ()](#apidoc.element.redis.RedisClient.prototype.BRPOPLPUSH)
- description and source-code
```javascript
BRPOPLPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.CLIENT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>CLIENT ()](#apidoc.element.redis.RedisClient.prototype.CLIENT)
- description and source-code
```javascript
function client() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = undefined;
    // CLIENT REPLY ON|OFF|SKIP
<span class="apidocCodeCommentSpan">    /* istanbul ignore next: TODO: Remove this as soon as Travis runs Redis 3.2 */
</span>    if (arr.length === 2 && arr[0].toString().toUpperCase() === 'REPLY') {
        var reply_on_off = arr[1].toString().toUpperCase();
        if (reply_on_off === 'ON' || reply_on_off === 'OFF' || reply_on_off === 'SKIP') {
            call_on_write = function () {
                self.reply = reply_on_off;
            };
        }
    }
    return this.internal_send_command(new Command('client', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.CLUSTER"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>CLUSTER ()](#apidoc.element.redis.RedisClient.prototype.CLUSTER)
- description and source-code
```javascript
CLUSTER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.COMMAND"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>COMMAND ()](#apidoc.element.redis.RedisClient.prototype.COMMAND)
- description and source-code
```javascript
COMMAND = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.CONFIG"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>CONFIG ()](#apidoc.element.redis.RedisClient.prototype.CONFIG)
- description and source-code
```javascript
CONFIG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.DBSIZE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DBSIZE ()](#apidoc.element.redis.RedisClient.prototype.DBSIZE)
- description and source-code
```javascript
DBSIZE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.DEBUG"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DEBUG ()](#apidoc.element.redis.RedisClient.prototype.DEBUG)
- description and source-code
```javascript
DEBUG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.DECR"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DECR ()](#apidoc.element.redis.RedisClient.prototype.DECR)
- description and source-code
```javascript
DECR = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.DECRBY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DECRBY ()](#apidoc.element.redis.RedisClient.prototype.DECRBY)
- description and source-code
```javascript
DECRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.DEL"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DEL ()](#apidoc.element.redis.RedisClient.prototype.DEL)
- description and source-code
```javascript
DEL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.DISCARD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DISCARD ()](#apidoc.element.redis.RedisClient.prototype.DISCARD)
- description and source-code
```javascript
DISCARD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.DUMP"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>DUMP ()](#apidoc.element.redis.RedisClient.prototype.DUMP)
- description and source-code
```javascript
DUMP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ECHO"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ECHO ()](#apidoc.element.redis.RedisClient.prototype.ECHO)
- description and source-code
```javascript
ECHO = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.EVAL"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EVAL ()](#apidoc.element.redis.RedisClient.prototype.EVAL)
- description and source-code
```javascript
EVAL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.EVALSHA"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EVALSHA ()](#apidoc.element.redis.RedisClient.prototype.EVALSHA)
- description and source-code
```javascript
EVALSHA = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.EXEC"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXEC ()](#apidoc.element.redis.RedisClient.prototype.EXEC)
- description and source-code
```javascript
EXEC = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.EXISTS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXISTS ()](#apidoc.element.redis.RedisClient.prototype.EXISTS)
- description and source-code
```javascript
EXISTS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.EXPIRE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXPIRE ()](#apidoc.element.redis.RedisClient.prototype.EXPIRE)
- description and source-code
```javascript
EXPIRE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.EXPIREAT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>EXPIREAT ()](#apidoc.element.redis.RedisClient.prototype.EXPIREAT)
- description and source-code
```javascript
EXPIREAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.FLUSHALL"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>FLUSHALL ()](#apidoc.element.redis.RedisClient.prototype.FLUSHALL)
- description and source-code
```javascript
FLUSHALL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.FLUSHDB"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>FLUSHDB ()](#apidoc.element.redis.RedisClient.prototype.FLUSHDB)
- description and source-code
```javascript
FLUSHDB = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GEOADD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEOADD ()](#apidoc.element.redis.RedisClient.prototype.GEOADD)
- description and source-code
```javascript
GEOADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GEODIST"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEODIST ()](#apidoc.element.redis.RedisClient.prototype.GEODIST)
- description and source-code
```javascript
GEODIST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GEOHASH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEOHASH ()](#apidoc.element.redis.RedisClient.prototype.GEOHASH)
- description and source-code
```javascript
GEOHASH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GEOPOS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEOPOS ()](#apidoc.element.redis.RedisClient.prototype.GEOPOS)
- description and source-code
```javascript
GEOPOS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GEORADIUS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEORADIUS ()](#apidoc.element.redis.RedisClient.prototype.GEORADIUS)
- description and source-code
```javascript
GEORADIUS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GEORADIUSBYMEMBER"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GEORADIUSBYMEMBER ()](#apidoc.element.redis.RedisClient.prototype.GEORADIUSBYMEMBER)
- description and source-code
```javascript
GEORADIUSBYMEMBER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GET ()](#apidoc.element.redis.RedisClient.prototype.GET)
- description and source-code
```javascript
GET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GETBIT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GETBIT ()](#apidoc.element.redis.RedisClient.prototype.GETBIT)
- description and source-code
```javascript
GETBIT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GETRANGE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GETRANGE ()](#apidoc.element.redis.RedisClient.prototype.GETRANGE)
- description and source-code
```javascript
GETRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.GETSET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>GETSET ()](#apidoc.element.redis.RedisClient.prototype.GETSET)
- description and source-code
```javascript
GETSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HDEL"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HDEL ()](#apidoc.element.redis.RedisClient.prototype.HDEL)
- description and source-code
```javascript
HDEL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HEXISTS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HEXISTS ()](#apidoc.element.redis.RedisClient.prototype.HEXISTS)
- description and source-code
```javascript
HEXISTS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HGET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HGET ()](#apidoc.element.redis.RedisClient.prototype.HGET)
- description and source-code
```javascript
HGET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HGETALL"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HGETALL ()](#apidoc.element.redis.RedisClient.prototype.HGETALL)
- description and source-code
```javascript
HGETALL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HINCRBY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HINCRBY ()](#apidoc.element.redis.RedisClient.prototype.HINCRBY)
- description and source-code
```javascript
HINCRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HINCRBYFLOAT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HINCRBYFLOAT ()](#apidoc.element.redis.RedisClient.prototype.HINCRBYFLOAT)
- description and source-code
```javascript
HINCRBYFLOAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HKEYS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HKEYS ()](#apidoc.element.redis.RedisClient.prototype.HKEYS)
- description and source-code
```javascript
HKEYS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HLEN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HLEN ()](#apidoc.element.redis.RedisClient.prototype.HLEN)
- description and source-code
```javascript
HLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HMGET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HMGET ()](#apidoc.element.redis.RedisClient.prototype.HMGET)
- description and source-code
```javascript
HMGET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HMSET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HMSET ()](#apidoc.element.redis.RedisClient.prototype.HMSET)
- description and source-code
```javascript
function hmset() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else if (typeof arguments[1] === 'object' && (arguments.length === 2 || arguments.length === 3 && (typeof arguments[2] === '
function' || typeof arguments[2] === 'undefined'))) {
        arr = [arguments[0]];
        for (var field in arguments[1]) {
            arr.push(field, arguments[1][field]);
        }
        callback = arguments[2];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command('hmset', arr, callback));
}
```
- example usage
```shell
...
'''

### client.hmset(hash, obj[, callback])

Multiple values in a hash can be set by supplying an object:

'''js
client.HMSET(key2, {
    "0123456789": "abcdefghij", // NOTE: key and value will be coerced to strings
    "some manner of key": "a type of value"
});
'''

The properties and values of this Object will be set as keys and values in the Redis hash.
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HSCAN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSCAN ()](#apidoc.element.redis.RedisClient.prototype.HSCAN)
- description and source-code
```javascript
HSCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HSET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSET ()](#apidoc.element.redis.RedisClient.prototype.HSET)
- description and source-code
```javascript
HSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HSETNX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSETNX ()](#apidoc.element.redis.RedisClient.prototype.HSETNX)
- description and source-code
```javascript
HSETNX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HSTRLEN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HSTRLEN ()](#apidoc.element.redis.RedisClient.prototype.HSTRLEN)
- description and source-code
```javascript
HSTRLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.HVALS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>HVALS ()](#apidoc.element.redis.RedisClient.prototype.HVALS)
- description and source-code
```javascript
HVALS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.INCR"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INCR ()](#apidoc.element.redis.RedisClient.prototype.INCR)
- description and source-code
```javascript
INCR = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.INCRBY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INCRBY ()](#apidoc.element.redis.RedisClient.prototype.INCRBY)
- description and source-code
```javascript
INCRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.INCRBYFLOAT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INCRBYFLOAT ()](#apidoc.element.redis.RedisClient.prototype.INCRBYFLOAT)
- description and source-code
```javascript
INCRBYFLOAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.INFO"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>INFO (section, callback)](#apidoc.element.redis.RedisClient.prototype.INFO)
- description and source-code
```javascript
function info(section, callback) {
    var args = [];
    if (typeof section === 'function') {
        callback = section;
    } else if (section !== undefined) {
        args = Array.isArray(section) ? section : [section];
    }
    return this.internal_send_command(new Command('info', args, info_callback(this, callback)));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.KEYS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>KEYS ()](#apidoc.element.redis.RedisClient.prototype.KEYS)
- description and source-code
```javascript
KEYS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LASTSAVE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LASTSAVE ()](#apidoc.element.redis.RedisClient.prototype.LASTSAVE)
- description and source-code
```javascript
LASTSAVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LATENCY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LATENCY ()](#apidoc.element.redis.RedisClient.prototype.LATENCY)
- description and source-code
```javascript
LATENCY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LINDEX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LINDEX ()](#apidoc.element.redis.RedisClient.prototype.LINDEX)
- description and source-code
```javascript
LINDEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LINSERT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LINSERT ()](#apidoc.element.redis.RedisClient.prototype.LINSERT)
- description and source-code
```javascript
LINSERT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LLEN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LLEN ()](#apidoc.element.redis.RedisClient.prototype.LLEN)
- description and source-code
```javascript
LLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LPOP"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LPOP ()](#apidoc.element.redis.RedisClient.prototype.LPOP)
- description and source-code
```javascript
LPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LPUSH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LPUSH ()](#apidoc.element.redis.RedisClient.prototype.LPUSH)
- description and source-code
```javascript
LPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LPUSHX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LPUSHX ()](#apidoc.element.redis.RedisClient.prototype.LPUSHX)
- description and source-code
```javascript
LPUSHX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LRANGE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LRANGE ()](#apidoc.element.redis.RedisClient.prototype.LRANGE)
- description and source-code
```javascript
LRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LREM"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LREM ()](#apidoc.element.redis.RedisClient.prototype.LREM)
- description and source-code
```javascript
LREM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LSET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LSET ()](#apidoc.element.redis.RedisClient.prototype.LSET)
- description and source-code
```javascript
LSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.LTRIM"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>LTRIM ()](#apidoc.element.redis.RedisClient.prototype.LTRIM)
- description and source-code
```javascript
LTRIM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MEMORY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MEMORY ()](#apidoc.element.redis.RedisClient.prototype.MEMORY)
- description and source-code
```javascript
MEMORY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MGET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MGET ()](#apidoc.element.redis.RedisClient.prototype.MGET)
- description and source-code
```javascript
MGET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MIGRATE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MIGRATE ()](#apidoc.element.redis.RedisClient.prototype.MIGRATE)
- description and source-code
```javascript
MIGRATE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MODULE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MODULE ()](#apidoc.element.redis.RedisClient.prototype.MODULE)
- description and source-code
```javascript
MODULE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MONITOR"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MONITOR (callback)](#apidoc.element.redis.RedisClient.prototype.MONITOR)
- description and source-code
```javascript
function monitor(callback) {
    // Use a individual command, as this is a special case that does not has to be checked for any other command
    var self = this;
    var call_on_write = function () {
        // Activating monitor mode has to happen before Redis returned the callback. The monitor result is returned first.
        // Therefore we expect the command to be properly processed. If this is not the case, it's not an issue either.
        self.monitoring = true;
    };
    return this.internal_send_command(new Command('monitor', [], callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MOVE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MOVE ()](#apidoc.element.redis.RedisClient.prototype.MOVE)
- description and source-code
```javascript
MOVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MSET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MSET ()](#apidoc.element.redis.RedisClient.prototype.MSET)
- description and source-code
```javascript
MSET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MSETNX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MSETNX ()](#apidoc.element.redis.RedisClient.prototype.MSETNX)
- description and source-code
```javascript
MSETNX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.MULTI"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>MULTI (args)](#apidoc.element.redis.RedisClient.prototype.MULTI)
- description and source-code
```javascript
function multi(args) {
    var multi = new Multi(this, args);
    multi.exec = multi.EXEC = multi.exec_transaction;
    return multi;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.OBJECT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>OBJECT ()](#apidoc.element.redis.RedisClient.prototype.OBJECT)
- description and source-code
```javascript
OBJECT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PERSIST"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PERSIST ()](#apidoc.element.redis.RedisClient.prototype.PERSIST)
- description and source-code
```javascript
PERSIST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PEXPIRE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PEXPIRE ()](#apidoc.element.redis.RedisClient.prototype.PEXPIRE)
- description and source-code
```javascript
PEXPIRE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PEXPIREAT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PEXPIREAT ()](#apidoc.element.redis.RedisClient.prototype.PEXPIREAT)
- description and source-code
```javascript
PEXPIREAT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PFADD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFADD ()](#apidoc.element.redis.RedisClient.prototype.PFADD)
- description and source-code
```javascript
PFADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PFCOUNT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFCOUNT ()](#apidoc.element.redis.RedisClient.prototype.PFCOUNT)
- description and source-code
```javascript
PFCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PFDEBUG"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFDEBUG ()](#apidoc.element.redis.RedisClient.prototype.PFDEBUG)
- description and source-code
```javascript
PFDEBUG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PFMERGE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFMERGE ()](#apidoc.element.redis.RedisClient.prototype.PFMERGE)
- description and source-code
```javascript
PFMERGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PFSELFTEST"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PFSELFTEST ()](#apidoc.element.redis.RedisClient.prototype.PFSELFTEST)
- description and source-code
```javascript
PFSELFTEST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PING"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PING ()](#apidoc.element.redis.RedisClient.prototype.PING)
- description and source-code
```javascript
PING = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.POST"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>POST ()](#apidoc.element.redis.RedisClient.prototype.POST)
- description and source-code
```javascript
POST = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PSETEX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PSETEX ()](#apidoc.element.redis.RedisClient.prototype.PSETEX)
- description and source-code
```javascript
PSETEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PSUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PSUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.PSUBSCRIBE)
- description and source-code
```javascript
function psubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('psubscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PSYNC"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PSYNC ()](#apidoc.element.redis.RedisClient.prototype.PSYNC)
- description and source-code
```javascript
PSYNC = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PTTL"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PTTL ()](#apidoc.element.redis.RedisClient.prototype.PTTL)
- description and source-code
```javascript
PTTL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PUBLISH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PUBLISH ()](#apidoc.element.redis.RedisClient.prototype.PUBLISH)
- description and source-code
```javascript
PUBLISH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PUBSUB"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PUBSUB ()](#apidoc.element.redis.RedisClient.prototype.PUBSUB)
- description and source-code
```javascript
PUBSUB = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.PUNSUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>PUNSUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.PUNSUBSCRIBE)
- description and source-code
```javascript
function punsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('punsubscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.QUIT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>QUIT (callback)](#apidoc.element.redis.RedisClient.prototype.QUIT)
- description and source-code
```javascript
function quit(callback) {
    // TODO: Consider this for v.3
    // Allow the quit command to be fired as soon as possible to prevent it landing in the offline queue.
    // this.ready = this.offline_queue.length === 0;
    var backpressure_indicator = this.internal_send_command(new Command('quit', [], quit_callback(this, callback)));
    // Calling quit should always end the connection, no matter if there's a connection or not
    this.closing = true;
    this.ready = false;
    return backpressure_indicator;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RANDOMKEY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RANDOMKEY ()](#apidoc.element.redis.RedisClient.prototype.RANDOMKEY)
- description and source-code
```javascript
RANDOMKEY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.READONLY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>READONLY ()](#apidoc.element.redis.RedisClient.prototype.READONLY)
- description and source-code
```javascript
READONLY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.READWRITE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>READWRITE ()](#apidoc.element.redis.RedisClient.prototype.READWRITE)
- description and source-code
```javascript
READWRITE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RENAME"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RENAME ()](#apidoc.element.redis.RedisClient.prototype.RENAME)
- description and source-code
```javascript
RENAME = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RENAMENX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RENAMENX ()](#apidoc.element.redis.RedisClient.prototype.RENAMENX)
- description and source-code
```javascript
RENAMENX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.REPLCONF"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>REPLCONF ()](#apidoc.element.redis.RedisClient.prototype.REPLCONF)
- description and source-code
```javascript
REPLCONF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RESTORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RESTORE ()](#apidoc.element.redis.RedisClient.prototype.RESTORE)
- description and source-code
```javascript
RESTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RESTORE-ASKING"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RESTORE-ASKING ()](#apidoc.element.redis.RedisClient.prototype.RESTORE-ASKING)
- description and source-code
```javascript
RESTORE-ASKING = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ROLE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ROLE ()](#apidoc.element.redis.RedisClient.prototype.ROLE)
- description and source-code
```javascript
ROLE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RPOP"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPOP ()](#apidoc.element.redis.RedisClient.prototype.RPOP)
- description and source-code
```javascript
RPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RPOPLPUSH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPOPLPUSH ()](#apidoc.element.redis.RedisClient.prototype.RPOPLPUSH)
- description and source-code
```javascript
RPOPLPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RPUSH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPUSH ()](#apidoc.element.redis.RedisClient.prototype.RPUSH)
- description and source-code
```javascript
RPUSH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.RPUSHX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>RPUSHX ()](#apidoc.element.redis.RedisClient.prototype.RPUSHX)
- description and source-code
```javascript
RPUSHX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SADD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SADD ()](#apidoc.element.redis.RedisClient.prototype.SADD)
- description and source-code
```javascript
SADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SAVE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SAVE ()](#apidoc.element.redis.RedisClient.prototype.SAVE)
- description and source-code
```javascript
SAVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SCAN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SCAN ()](#apidoc.element.redis.RedisClient.prototype.SCAN)
- description and source-code
```javascript
SCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SCARD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SCARD ()](#apidoc.element.redis.RedisClient.prototype.SCARD)
- description and source-code
```javascript
SCARD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SCRIPT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SCRIPT ()](#apidoc.element.redis.RedisClient.prototype.SCRIPT)
- description and source-code
```javascript
SCRIPT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SDIFF"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SDIFF ()](#apidoc.element.redis.RedisClient.prototype.SDIFF)
- description and source-code
```javascript
SDIFF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SDIFFSTORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SDIFFSTORE ()](#apidoc.element.redis.RedisClient.prototype.SDIFFSTORE)
- description and source-code
```javascript
SDIFFSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SELECT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SELECT (db, callback)](#apidoc.element.redis.RedisClient.prototype.SELECT)
- description and source-code
```javascript
function select(db, callback) {
    return this.internal_send_command(new Command('select', [db], select_callback(this, db, callback)));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SET"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SET ()](#apidoc.element.redis.RedisClient.prototype.SET)
- description and source-code
```javascript
SET = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SETBIT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETBIT ()](#apidoc.element.redis.RedisClient.prototype.SETBIT)
- description and source-code
```javascript
SETBIT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SETEX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETEX ()](#apidoc.element.redis.RedisClient.prototype.SETEX)
- description and source-code
```javascript
SETEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SETNX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETNX ()](#apidoc.element.redis.RedisClient.prototype.SETNX)
- description and source-code
```javascript
SETNX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SETRANGE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SETRANGE ()](#apidoc.element.redis.RedisClient.prototype.SETRANGE)
- description and source-code
```javascript
SETRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SHUTDOWN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SHUTDOWN ()](#apidoc.element.redis.RedisClient.prototype.SHUTDOWN)
- description and source-code
```javascript
SHUTDOWN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SINTER"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SINTER ()](#apidoc.element.redis.RedisClient.prototype.SINTER)
- description and source-code
```javascript
SINTER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SINTERSTORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SINTERSTORE ()](#apidoc.element.redis.RedisClient.prototype.SINTERSTORE)
- description and source-code
```javascript
SINTERSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SISMEMBER"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SISMEMBER ()](#apidoc.element.redis.RedisClient.prototype.SISMEMBER)
- description and source-code
```javascript
SISMEMBER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SLAVEOF"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SLAVEOF ()](#apidoc.element.redis.RedisClient.prototype.SLAVEOF)
- description and source-code
```javascript
SLAVEOF = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SLOWLOG"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SLOWLOG ()](#apidoc.element.redis.RedisClient.prototype.SLOWLOG)
- description and source-code
```javascript
SLOWLOG = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SMEMBERS"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SMEMBERS ()](#apidoc.element.redis.RedisClient.prototype.SMEMBERS)
- description and source-code
```javascript
SMEMBERS = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SMOVE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SMOVE ()](#apidoc.element.redis.RedisClient.prototype.SMOVE)
- description and source-code
```javascript
SMOVE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SORT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SORT ()](#apidoc.element.redis.RedisClient.prototype.SORT)
- description and source-code
```javascript
SORT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SPOP"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SPOP ()](#apidoc.element.redis.RedisClient.prototype.SPOP)
- description and source-code
```javascript
SPOP = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SRANDMEMBER"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SRANDMEMBER ()](#apidoc.element.redis.RedisClient.prototype.SRANDMEMBER)
- description and source-code
```javascript
SRANDMEMBER = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SREM"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SREM ()](#apidoc.element.redis.RedisClient.prototype.SREM)
- description and source-code
```javascript
SREM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SSCAN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SSCAN ()](#apidoc.element.redis.RedisClient.prototype.SSCAN)
- description and source-code
```javascript
SSCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.STRLEN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>STRLEN ()](#apidoc.element.redis.RedisClient.prototype.STRLEN)
- description and source-code
```javascript
STRLEN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.SUBSCRIBE)
- description and source-code
```javascript
function subscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('subscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SUBSTR"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUBSTR ()](#apidoc.element.redis.RedisClient.prototype.SUBSTR)
- description and source-code
```javascript
SUBSTR = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SUNION"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUNION ()](#apidoc.element.redis.RedisClient.prototype.SUNION)
- description and source-code
```javascript
SUNION = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SUNIONSTORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SUNIONSTORE ()](#apidoc.element.redis.RedisClient.prototype.SUNIONSTORE)
- description and source-code
```javascript
SUNIONSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SWAPDB"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SWAPDB ()](#apidoc.element.redis.RedisClient.prototype.SWAPDB)
- description and source-code
```javascript
SWAPDB = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.SYNC"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>SYNC ()](#apidoc.element.redis.RedisClient.prototype.SYNC)
- description and source-code
```javascript
SYNC = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.TIME"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TIME ()](#apidoc.element.redis.RedisClient.prototype.TIME)
- description and source-code
```javascript
TIME = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.TOUCH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TOUCH ()](#apidoc.element.redis.RedisClient.prototype.TOUCH)
- description and source-code
```javascript
TOUCH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.TTL"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TTL ()](#apidoc.element.redis.RedisClient.prototype.TTL)
- description and source-code
```javascript
TTL = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.TYPE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>TYPE ()](#apidoc.element.redis.RedisClient.prototype.TYPE)
- description and source-code
```javascript
TYPE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.UNLINK"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>UNLINK ()](#apidoc.element.redis.RedisClient.prototype.UNLINK)
- description and source-code
```javascript
UNLINK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.UNSUBSCRIBE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>UNSUBSCRIBE ()](#apidoc.element.redis.RedisClient.prototype.UNSUBSCRIBE)
- description and source-code
```javascript
function unsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('unsubscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.UNWATCH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>UNWATCH ()](#apidoc.element.redis.RedisClient.prototype.UNWATCH)
- description and source-code
```javascript
UNWATCH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.WAIT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>WAIT ()](#apidoc.element.redis.RedisClient.prototype.WAIT)
- description and source-code
```javascript
WAIT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.WATCH"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>WATCH ()](#apidoc.element.redis.RedisClient.prototype.WATCH)
- description and source-code
```javascript
WATCH = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZADD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZADD ()](#apidoc.element.redis.RedisClient.prototype.ZADD)
- description and source-code
```javascript
ZADD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZCARD"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZCARD ()](#apidoc.element.redis.RedisClient.prototype.ZCARD)
- description and source-code
```javascript
ZCARD = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZCOUNT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZCOUNT ()](#apidoc.element.redis.RedisClient.prototype.ZCOUNT)
- description and source-code
```javascript
ZCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZINCRBY"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZINCRBY ()](#apidoc.element.redis.RedisClient.prototype.ZINCRBY)
- description and source-code
```javascript
ZINCRBY = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZINTERSTORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZINTERSTORE ()](#apidoc.element.redis.RedisClient.prototype.ZINTERSTORE)
- description and source-code
```javascript
ZINTERSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZLEXCOUNT"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZLEXCOUNT ()](#apidoc.element.redis.RedisClient.prototype.ZLEXCOUNT)
- description and source-code
```javascript
ZLEXCOUNT = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZRANGE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANGE ()](#apidoc.element.redis.RedisClient.prototype.ZRANGE)
- description and source-code
```javascript
ZRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZRANGEBYLEX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANGEBYLEX ()](#apidoc.element.redis.RedisClient.prototype.ZRANGEBYLEX)
- description and source-code
```javascript
ZRANGEBYLEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZRANGEBYSCORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANGEBYSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZRANGEBYSCORE)
- description and source-code
```javascript
ZRANGEBYSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZRANK"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZRANK ()](#apidoc.element.redis.RedisClient.prototype.ZRANK)
- description and source-code
```javascript
ZRANK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREM"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREM ()](#apidoc.element.redis.RedisClient.prototype.ZREM)
- description and source-code
```javascript
ZREM = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYLEX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREMRANGEBYLEX ()](#apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYLEX)
- description and source-code
```javascript
ZREMRANGEBYLEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYRANK"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREMRANGEBYRANK ()](#apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYRANK)
- description and source-code
```javascript
ZREMRANGEBYRANK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYSCORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREMRANGEBYSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZREMRANGEBYSCORE)
- description and source-code
```javascript
ZREMRANGEBYSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREVRANGE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANGE ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANGE)
- description and source-code
```javascript
ZREVRANGE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREVRANGEBYLEX"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANGEBYLEX ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANGEBYLEX)
- description and source-code
```javascript
ZREVRANGEBYLEX = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREVRANGEBYSCORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANGEBYSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANGEBYSCORE)
- description and source-code
```javascript
ZREVRANGEBYSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZREVRANK"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZREVRANK ()](#apidoc.element.redis.RedisClient.prototype.ZREVRANK)
- description and source-code
```javascript
ZREVRANK = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZSCAN"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZSCAN ()](#apidoc.element.redis.RedisClient.prototype.ZSCAN)
- description and source-code
```javascript
ZSCAN = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZSCORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZSCORE ()](#apidoc.element.redis.RedisClient.prototype.ZSCORE)
- description and source-code
```javascript
ZSCORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ZUNIONSTORE"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ZUNIONSTORE ()](#apidoc.element.redis.RedisClient.prototype.ZUNIONSTORE)
- description and source-code
```javascript
ZUNIONSTORE = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.append"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>append ()](#apidoc.element.redis.RedisClient.prototype.append)
- description and source-code
```javascript
append = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.asking"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>asking ()](#apidoc.element.redis.RedisClient.prototype.asking)
- description and source-code
```javascript
asking = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.auth"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>auth (pass, callback)](#apidoc.element.redis.RedisClient.prototype.auth)
- description and source-code
```javascript
function auth(pass, callback) {
    debug('Sending auth to ' + this.address + ' id ' + this.connection_id);

    // Stash auth for connect and reconnect.
    this.auth_pass = pass;
    var ready = this.ready;
    this.ready = ready || this.offline_queue.length === 0;
    var tmp = this.internal_send_command(new Command('auth', [pass], auth_callback(this, pass, callback)));
    this.ready = ready;
    return tmp;
}
```
- example usage
```shell
...
        }
        // reconnect after
        return Math.min(options.attempt * 100, 3000);
    }
});
'''

## client.auth(password[, callback])

When connecting to a Redis server that requires authentication, the 'AUTH' command must be sent as the
first command after connecting. This can be tricky to coordinate with reconnections, the ready check,
etc. To make this easier, 'client.auth()' stashes 'password' and will send it after each connection,
including reconnections. 'callback' is invoked only once, after the response to the very first
'AUTH' command sent.
NOTE: Your call to 'client.auth()' should not be inside the ready handler. If
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.batch"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>batch (args)](#apidoc.element.redis.RedisClient.prototype.batch)
- description and source-code
```javascript
function batch(args) {
    return new Multi(this, args);
}
```
- example usage
```shell
...
});
'''

### Multi.exec_atomic([callback])

Identical to Multi.exec but with the difference that executing a single command will not use transactions.

## client.batch([commands])

Identical to .multi without transactions. This is recommended if you want to execute many commands at once but don't have to rely
 on transactions.

'BATCH' commands are queued up until an 'EXEC' is issued, and then all commands are run atomically by
Redis. The interface in 'node_redis' is to return an individual 'Batch' object by calling 'client.batch()'.
The only difference between .batch and .multi is that no transaction is going to be used.
Be aware that the errors are - just like in multi statements - in the result. Otherwise both, errors and results could be returned
 at the same time.
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.bgrewriteaof"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bgrewriteaof ()](#apidoc.element.redis.RedisClient.prototype.bgrewriteaof)
- description and source-code
```javascript
bgrewriteaof = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.bgsave"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bgsave ()](#apidoc.element.redis.RedisClient.prototype.bgsave)
- description and source-code
```javascript
bgsave = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.bitcount"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitcount ()](#apidoc.element.redis.RedisClient.prototype.bitcount)
- description and source-code
```javascript
bitcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.bitfield"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitfield ()](#apidoc.element.redis.RedisClient.prototype.bitfield)
- description and source-code
```javascript
bitfield = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.bitop"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitop ()](#apidoc.element.redis.RedisClient.prototype.bitop)
- description and source-code
```javascript
bitop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.bitpos"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>bitpos ()](#apidoc.element.redis.RedisClient.prototype.bitpos)
- description and source-code
```javascript
bitpos = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.blpop"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>blpop ()](#apidoc.element.redis.RedisClient.prototype.blpop)
- description and source-code
```javascript
blpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.brpop"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>brpop ()](#apidoc.element.redis.RedisClient.prototype.brpop)
- description and source-code
```javascript
brpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
var get = function() {
    console.log("get called");
    client.get("any_key",function() { console.log("get returned"); });
    setTimeout( get, 1000 );
};
var brpop = function() {
    console.log("brpop called");
    clientBlocking.brpop("nonexistent", 5, function() {
        console.log("brpop return");
        setTimeout( brpop, 1000 );
    });
};
get();
brpop();
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.brpoplpush"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>brpoplpush ()](#apidoc.element.redis.RedisClient.prototype.brpoplpush)
- description and source-code
```javascript
brpoplpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.client"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>client ()](#apidoc.element.redis.RedisClient.prototype.client)
- description and source-code
```javascript
function client() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = undefined;
    // CLIENT REPLY ON|OFF|SKIP
<span class="apidocCodeCommentSpan">    /* istanbul ignore next: TODO: Remove this as soon as Travis runs Redis 3.2 */
</span>    if (arr.length === 2 && arr[0].toString().toUpperCase() === 'REPLY') {
        var reply_on_off = arr[1].toString().toUpperCase();
        if (reply_on_off === 'ON' || reply_on_off === 'OFF' || reply_on_off === 'SKIP') {
            call_on_write = function () {
                self.reply = reply_on_off;
            };
        }
    }
    return this.internal_send_command(new Command('client', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.cluster"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>cluster ()](#apidoc.element.redis.RedisClient.prototype.cluster)
- description and source-code
```javascript
cluster = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.command"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>command ()](#apidoc.element.redis.RedisClient.prototype.command)
- description and source-code
```javascript
command = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.config"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>config ()](#apidoc.element.redis.RedisClient.prototype.config)
- description and source-code
```javascript
config = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.connection_gone"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>connection_gone (why, error)](#apidoc.element.redis.RedisClient.prototype.connection_gone)
- description and source-code
```javascript
connection_gone = function (why, error) {
    // If a retry is already in progress, just let that happen
    if (this.retry_timer) {
        return;
    }
    error = error || null;

    debug('Redis connection is gone from ' + why + ' event.');
    this.connected = false;
    this.ready = false;
    // Deactivate cork to work with the offline queue
    this.cork = noop;
    this.uncork = noop;
    this.pipeline = false;
    this.pub_sub_mode = 0;

    // since we are collapsing end and close, users don't expect to be called twice
    if (!this.emitted_end) {
        this.emit('end');
        this.emitted_end = true;
    }

    // If this is a requested shutdown, then don't retry
    if (this.closing) {
        debug('Connection ended by quit / end command, not retrying.');
        this.flush_and_error({
            message: 'Stream connection ended and command aborted.',
            code: 'NR_CLOSED'
        }, {
            error: error
        });
        return;
    }

    if (typeof this.options.retry_strategy === 'function') {
        var retry_params = {
            attempt: this.attempts,
            error: error
        };
        if (this.options.camel_case) {
            retry_params.totalRetryTime = this.retry_totaltime;
            retry_params.timesConnected = this.times_connected;
        } else {
            retry_params.total_retry_time = this.retry_totaltime;
            retry_params.times_connected = this.times_connected;
        }
        this.retry_delay = this.options.retry_strategy(retry_params);
        if (typeof this.retry_delay !== 'number') {
            // Pass individual error through
            if (this.retry_delay instanceof Error) {
                error = this.retry_delay;
            }
            this.flush_and_error({
                message: 'Stream connection ended and command aborted.',
                code: 'NR_CLOSED'
            }, {
                error: error
            });
            this.end(false);
            return;
        }
    }

    if (this.max_attempts !== 0 && this.attempts >= this.max_attempts || this.retry_totaltime >= this.connect_timeout) {
        var message = 'Redis connection in broken state: ';
        if (this.retry_totaltime >= this.connect_timeout) {
            message += 'connection timeout exceeded.';
        } else {
            message += 'maximum connection attempts exceeded.';
        }

        this.flush_and_error({
            message: message,
            code: 'CONNECTION_BROKEN',
        }, {
            error: error
        });
        var err = new Error(message);
        err.code = 'CONNECTION_BROKEN';
        if (error) {
            err.origin = error;
        }
        this.emit('error', err);
        this.end(false);
        return;
    }

    // Retry commands after a reconnect instead of throwing an error. Use this with caution
    if (this.options.retry_unfulfilled_commands) {
        this.offline_queue.unshift.apply(this.offline_queue, this.command_queue.toArray());
        this.command_queue.clear();
    } else if (this.command_queue.length !== 0) {
        this.flush_and_error({
            message: 'Redis connection lost and command aborted.',
            code: 'UNCERTAIN_STATE'
        }, {
            error: error,
            queues: ['command_queue']
        });
    }

    if (this.retry_max_delay !== null && this.retry_delay > this.retry_max_delay) {
        this.retry_delay = this.retry_max_delay;
    } else if (this.retry_totaltime + this.retry_delay > this.connect_timeout) {
        // Do not exceed the maximum
        this.retry_delay = this.connect_timeout - this.retry_totaltime;
    }

    debug('Retry connection in ' + this.retry_delay + ' ms');

    this.retry_timer = setTimeout(retry_connection, this.retry_delay, this, error);
}
```
- example usage
```shell
...
    }
}

if (this.options.connect_timeout) {
    this.stream.setTimeout(this.connect_timeout, function () {
        // Note: This is only tested if a internet connection is established
        self.retry_totaltime = self.connect_timeout;
        self.connection_gone('timeout');
    });
}

/* istanbul ignore next: travis does not work with stunnel atm. Therefore the tls tests are skipped on travis */
var connect_event = this.options.tls ? 'secureConnect' : 'connect';
this.stream.once(connect_event, function () {
    this.removeAllListeners('timeout');
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.cork"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>cork ()](#apidoc.element.redis.RedisClient.prototype.cork)
- description and source-code
```javascript
function noop() {}
```
- example usage
```shell
...

debug('on_ready called ' + this.address + ' id ' + this.connection_id);
this.ready = true;

this.cork = function () {
    self.pipeline = true;
    if (self.stream.cork) {
        self.stream.cork();
    }
};
this.uncork = function () {
    if (self.fire_strings) {
        self.write_strings();
    } else {
        self.write_buffers();
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.create_stream"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>create_stream ()](#apidoc.element.redis.RedisClient.prototype.create_stream)
- description and source-code
```javascript
create_stream = function () {
    var self = this;

    // Init parser
    this.reply_parser = create_parser(this);

    if (this.options.stream) {
        // Only add the listeners once in case of a reconnect try (that won't work)
        if (this.stream) {
            return;
        }
        this.stream = this.options.stream;
    } else {
        // On a reconnect destroy the former stream and retry
        if (this.stream) {
            this.stream.removeAllListeners();
            this.stream.destroy();
        }

<span class="apidocCodeCommentSpan">        /* istanbul ignore if: travis does not work with stunnel atm. Therefore the tls tests are skipped on travis */
</span>        if (this.options.tls) {
            this.stream = tls.connect(this.connection_options);
        } else {
            this.stream = net.createConnection(this.connection_options);
        }
    }

    if (this.options.connect_timeout) {
        this.stream.setTimeout(this.connect_timeout, function () {
            // Note: This is only tested if a internet connection is established
            self.retry_totaltime = self.connect_timeout;
            self.connection_gone('timeout');
        });
    }

    /* istanbul ignore next: travis does not work with stunnel atm. Therefore the tls tests are skipped on travis */
    var connect_event = this.options.tls ? 'secureConnect' : 'connect';
    this.stream.once(connect_event, function () {
        this.removeAllListeners('timeout');
        self.times_connected++;
        self.on_connect();
    });

    this.stream.on('data', function (buffer_from_socket) {
        // The buffer_from_socket.toString() has a significant impact on big chunks and therefore this should only be used if necessary
        debug('Net read ' + self.address + ' id ' + self.connection_id); // + ': ' + buffer_from_socket.toString());
        self.reply_parser.execute(buffer_from_socket);
        self.emit_idle();
    });

    this.stream.on('error', function (err) {
        self.on_error(err);
    });

    /* istanbul ignore next: difficult to test and not important as long as we keep this listener */
    this.stream.on('clientError', function (err) {
        debug('clientError occured');
        self.on_error(err);
    });

    this.stream.once('close', function (hadError) {
        self.connection_gone('close');
    });

    this.stream.once('end', function () {
        self.connection_gone('end');
    });

    this.stream.on('drain', function () {
        self.drain();
    });

    if (this.options.socket_nodelay) {
        this.stream.setNoDelay();
    }

    // Fire the command before redis is connected to be sure it's the first fired command
    if (this.auth_pass !== undefined) {
        this.ready = true;
        this.auth(this.auth_pass);
        this.ready = false;
    }
}
```
- example usage
```shell
...
this.fire_strings = true; // Determine if strings or buffers should be written to the stream
this.pipeline = false;
this.sub_commands_left = 0;
this.times_connected = 0;
this.buffers = options.return_buffers || options.detect_buffers;
this.options = options;
this.reply = 'ON'; // Returning replies is the default
this.create_stream();
// The listeners will not be attached right away, so let's print the deprecation message while the listener is attached
this.on('newListener', function (event) {
    if (event === 'idle') {
        this.warn(
            'The idle event listener is deprecated and will likely be removed in v.3.0.0.\n' +
            'If you rely on this feature please open a new ticket in node_redis with your use case'
        );
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.dbsize"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>dbsize ()](#apidoc.element.redis.RedisClient.prototype.dbsize)
- description and source-code
```javascript
dbsize = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
    .scard("bigset")
    .smembers("bigset")
    .keys("*", function (err, replies) {
        // NOTE: code in this callback is NOT atomic
        // this only happens after the the .exec call finishes.
        client.mget(replies, redis.print);
    })
    .dbsize()
    .exec(function (err, replies) {
        console.log("MULTI got " + replies.length + " replies");
        replies.forEach(function (reply, index) {
            console.log("Reply " + index + ": " + reply.toString());
        });
    });
'''
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.debug"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>debug ()](#apidoc.element.redis.RedisClient.prototype.debug)
- description and source-code
```javascript
debug = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.decr"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>decr ()](#apidoc.element.redis.RedisClient.prototype.decr)
- description and source-code
```javascript
decr = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.decrby"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>decrby ()](#apidoc.element.redis.RedisClient.prototype.decrby)
- description and source-code
```javascript
decrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.del"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>del ()](#apidoc.element.redis.RedisClient.prototype.del)
- description and source-code
```javascript
del = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.discard"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>discard ()](#apidoc.element.redis.RedisClient.prototype.discard)
- description and source-code
```javascript
discard = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.drain"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>drain ()](#apidoc.element.redis.RedisClient.prototype.drain)
- description and source-code
```javascript
drain = function () {
    this.emit('drain');
    this.should_buffer = false;
}
```
- example usage
```shell
...
});

this.stream.once('end', function () {
    self.connection_gone('end');
});

this.stream.on('drain', function () {
    self.drain();
});

if (this.options.socket_nodelay) {
    this.stream.setNoDelay();
}

// Fire the command before redis is connected to be sure it's the first fired command
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.dump"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>dump ()](#apidoc.element.redis.RedisClient.prototype.dump)
- description and source-code
```javascript
dump = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.duplicate"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>duplicate (options, callback)](#apidoc.element.redis.RedisClient.prototype.duplicate)
- description and source-code
```javascript
duplicate = function (options, callback) {
    if (typeof options === 'function') {
        callback = options;
        options = null;
    }
    var existing_options = utils.clone(this.options);
    options = utils.clone(options);
    for (var elem in options) {
        existing_options[elem] = options[elem];
    }
    var client = new RedisClient(existing_options);
    client.selected_db = this.selected_db;
    if (typeof callback === 'function') {
        var ready_listener = function () {
            callback(null, client);
            client.removeAllListeners(error_listener);
        };
        var error_listener = function (err) {
            callback(err);
            client.end(true);
        };
        client.once('ready', ready_listener);
        client.once('error', error_listener);
        return;
    }
    return client;
}
```
- example usage
```shell
...
sub.subscribe("a nice channel");
'''

When a client issues a 'SUBSCRIBE' or 'PSUBSCRIBE', that connection is put into a "subscriber" mode.
At that point, only commands that modify the subscription set are valid and quit (and depending on the redis version ping as well
). When the subscription
set is empty, the connection is put back into regular mode.

If you need to send regular commands to Redis while in subscriber mode, just open another connection with a new client (hint: use
 'client.duplicate()').

## Subscriber Events

If a client has subscriptions active, it may emit these events:

### "message" (channel, message)
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.echo"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>echo ()](#apidoc.element.redis.RedisClient.prototype.echo)
- description and source-code
```javascript
echo = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.emit_idle"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>emit_idle ()](#apidoc.element.redis.RedisClient.prototype.emit_idle)
- description and source-code
```javascript
emit_idle = function () {
    if (this.command_queue.length === 0 && this.pub_sub_mode === 0) {
        this.emit('idle');
    }
}
```
- example usage
```shell
...
    self.on_connect();
});

this.stream.on('data', function (buffer_from_socket) {
    // The buffer_from_socket.toString() has a significant impact on big chunks and therefore this should only be used if necessary
    debug('Net read ' + self.address + ' id ' + self.connection_id); // + ': ' + buffer_from_socket.toString());
    self.reply_parser.execute(buffer_from_socket);
    self.emit_idle();
});

this.stream.on('error', function (err) {
    self.on_error(err);
});

/* istanbul ignore next: difficult to test and not important as long as we keep this listener */
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.end"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>end (flush)](#apidoc.element.redis.RedisClient.prototype.end)
- description and source-code
```javascript
end = function (flush) {
    // Flush queue if wanted
    if (flush) {
        this.flush_and_error({
            message: 'Connection forcefully ended and command aborted.',
            code: 'NR_CLOSED'
        });
    } else if (arguments.length === 0) {
        this.warn(
            'Using .end() without the flush parameter is deprecated and throws from v.3.0.0 on.\n' +
            'Please check the doku (https://github.com/NodeRedis/node_redis) and explictly use flush.'
        );
    }
    // Clear retry_timer
    if (this.retry_timer) {
        clearTimeout(this.retry_timer);
        this.retry_timer = null;
    }
    this.stream.removeAllListeners();
    this.stream.on('error', noop);
    this.connected = false;
    this.ready = false;
    this.closing = true;
    return this.stream.destroySoon();
}
```
- example usage
```shell
...

## client.quit()

This sends the quit command to the redis server and ends cleanly right after all running commands were properly handled.
If this is called while reconnecting (and therefor no connection to the redis server exists) it is going to end the connection right
 away instead of
resulting in further reconnections! All offline commands are going to be flushed with an error in that case.

## client.end(flush)

Forcibly close the connection to the Redis server. Note that this does not wait until all replies have been parsed.
If you want to exit cleanly, call 'client.quit()' as mentioned above.

You should set flush to true, if you are not absolutely sure you do not care about any other commands.
If you set flush to false all still running commands will silently fail.
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.eval"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>eval ()](#apidoc.element.redis.RedisClient.prototype.eval)
- description and source-code
```javascript
eval = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.evalsha"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>evalsha ()](#apidoc.element.redis.RedisClient.prototype.evalsha)
- description and source-code
```javascript
evalsha = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.exec"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>exec ()](#apidoc.element.redis.RedisClient.prototype.exec)
- description and source-code
```javascript
exec = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
    .smembers("bigset")
    .keys("*", function (err, replies) {
        // NOTE: code in this callback is NOT atomic
        // this only happens after the the .exec call finishes.
        client.mget(replies, redis.print);
    })
    .dbsize()
    .exec(function (err, replies) {
        console.log("MULTI got " + replies.length + " replies");
        replies.forEach(function (reply, index) {
            console.log("Reply " + index + ": " + reply.toString());
        });
    });
'''
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.exists"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>exists ()](#apidoc.element.redis.RedisClient.prototype.exists)
- description and source-code
```javascript
exists = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.expire"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>expire ()](#apidoc.element.redis.RedisClient.prototype.expire)
- description and source-code
```javascript
expire = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.expireat"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>expireat ()](#apidoc.element.redis.RedisClient.prototype.expireat)
- description and source-code
```javascript
expireat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.flush_and_error"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>flush_and_error (error_attributes, options)](#apidoc.element.redis.RedisClient.prototype.flush_and_error)
- description and source-code
```javascript
flush_and_error = function (error_attributes, options) {
    options = options || {};
    var aggregated_errors = [];
    var queue_names = options.queues || ['command_queue', 'offline_queue']; // Flush the command_queue first to keep the order intakt
    for (var i = 0; i < queue_names.length; i++) {
        // If the command was fired it might have been processed so far
        if (queue_names[i] === 'command_queue') {
            error_attributes.message += ' It might have been processed.';
        } else { // As the command_queue is flushed first, remove this for the offline queue
            error_attributes.message = error_attributes.message.replace(' It might have been processed.', '');
        }
        // Don't flush everything from the queue
        for (var command_obj = this[queue_names[i]].shift(); command_obj; command_obj = this[queue_names[i]].shift()) {
            var err = new errorClasses.AbortError(error_attributes);
            if (command_obj.error) {
                err.stack = err.stack + command_obj.error.stack.replace(/^Error.*?\n/, '\n');
            }
            err.command = command_obj.command.toUpperCase();
            if (command_obj.args && command_obj.args.length) {
                err.args = command_obj.args;
            }
            if (options.error) {
                err.origin = options.error;
            }
            if (typeof command_obj.callback === 'function') {
                command_obj.callback(err);
            } else {
                aggregated_errors.push(err);
            }
        }
    }
    // Currently this would be a breaking change, therefore it's only emitted in debug_mode
    if (exports.debug_mode && aggregated_errors.length) {
        var error;
        if (aggregated_errors.length === 1) {
            error = aggregated_errors[0];
        } else {
            error_attributes.message = error_attributes.message.replace('It', 'They').replace(/command/i, '$&s');
            error = new errorClasses.AggregateError(error_attributes);
            error.errors = aggregated_errors;
        }
        this.emit('error', error);
    }
}
```
- example usage
```shell
...
    self.return_error(err);
},
returnFatalError: function (err) {
    // Error out all fired commands. Otherwise they might rely on faulty data. We have to reconnect to get in a working state again
    // Note: the execution order is important. First flush and emit, then create the stream
    err.message += '. Please report this.';
    self.ready = false;
    self.flush_and_error({
        message: 'Fatal error encountert. Command aborted.',
        code: 'NR_FATAL'
    }, {
        error: err,
        queues: ['command_queue']
    });
    self.emit('error', err);
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.flushall"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>flushall ()](#apidoc.element.redis.RedisClient.prototype.flushall)
- description and source-code
```javascript
flushall = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.flushdb"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>flushdb ()](#apidoc.element.redis.RedisClient.prototype.flushdb)
- description and source-code
```javascript
flushdb = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.geoadd"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geoadd ()](#apidoc.element.redis.RedisClient.prototype.geoadd)
- description and source-code
```javascript
geoadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.geodist"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geodist ()](#apidoc.element.redis.RedisClient.prototype.geodist)
- description and source-code
```javascript
geodist = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.geohash"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geohash ()](#apidoc.element.redis.RedisClient.prototype.geohash)
- description and source-code
```javascript
geohash = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.geopos"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>geopos ()](#apidoc.element.redis.RedisClient.prototype.geopos)
- description and source-code
```javascript
geopos = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.georadius"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>georadius ()](#apidoc.element.redis.RedisClient.prototype.georadius)
- description and source-code
```javascript
georadius = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.georadiusbymember"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>georadiusbymember ()](#apidoc.element.redis.RedisClient.prototype.georadiusbymember)
- description and source-code
```javascript
georadiusbymember = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.get"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>get ()](#apidoc.element.redis.RedisClient.prototype.get)
- description and source-code
```javascript
get = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
bluebird.promisifyAll(redis.Multi.prototype);
'''

It'll add a *Async* to all node_redis functions (e.g. return client.getAsync().then())

'''js
// We expect a value 'foo': 'bar' to be present
// So instead of writing client.get('foo', cb); you have to write:
return client.getAsync('foo').then(function(res) {
    console.log(res); // => 'bar'
});

// Using multi with promises looks like:

return client.multi().get('foo').execAsync().then(function(res) {
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.getbit"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>getbit ()](#apidoc.element.redis.RedisClient.prototype.getbit)
- description and source-code
```javascript
getbit = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.getrange"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>getrange ()](#apidoc.element.redis.RedisClient.prototype.getrange)
- description and source-code
```javascript
getrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.getset"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>getset ()](#apidoc.element.redis.RedisClient.prototype.getset)
- description and source-code
```javascript
getset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.handle_reply"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>handle_reply (reply, command)](#apidoc.element.redis.RedisClient.prototype.handle_reply)
- description and source-code
```javascript
handle_reply = function (reply, command) {
    if (command === 'hgetall') {
        reply = utils.reply_to_object(reply);
    }
    return reply;
}
```
- example usage
```shell
...
    }
};

function normal_reply (self, reply) {
    var command_obj = self.command_queue.shift();
    if (typeof command_obj.callback === 'function') {
        if (command_obj.command !== 'exec') {
            reply = self.handle_reply(reply, command_obj.command, command_obj.buffer_args);
        }
        command_obj.callback(null, reply);
    } else {
        debug('No callback for reply');
    }
}
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hdel"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hdel ()](#apidoc.element.redis.RedisClient.prototype.hdel)
- description and source-code
```javascript
hdel = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hexists"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hexists ()](#apidoc.element.redis.RedisClient.prototype.hexists)
- description and source-code
```javascript
hexists = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hget"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hget ()](#apidoc.element.redis.RedisClient.prototype.hget)
- description and source-code
```javascript
hget = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hgetall"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hgetall ()](#apidoc.element.redis.RedisClient.prototype.hgetall)
- description and source-code
```javascript
hgetall = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
'''

## Friendlier hash commands

Most Redis commands take a single String or an Array of Strings as arguments, and replies are sent back as a single String or an
 Array of Strings.
When dealing with hash values, there are a couple of useful exceptions to this.

### client.hgetall(hash, callback)

The reply from an HGETALL command will be converted into a JavaScript Object by 'node_redis'. That way you can interact
with the responses using JavaScript syntax.

Example:

'''js
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hincrby"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hincrby ()](#apidoc.element.redis.RedisClient.prototype.hincrby)
- description and source-code
```javascript
hincrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hincrbyfloat"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hincrbyfloat ()](#apidoc.element.redis.RedisClient.prototype.hincrbyfloat)
- description and source-code
```javascript
hincrbyfloat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hkeys"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hkeys ()](#apidoc.element.redis.RedisClient.prototype.hkeys)
- description and source-code
```javascript
hkeys = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
client.on("error", function (err) {
    console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
    console.log(replies.length + " replies:");
    replies.forEach(function (reply, i) {
        console.log("    " + i + ": " + reply);
    });
    client.quit();
});
'''
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hlen"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hlen ()](#apidoc.element.redis.RedisClient.prototype.hlen)
- description and source-code
```javascript
hlen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hmget"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hmget ()](#apidoc.element.redis.RedisClient.prototype.hmget)
- description and source-code
```javascript
hmget = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hmset"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hmset ()](#apidoc.element.redis.RedisClient.prototype.hmset)
- description and source-code
```javascript
function hmset() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else if (Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else if (typeof arguments[1] === 'object' && (arguments.length === 2 || arguments.length === 3 && (typeof arguments[2] === '
function' || typeof arguments[2] === 'undefined'))) {
        arr = [arguments[0]];
        for (var field in arguments[1]) {
            arr.push(field, arguments[1][field]);
        }
        callback = arguments[2];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command('hmset', arr, callback));
}
```
- example usage
```shell
...

Each Redis command is exposed as a function on the 'client' object.
All functions take either an 'args' Array plus optional 'callback' Function or
a variable number of individual arguments followed by an optional callback.
Examples:

'''js
client.hmset(["key", "test keys 1", "test val 1", "test keys 2", "test val 2"], function (err, res) {});
// Works the same as
client.hmset("key", ["test keys 1", "test val 1", "test keys 2", "test val 2"], function (err, res) {});
// Or
client.hmset("key", "test keys 1", "test val 1", "test keys 2", "test val 2", function (err, res) {});
'''

Note that in either form the 'callback' is optional:
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hscan"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hscan ()](#apidoc.element.redis.RedisClient.prototype.hscan)
- description and source-code
```javascript
hscan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hset"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hset ()](#apidoc.element.redis.RedisClient.prototype.hset)
- description and source-code
```javascript
hset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
// client.select(3, function() { /* ... */ });

client.on("error", function (err) {
console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
console.log(replies.length + " replies:");
replies.forEach(function (reply, i) {
    console.log("    " + i + ": " + reply);
});
client.quit();
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hsetnx"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hsetnx ()](#apidoc.element.redis.RedisClient.prototype.hsetnx)
- description and source-code
```javascript
hsetnx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hstrlen"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hstrlen ()](#apidoc.element.redis.RedisClient.prototype.hstrlen)
- description and source-code
```javascript
hstrlen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.hvals"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>hvals ()](#apidoc.element.redis.RedisClient.prototype.hvals)
- description and source-code
```javascript
hvals = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.incr"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>incr ()](#apidoc.element.redis.RedisClient.prototype.incr)
- description and source-code
```javascript
incr = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...

'''js
var redis  = require("redis"),
    client = redis.createClient(), multi;

// start a separate multi command queue
multi = client.multi();
multi.incr("incr thing", redis.print);
multi.incr("incr other thing", redis.print);

// runs immediately
client.mset("incr thing", 100, "incr other thing", 1, redis.print);

// drains multi queue and runs atomically
multi.exec(function (err, replies) {
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.incrby"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>incrby ()](#apidoc.element.redis.RedisClient.prototype.incrby)
- description and source-code
```javascript
incrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.incrbyfloat"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>incrbyfloat ()](#apidoc.element.redis.RedisClient.prototype.incrbyfloat)
- description and source-code
```javascript
incrbyfloat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.info"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>info (section, callback)](#apidoc.element.redis.RedisClient.prototype.info)
- description and source-code
```javascript
function info(section, callback) {
    var args = [];
    if (typeof section === 'function') {
        callback = section;
    } else if (section !== undefined) {
        args = Array.isArray(section) ? section : [section];
    }
    return this.internal_send_command(new Command('info', args, info_callback(this, callback)));
}
```
- example usage
```shell
...
};

RedisClient.prototype.ready_check = function () {
var self = this;
debug('Checking server ready state...');
// Always fire this info command as first command even if other commands are already queued up
this.ready = true;
this.info(function (err, res) {
    self.on_info_cmd(err, res);
});
this.ready = false;
};

RedisClient.prototype.send_offline_queue = function () {
for (var command_obj = this.offline_queue.shift(); command_obj; command_obj = this.offline_queue.shift()) {
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.initialize_retry_vars"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>initialize_retry_vars ()](#apidoc.element.redis.RedisClient.prototype.initialize_retry_vars)
- description and source-code
```javascript
initialize_retry_vars = function () {
    this.retry_timer = null;
    this.retry_totaltime = 0;
    this.retry_delay = 200;
    this.retry_backoff = 1.7;
    this.attempts = 1;
}
```
- example usage
```shell
...
if ('retry_max_delay' in options) {
    self.warn(
        'retry_max_delay is deprecated and will be removed in v.3.0.0.\n' +
        'To reduce the amount of options and the improve the reconnection handling please use the new 'retry_strategy' option instead
.\n' +
        'This replaces the max_attempts and retry_max_delay option.'
    );
}
this.initialize_retry_vars();
this.pub_sub_mode = 0;
this.subscription_set = {};
this.monitoring = false;
this.message_buffers = false;
this.closing = false;
this.server_info = {};
this.auth_pass = options.auth_pass || options.password;
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.internal_send_command"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>internal_send_command (command_obj)](#apidoc.element.redis.RedisClient.prototype.internal_send_command)
- description and source-code
```javascript
internal_send_command = function (command_obj) {
    var arg, prefix_keys;
    var i = 0;
    var command_str = '';
    var args = command_obj.args;
    var command = command_obj.command;
    var len = args.length;
    var big_data = false;
    var args_copy = new Array(len);

    if (process.domain && command_obj.callback) {
        command_obj.callback = process.domain.bind(command_obj.callback);
    }

    if (this.ready === false || this.stream.writable === false) {
        // Handle offline commands right away
        handle_offline_command(this, command_obj);
        return false; // Indicate buffering
    }

    for (i = 0; i < len; i += 1) {
        if (typeof args[i] === 'string') {
            // 30000 seemed to be a good value to switch to buffers after testing and checking the pros and cons
            if (args[i].length > 30000) {
                big_data = true;
                args_copy[i] = new Buffer(args[i], 'utf8');
            } else {
                args_copy[i] = args[i];
            }
        } else if (typeof args[i] === 'object') { // Checking for object instead of Buffer.isBuffer helps us finding data types
that we can't handle properly
            if (args[i] instanceof Date) { // Accept dates as valid input
                args_copy[i] = args[i].toString();
            } else if (args[i] === null) {
                this.warn(
                    'Deprecated: The ' + command.toUpperCase() + ' command contains a "null" argument.\n' +
                    'This is converted to a "null" string now and will return an error from v.3.0 on.\n' +
                    'Please handle this in your code to make sure everything works as you intended it to.'
                );
                args_copy[i] = 'null'; // Backwards compatible :/
            } else if (Buffer.isBuffer(args[i])) {
                args_copy[i] = args[i];
                command_obj.buffer_args = true;
                big_data = true;
            } else {
                this.warn(
                    'Deprecated: The ' + command.toUpperCase() + ' command contains a argument of type ' + args[i].constructor.name
 + '.\n' +
                    'This is converted to "' + args[i].toString() + '" by using .toString() now and will return an error from v.
3.0 on.\n' +
                    'Please handle this in your code to make sure everything works as you intended it to.'
                );
                args_copy[i] = args[i].toString(); // Backwards compatible :/
            }
        } else if (typeof args[i] === 'undefined') {
            this.warn(
                'Deprecated: The ' + command.toUpperCase() + ' command contains a "undefined" argument.\n' +
                'This is converted to a "undefined" string now and will return an error from v.3.0 on.\n' +
                'Please handle this in your code to make sure everything works as you intended it to.'
            );
            args_copy[i] = 'undefined'; // Backwards compatible :/
        } else {
            // Seems like numbers are converted fast using string concatenation
            args_copy[i] = '' + args[i];
        }
    }

    if (this.options.prefix) {
        prefix_keys = commands.getKeyIndexes(command, args_copy);
        for (i = prefix_keys.pop(); i !== undefined; i = prefix_keys.pop()) {
            args_copy[i] = this.options.prefix + args_copy[i];
        }
    }
    if (this.options.rename_commands && this.options.rename_commands[command]) {
        command = this.options.rename_commands[command];
    }
    // Always use 'Multi bulk commands', but if passed any Buffer args, then do multiple writes, one for each arg.
    // This means that using Buffers in commands is going to be slower, so use Strings if you don't already have a Buffer.
    command_str = '*' + (len + 1) + '\r\n$' + command.length + '\r\n' + command + '\r\n';

    if (big_data === false) { // Build up a string and send entire command in one write
        for (i = 0; i < len; i += 1) {
            arg = args_copy[i];
            command_str += '$' + Buffer.byteLength(arg) + '\r\n' + arg + '\r\n';
        } ...
```
- example usage
```shell
...
        // TODO: Consider using next tick here. See https://github.com/NodeRedis/node_redis/issues/1033
        self.stream.uncork();
    }
};

// Restore modal commands from previous connection. The order of the commands is important
if (this.selected_db !== undefined) {
    this.internal_send_command(new Command('select', [this.selected_db]));
}
if (this.monitoring) { // Monitor has to be fired before pub sub commands
    this.internal_send_command(new Command('monitor', []));
}
var callback_count = Object.keys(this.subscription_set).length;
if (!this.options.disable_resubscribing && callback_count) {
    // only emit 'ready' when all subscriptions were made again
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.keys"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>keys ()](#apidoc.element.redis.RedisClient.prototype.keys)
- description and source-code
```javascript
keys = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
set_size -= 1;
}

// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
.exec(function (err, replies) {
    console.log("MULTI got " + replies.length + " replies");
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lastsave"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lastsave ()](#apidoc.element.redis.RedisClient.prototype.lastsave)
- description and source-code
```javascript
lastsave = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.latency"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>latency ()](#apidoc.element.redis.RedisClient.prototype.latency)
- description and source-code
```javascript
latency = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lindex"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lindex ()](#apidoc.element.redis.RedisClient.prototype.lindex)
- description and source-code
```javascript
lindex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.linsert"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>linsert ()](#apidoc.element.redis.RedisClient.prototype.linsert)
- description and source-code
```javascript
linsert = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.llen"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>llen ()](#apidoc.element.redis.RedisClient.prototype.llen)
- description and source-code
```javascript
llen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lpop"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lpop ()](#apidoc.element.redis.RedisClient.prototype.lpop)
- description and source-code
```javascript
lpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lpush"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lpush ()](#apidoc.element.redis.RedisClient.prototype.lpush)
- description and source-code
```javascript
lpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lpushx"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lpushx ()](#apidoc.element.redis.RedisClient.prototype.lpushx)
- description and source-code
```javascript
lpushx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lrange"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lrange ()](#apidoc.element.redis.RedisClient.prototype.lrange)
- description and source-code
```javascript
lrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lrem"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lrem ()](#apidoc.element.redis.RedisClient.prototype.lrem)
- description and source-code
```javascript
lrem = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.lset"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>lset ()](#apidoc.element.redis.RedisClient.prototype.lset)
- description and source-code
```javascript
lset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ltrim"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ltrim ()](#apidoc.element.redis.RedisClient.prototype.ltrim)
- description and source-code
```javascript
ltrim = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.memory"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>memory ()](#apidoc.element.redis.RedisClient.prototype.memory)
- description and source-code
```javascript
memory = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.mget"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>mget ()](#apidoc.element.redis.RedisClient.prototype.mget)
- description and source-code
```javascript
mget = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
.exec(function (err, replies) {
    console.log("MULTI got " + replies.length + " replies");
    replies.forEach(function (reply, index) {
        console.log("Reply " + index + ": " + reply.toString());
    });
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.migrate"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>migrate ()](#apidoc.element.redis.RedisClient.prototype.migrate)
- description and source-code
```javascript
migrate = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.module"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>module ()](#apidoc.element.redis.RedisClient.prototype.module)
- description and source-code
```javascript
module = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.monitor"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>monitor (callback)](#apidoc.element.redis.RedisClient.prototype.monitor)
- description and source-code
```javascript
function monitor(callback) {
    // Use a individual command, as this is a special case that does not has to be checked for any other command
    var self = this;
    var call_on_write = function () {
        // Activating monitor mode has to happen before Redis returned the callback. The monitor result is returned first.
        // Therefore we expect the command to be properly processed. If this is not the case, it's not an issue either.
        self.monitoring = true;
    };
    return this.internal_send_command(new Command('monitor', [], callback, call_on_write));
}
```
- example usage
```shell
...
A 'monitor' event is going to be emitted for every command fired from any client connected to the server including the monitoring
 client itself.
The callback for the 'monitor' event takes a timestamp from the Redis server, an array of command arguments and the raw monitoring
 string.

Example:

'''js
var client  = require("redis").createClient();
client.monitor(function (err, res) {
    console.log("Entering monitoring mode.");
});
client.set('foo', 'bar');

client.on("monitor", function (time, args, raw_reply) {
    console.log(time + ": " + args); // 1458910076.446514:['set', 'foo', 'bar']
});
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.move"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>move ()](#apidoc.element.redis.RedisClient.prototype.move)
- description and source-code
```javascript
move = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.mset"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>mset ()](#apidoc.element.redis.RedisClient.prototype.mset)
- description and source-code
```javascript
mset = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...

// start a separate multi command queue
multi = client.multi();
multi.incr("incr thing", redis.print);
multi.incr("incr other thing", redis.print);

// runs immediately
client.mset("incr thing", 100, "incr other thing", 1, redis.print);

// drains multi queue and runs atomically
multi.exec(function (err, replies) {
    console.log(replies); // 101, 2
});
'''
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.msetnx"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>msetnx ()](#apidoc.element.redis.RedisClient.prototype.msetnx)
- description and source-code
```javascript
msetnx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.multi"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>multi (args)](#apidoc.element.redis.RedisClient.prototype.multi)
- description and source-code
```javascript
function multi(args) {
    var multi = new Multi(this, args);
    multi.exec = multi.EXEC = multi.exec_transaction;
    return multi;
}
```
- example usage
```shell
...
// So instead of writing client.get('foo', cb); you have to write:
return client.getAsync('foo').then(function(res) {
    console.log(res); // => 'bar'
});

// Using multi with promises looks like:

return client.multi().get('foo').execAsync().then(function(res) {
    console.log(res); // => 'bar'
});
'''

### Sending Commands

Each Redis command is exposed as a function on the 'client' object.
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.object"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>object ()](#apidoc.element.redis.RedisClient.prototype.object)
- description and source-code
```javascript
object = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.on_connect"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_connect ()](#apidoc.element.redis.RedisClient.prototype.on_connect)
- description and source-code
```javascript
on_connect = function () {
    debug('Stream connected ' + this.address + ' id ' + this.connection_id);

    this.connected = true;
    this.ready = false;
    this.emitted_end = false;
    this.stream.setKeepAlive(this.options.socket_keepalive);
    this.stream.setTimeout(0);

    this.emit('connect');
    this.initialize_retry_vars();

    if (this.options.no_ready_check) {
        this.on_ready();
    } else {
        this.ready_check();
    }
}
```
- example usage
```shell
...
}

/* istanbul ignore next: travis does not work with stunnel atm. Therefore the tls tests are skipped on travis */
var connect_event = this.options.tls ? 'secureConnect' : 'connect';
this.stream.once(connect_event, function () {
    this.removeAllListeners('timeout');
    self.times_connected++;
    self.on_connect();
});

this.stream.on('data', function (buffer_from_socket) {
    // The buffer_from_socket.toString() has a significant impact on big chunks and therefore this should only be used if necessary
    debug('Net read ' + self.address + ' id ' + self.connection_id); // + ': ' + buffer_from_socket.toString());
    self.reply_parser.execute(buffer_from_socket);
    self.emit_idle();
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.on_error"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_error (err)](#apidoc.element.redis.RedisClient.prototype.on_error)
- description and source-code
```javascript
on_error = function (err) {
    if (this.closing) {
        return;
    }

    err.message = 'Redis connection to ' + this.address + ' failed - ' + err.message;
    debug(err.message);
    this.connected = false;
    this.ready = false;

    // Only emit the error if the retry_stategy option is not set
    if (!this.options.retry_strategy) {
        this.emit('error', err);
    }
    // 'error' events get turned into exceptions if they aren't listened for. If the user handled this error
    // then we should try to reconnect.
    this.connection_gone('error', err);
}
```
- example usage
```shell
...
    // The buffer_from_socket.toString() has a significant impact on big chunks and therefore this should only be used if necessary
    debug('Net read ' + self.address + ' id ' + self.connection_id); // + ': ' + buffer_from_socket.toString());
    self.reply_parser.execute(buffer_from_socket);
    self.emit_idle();
});

this.stream.on('error', function (err) {
    self.on_error(err);
});

/* istanbul ignore next: difficult to test and not important as long as we keep this listener */
this.stream.on('clientError', function (err) {
    debug('clientError occured');
    self.on_error(err);
});
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.on_info_cmd"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_info_cmd (err, res)](#apidoc.element.redis.RedisClient.prototype.on_info_cmd)
- description and source-code
```javascript
on_info_cmd = function (err, res) {
    if (err) {
        if (err.message === "ERR unknown command 'info'") {
            this.on_ready();
            return;
        }
        err.message = 'Ready check failed: ' + err.message;
        this.emit('error', err);
        return;
    }

<span class="apidocCodeCommentSpan">    /* istanbul ignore if: some servers might not respond with any info data. This is just a safety check that is difficult to test
 */
</span>    if (!res) {
        debug('The info command returned without any data.');
        this.on_ready();
        return;
    }

    if (!this.server_info.loading || this.server_info.loading === '0') {
        // If the master_link_status exists but the link is not up, try again after 50 ms
        if (this.server_info.master_link_status && this.server_info.master_link_status !== 'up') {
            this.server_info.loading_eta_seconds = 0.05;
        } else {
            // Eta loading should change
            debug('Redis server ready.');
            this.on_ready();
            return;
        }
    }

    var retry_time = +this.server_info.loading_eta_seconds * 1000;
    if (retry_time > 1000) {
        retry_time = 1000;
    }
    debug('Redis server still loading, trying again in ' + retry_time);
    setTimeout(function (self) {
        self.ready_check();
    }, retry_time, this);
}
```
- example usage
```shell
...

RedisClient.prototype.ready_check = function () {
var self = this;
debug('Checking server ready state...');
// Always fire this info command as first command even if other commands are already queued up
this.ready = true;
this.info(function (err, res) {
    self.on_info_cmd(err, res);
});
this.ready = false;
};

RedisClient.prototype.send_offline_queue = function () {
for (var command_obj = this.offline_queue.shift(); command_obj; command_obj = this.offline_queue.shift()) {
    debug('Sending offline command: ' + command_obj.command);
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.on_ready"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>on_ready ()](#apidoc.element.redis.RedisClient.prototype.on_ready)
- description and source-code
```javascript
on_ready = function () {
    var self = this;

    debug('on_ready called ' + this.address + ' id ' + this.connection_id);
    this.ready = true;

    this.cork = function () {
        self.pipeline = true;
        if (self.stream.cork) {
            self.stream.cork();
        }
    };
    this.uncork = function () {
        if (self.fire_strings) {
            self.write_strings();
        } else {
            self.write_buffers();
        }
        self.pipeline = false;
        self.fire_strings = true;
        if (self.stream.uncork) {
            // TODO: Consider using next tick here. See https://github.com/NodeRedis/node_redis/issues/1033
            self.stream.uncork();
        }
    };

    // Restore modal commands from previous connection. The order of the commands is important
    if (this.selected_db !== undefined) {
        this.internal_send_command(new Command('select', [this.selected_db]));
    }
    if (this.monitoring) { // Monitor has to be fired before pub sub commands
        this.internal_send_command(new Command('monitor', []));
    }
    var callback_count = Object.keys(this.subscription_set).length;
    if (!this.options.disable_resubscribing && callback_count) {
        // only emit 'ready' when all subscriptions were made again
        // TODO: Remove the countdown for ready here. This is not coherent with all other modes and should therefore not be handled
 special
        // We know we are ready as soon as all commands were fired
        var callback = function () {
            callback_count--;
            if (callback_count === 0) {
                self.emit('ready');
            }
        };
        debug('Sending pub/sub on_ready commands');
        for (var key in this.subscription_set) {
            var command = key.slice(0, key.indexOf('_'));
            var args = this.subscription_set[key];
            this[command]([args], callback);
        }
        this.send_offline_queue();
        return;
    }
    this.send_offline_queue();
    this.emit('ready');
}
```
- example usage
```shell
...
this.stream.setKeepAlive(this.options.socket_keepalive);
this.stream.setTimeout(0);

this.emit('connect');
this.initialize_retry_vars();

if (this.options.no_ready_check) {
    this.on_ready();
} else {
    this.ready_check();
}
};

RedisClient.prototype.on_ready = function () {
var self = this;
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.persist"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>persist ()](#apidoc.element.redis.RedisClient.prototype.persist)
- description and source-code
```javascript
persist = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pexpire"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pexpire ()](#apidoc.element.redis.RedisClient.prototype.pexpire)
- description and source-code
```javascript
pexpire = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pexpireat"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pexpireat ()](#apidoc.element.redis.RedisClient.prototype.pexpireat)
- description and source-code
```javascript
pexpireat = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pfadd"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfadd ()](#apidoc.element.redis.RedisClient.prototype.pfadd)
- description and source-code
```javascript
pfadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pfcount"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfcount ()](#apidoc.element.redis.RedisClient.prototype.pfcount)
- description and source-code
```javascript
pfcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pfdebug"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfdebug ()](#apidoc.element.redis.RedisClient.prototype.pfdebug)
- description and source-code
```javascript
pfdebug = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pfmerge"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfmerge ()](#apidoc.element.redis.RedisClient.prototype.pfmerge)
- description and source-code
```javascript
pfmerge = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pfselftest"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pfselftest ()](#apidoc.element.redis.RedisClient.prototype.pfselftest)
- description and source-code
```javascript
pfselftest = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ping"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ping ()](#apidoc.element.redis.RedisClient.prototype.ping)
- description and source-code
```javascript
ping = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.post"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>post ()](#apidoc.element.redis.RedisClient.prototype.post)
- description and source-code
```javascript
post = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.psetex"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>psetex ()](#apidoc.element.redis.RedisClient.prototype.psetex)
- description and source-code
```javascript
psetex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.psubscribe"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>psubscribe ()](#apidoc.element.redis.RedisClient.prototype.psubscribe)
- description and source-code
```javascript
function psubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('psubscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.psync"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>psync ()](#apidoc.element.redis.RedisClient.prototype.psync)
- description and source-code
```javascript
psync = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pttl"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pttl ()](#apidoc.element.redis.RedisClient.prototype.pttl)
- description and source-code
```javascript
pttl = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.publish"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>publish ()](#apidoc.element.redis.RedisClient.prototype.publish)
- description and source-code
```javascript
publish = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...

'''js
var redis = require("redis");
var sub = redis.createClient(), pub = redis.createClient();
var msg_count = 0;

sub.on("subscribe", function (channel, count) {
pub.publish("a nice channel", "I am sending a message.");
pub.publish("a nice channel", "I am sending a second message.");
pub.publish("a nice channel", "I am sending my last message.");
});

sub.on("message", function (channel, message) {
console.log("sub channel " + channel + ": " + message);
msg_count += 1;
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.pubsub"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>pubsub ()](#apidoc.element.redis.RedisClient.prototype.pubsub)
- description and source-code
```javascript
pubsub = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.punsubscribe"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>punsubscribe ()](#apidoc.element.redis.RedisClient.prototype.punsubscribe)
- description and source-code
```javascript
function punsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('punsubscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.quit"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>quit (callback)](#apidoc.element.redis.RedisClient.prototype.quit)
- description and source-code
```javascript
function quit(callback) {
    // TODO: Consider this for v.3
    // Allow the quit command to be fired as soon as possible to prevent it landing in the offline queue.
    // this.ready = this.offline_queue.length === 0;
    var backpressure_indicator = this.internal_send_command(new Command('quit', [], quit_callback(this, callback)));
    // Calling quit should always end the connection, no matter if there's a connection or not
    this.closing = true;
    this.ready = false;
    return backpressure_indicator;
}
```
- example usage
```shell
...
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
console.log(replies.length + " replies:");
replies.forEach(function (reply, i) {
    console.log("    " + i + ": " + reply);
});
client.quit();
});
'''

This will display:

mjr:~/work/node_redis (master)$ node example.js
Reply: OK
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.randomkey"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>randomkey ()](#apidoc.element.redis.RedisClient.prototype.randomkey)
- description and source-code
```javascript
randomkey = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.readonly"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>readonly ()](#apidoc.element.redis.RedisClient.prototype.readonly)
- description and source-code
```javascript
readonly = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.readwrite"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>readwrite ()](#apidoc.element.redis.RedisClient.prototype.readwrite)
- description and source-code
```javascript
readwrite = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ready_check"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ready_check ()](#apidoc.element.redis.RedisClient.prototype.ready_check)
- description and source-code
```javascript
ready_check = function () {
    var self = this;
    debug('Checking server ready state...');
    // Always fire this info command as first command even if other commands are already queued up
    this.ready = true;
    this.info(function (err, res) {
        self.on_info_cmd(err, res);
    });
    this.ready = false;
}
```
- example usage
```shell
...

this.emit('connect');
this.initialize_retry_vars();

if (this.options.no_ready_check) {
    this.on_ready();
} else {
    this.ready_check();
}
};

RedisClient.prototype.on_ready = function () {
var self = this;

debug('on_ready called ' + this.address + ' id ' + this.connection_id);
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.rename"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rename ()](#apidoc.element.redis.RedisClient.prototype.rename)
- description and source-code
```javascript
rename = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.renamenx"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>renamenx ()](#apidoc.element.redis.RedisClient.prototype.renamenx)
- description and source-code
```javascript
renamenx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.replconf"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>replconf ()](#apidoc.element.redis.RedisClient.prototype.replconf)
- description and source-code
```javascript
replconf = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.restore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>restore ()](#apidoc.element.redis.RedisClient.prototype.restore)
- description and source-code
```javascript
restore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.restore-asking"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>restore-asking ()](#apidoc.element.redis.RedisClient.prototype.restore-asking)
- description and source-code
```javascript
restore-asking = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.return_error"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>return_error (err)](#apidoc.element.redis.RedisClient.prototype.return_error)
- description and source-code
```javascript
return_error = function (err) {
    var command_obj = this.command_queue.shift();
    if (command_obj.error) {
        err.stack = command_obj.error.stack.replace(/^Error.*?\n/, 'ReplyError: ' + err.message + '\n');
    }
    err.command = command_obj.command.toUpperCase();
    if (command_obj.args && command_obj.args.length) {
        err.args = command_obj.args;
    }

    // Count down pub sub mode if in entering modus
    if (this.pub_sub_mode > 1) {
        this.pub_sub_mode--;
    }

    var match = err.message.match(utils.err_code);
    // LUA script could return user errors that don't behave like all other errors!
    if (match) {
        err.code = match[1];
    }

    utils.callback_or_emit(this, command_obj.callback, err);
}
```
- example usage
```shell
...
function create_parser (self) {
return new Parser({
    returnReply: function (data) {
        self.return_reply(data);
    },
    returnError: function (err) {
        // Return a ReplyError to indicate Redis returned an error
        self.return_error(err);
    },
    returnFatalError: function (err) {
        // Error out all fired commands. Otherwise they might rely on faulty data. We have to reconnect to get in a working state
 again
        // Note: the execution order is important. First flush and emit, then create the stream
        err.message += '. Please report this.';
        self.ready = false;
        self.flush_and_error({
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.return_reply"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>return_reply (reply)](#apidoc.element.redis.RedisClient.prototype.return_reply)
- description and source-code
```javascript
return_reply = function (reply) {
    if (this.monitoring) {
        var replyStr;
        if (this.buffers && Buffer.isBuffer(reply)) {
            replyStr = reply.toString();
        } else {
            replyStr = reply;
        }
        // If in monitor mode, all normal commands are still working and we only want to emit the streamlined commands
        if (typeof replyStr === 'string' && utils.monitor_regex.test(replyStr)) {
            var timestamp = replyStr.slice(0, replyStr.indexOf(' '));
            var args = replyStr.slice(replyStr.indexOf('"') + 1, -1).split('" "').map(function (elem) {
                return elem.replace(/\\"/g, '"');
            });
            this.emit('monitor', timestamp, args, replyStr);
            return;
        }
    }
    if (this.pub_sub_mode === 0) {
        normal_reply(this, reply);
    } else if (this.pub_sub_mode !== 1) {
        this.pub_sub_mode--;
        normal_reply(this, reply);
    } else if (!(reply instanceof Array) || reply.length <= 2) {
        // Only PING and QUIT are allowed in this context besides the pub sub commands
        // Ping replies with ['pong', null|value] and quit with 'OK'
        normal_reply(this, reply);
    } else {
        return_pub_sub(this, reply);
    }
}
```
- example usage
```shell
...
util.inherits(RedisClient, EventEmitter);

RedisClient.connection_id = 0;

function create_parser (self) {
return new Parser({
    returnReply: function (data) {
        self.return_reply(data);
    },
    returnError: function (err) {
        // Return a ReplyError to indicate Redis returned an error
        self.return_error(err);
    },
    returnFatalError: function (err) {
        // Error out all fired commands. Otherwise they might rely on faulty data. We have to reconnect to get in a working state
 again
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.role"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>role ()](#apidoc.element.redis.RedisClient.prototype.role)
- description and source-code
```javascript
role = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.rpop"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpop ()](#apidoc.element.redis.RedisClient.prototype.rpop)
- description and source-code
```javascript
rpop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.rpoplpush"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpoplpush ()](#apidoc.element.redis.RedisClient.prototype.rpoplpush)
- description and source-code
```javascript
rpoplpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.rpush"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpush ()](#apidoc.element.redis.RedisClient.prototype.rpush)
- description and source-code
```javascript
rpush = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.rpushx"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>rpushx ()](#apidoc.element.redis.RedisClient.prototype.rpushx)
- description and source-code
```javascript
rpushx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sadd"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sadd ()](#apidoc.element.redis.RedisClient.prototype.sadd)
- description and source-code
```javascript
sadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
Redis. The interface in 'node_redis' is to return an individual 'Multi' object by calling 'client.multi()'.
If any command fails to queue, all commands are rolled back and none is going to be executed (For further information look at [transactions
](http://redis.io/topics/transactions)).

'''js
var redis  = require("./index"),
    client = redis.createClient(), set_size = 20;

client.sadd("bigset", "a member");
client.sadd("bigset", "another member");

while (set_size > 0) {
    client.sadd("bigset", "member " + set_size);
    set_size -= 1;
}
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.save"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>save ()](#apidoc.element.redis.RedisClient.prototype.save)
- description and source-code
```javascript
save = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.scan"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>scan ()](#apidoc.element.redis.RedisClient.prototype.scan)
- description and source-code
```javascript
scan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.scard"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>scard ()](#apidoc.element.redis.RedisClient.prototype.scard)
- description and source-code
```javascript
scard = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
while (set_size > 0) {
client.sadd("bigset", "member " + set_size);
set_size -= 1;
}

// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.script"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>script ()](#apidoc.element.redis.RedisClient.prototype.script)
- description and source-code
```javascript
script = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
Note that this program will not exit cleanly because the client is still connected.

## Multi-word commands

To execute redis multi-word commands like 'SCRIPT LOAD' or 'CLIENT LIST' pass
the second word as first parameter:

    client.script('load', 'return 1');
    client.multi().script('load', 'return 1').exec(...);
    client.multi([['script', 'load', 'return 1']]).exec(...);

## client.duplicate([options][, callback])

Duplicate all current options and return a new redisClient instance. All options passed to the duplicate function are going to replace
 the original option.
If you pass a callback, duplicate is going to wait until the client is ready and returns it in the callback. If an error occurs
in the meanwhile, that is going to return an error instead in the callback.
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sdiff"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sdiff ()](#apidoc.element.redis.RedisClient.prototype.sdiff)
- description and source-code
```javascript
sdiff = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sdiffstore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sdiffstore ()](#apidoc.element.redis.RedisClient.prototype.sdiffstore)
- description and source-code
```javascript
sdiffstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.select"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>select (db, callback)](#apidoc.element.redis.RedisClient.prototype.select)
- description and source-code
```javascript
function select(db, callback) {
    return this.internal_send_command(new Command('select', [db], select_callback(this, db, callback)));
}
```
- example usage
```shell
...
## Usage Example

'''js
var redis = require("redis"),
    client = redis.createClient();

// if you'd like to select database 3, instead of 0 (default), call
// client.select(3, function() { /* ... */ });

client.on("error", function (err) {
    console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sendCommand"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sendCommand (command, args, callback)](#apidoc.element.redis.RedisClient.prototype.sendCommand)
- description and source-code
```javascript
sendCommand = function (command, args, callback) {
    // Throw to fail early instead of relying in order in this case
    if (typeof command !== 'string') {
        throw new TypeError('Wrong input type "' + (command !== null && command !== undefined ? command.constructor.name : command
) + '" for command name');
    }
    if (!Array.isArray(args)) {
        if (args === undefined || args === null) {
            args = [];
        } else if (typeof args === 'function' && callback === undefined) {
            callback = args;
            args = [];
        } else {
            throw new TypeError('Wrong input type "' + args.constructor.name + '" for args');
        }
    }
    if (typeof callback !== 'function' && callback !== undefined) {
        throw new TypeError('Wrong input type "' + (callback !== null ? callback.constructor.name : 'null') + '" for callback function
');
    }

    // Using the raw multi command is only possible with this function
    // If the command is not yet added to the client, the internal function should be called right away
    // Otherwise we need to redirect the calls to make sure the interal functions don't get skipped
    // The internal functions could actually be used for any non hooked function
    // but this might change from time to time and at the moment there's no good way to distinguishe them
    // from each other, so let's just do it do it this way for the time being
    if (command === 'multi' || typeof this[command] !== 'function') {
        return this.internal_send_command(new Command(command, args, callback));
    }
    if (typeof callback === 'function') {
        args = args.concat([callback]); // Prevent manipulating the input array
    }
    return this[command].apply(this, args);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.send_command"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>send_command (command, args, callback)](#apidoc.element.redis.RedisClient.prototype.send_command)
- description and source-code
```javascript
send_command = function (command, args, callback) {
    // Throw to fail early instead of relying in order in this case
    if (typeof command !== 'string') {
        throw new TypeError('Wrong input type "' + (command !== null && command !== undefined ? command.constructor.name : command
) + '" for command name');
    }
    if (!Array.isArray(args)) {
        if (args === undefined || args === null) {
            args = [];
        } else if (typeof args === 'function' && callback === undefined) {
            callback = args;
            args = [];
        } else {
            throw new TypeError('Wrong input type "' + args.constructor.name + '" for args');
        }
    }
    if (typeof callback !== 'function' && callback !== undefined) {
        throw new TypeError('Wrong input type "' + (callback !== null ? callback.constructor.name : 'null') + '" for callback function
');
    }

    // Using the raw multi command is only possible with this function
    // If the command is not yet added to the client, the internal function should be called right away
    // Otherwise we need to redirect the calls to make sure the interal functions don't get skipped
    // The internal functions could actually be used for any non hooked function
    // but this might change from time to time and at the moment there's no good way to distinguishe them
    // from each other, so let's just do it do it this way for the time being
    if (command === 'multi' || typeof this[command] !== 'function') {
        return this.internal_send_command(new Command(command, args, callback));
    }
    if (typeof callback === 'function') {
        args = args.concat([callback]); // Prevent manipulating the input array
    }
    return this[command].apply(this, args);
}
```
- example usage
```shell
...
    };
    get();
    brpop();

Another reason to use duplicate() is when multiple DBs on the same server are
accessed via the redis SELECT command.  Each DB could use its own connection.

## client.send_command(command_name[, [args][, callback]])

All Redis commands have been added to the 'client' object. However, if new commands are introduced before this library is updated
,
you can use 'send_command()' to send arbitrary commands to Redis.
The command_name has to be lower case.

All commands are sent as multi-bulk commands. 'args' can either be an Array of arguments, or omitted / set to undefined.
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.send_offline_queue"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>send_offline_queue ()](#apidoc.element.redis.RedisClient.prototype.send_offline_queue)
- description and source-code
```javascript
send_offline_queue = function () {
    for (var command_obj = this.offline_queue.shift(); command_obj; command_obj = this.offline_queue.shift()) {
        debug('Sending offline command: ' + command_obj.command);
        this.internal_send_command(command_obj);
    }
    this.drain();
}
```
- example usage
```shell
...
        };
        debug('Sending pub/sub on_ready commands');
        for (var key in this.subscription_set) {
            var command = key.slice(0, key.indexOf('_'));
            var args = this.subscription_set[key];
            this[command]([args], callback);
        }
        this.send_offline_queue();
        return;
    }
    this.send_offline_queue();
    this.emit('ready');
};

RedisClient.prototype.on_info_cmd = function (err, res) {
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.set"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>set ()](#apidoc.element.redis.RedisClient.prototype.set)
- description and source-code
```javascript
set = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
// if you'd like to select database 3, instead of 0 (default), call
// client.select(3, function() { /* ... */ });

client.on("error", function (err) {
console.log("Error " + err);
});

client.set("string key", "string val", redis.print);
client.hset("hash key", "hashtest 1", "some value", redis.print);
client.hset(["hash key", "hashtest 2", "some other value"], redis.print);
client.hkeys("hash key", function (err, replies) {
console.log(replies.length + " replies:");
replies.forEach(function (reply, i) {
    console.log("    " + i + ": " + reply);
});
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.setbit"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setbit ()](#apidoc.element.redis.RedisClient.prototype.setbit)
- description and source-code
```javascript
setbit = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.setex"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setex ()](#apidoc.element.redis.RedisClient.prototype.setex)
- description and source-code
```javascript
setex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.setnx"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setnx ()](#apidoc.element.redis.RedisClient.prototype.setnx)
- description and source-code
```javascript
setnx = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.setrange"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>setrange ()](#apidoc.element.redis.RedisClient.prototype.setrange)
- description and source-code
```javascript
setrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.shutdown"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>shutdown ()](#apidoc.element.redis.RedisClient.prototype.shutdown)
- description and source-code
```javascript
shutdown = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sinter"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sinter ()](#apidoc.element.redis.RedisClient.prototype.sinter)
- description and source-code
```javascript
sinter = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sinterstore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sinterstore ()](#apidoc.element.redis.RedisClient.prototype.sinterstore)
- description and source-code
```javascript
sinterstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sismember"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sismember ()](#apidoc.element.redis.RedisClient.prototype.sismember)
- description and source-code
```javascript
sismember = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.slaveof"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>slaveof ()](#apidoc.element.redis.RedisClient.prototype.slaveof)
- description and source-code
```javascript
slaveof = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.slowlog"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>slowlog ()](#apidoc.element.redis.RedisClient.prototype.slowlog)
- description and source-code
```javascript
slowlog = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.smembers"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>smembers ()](#apidoc.element.redis.RedisClient.prototype.smembers)
- description and source-code
```javascript
smembers = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
client.sadd("bigset", "member " + set_size);
set_size -= 1;
}

// multi chain with an individual callback
client.multi()
.scard("bigset")
.smembers("bigset")
.keys("*", function (err, replies) {
    // NOTE: code in this callback is NOT atomic
    // this only happens after the the .exec call finishes.
    client.mget(replies, redis.print);
})
.dbsize()
.exec(function (err, replies) {
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.smove"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>smove ()](#apidoc.element.redis.RedisClient.prototype.smove)
- description and source-code
```javascript
smove = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sort"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sort ()](#apidoc.element.redis.RedisClient.prototype.sort)
- description and source-code
```javascript
sort = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.spop"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>spop ()](#apidoc.element.redis.RedisClient.prototype.spop)
- description and source-code
```javascript
spop = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.srandmember"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>srandmember ()](#apidoc.element.redis.RedisClient.prototype.srandmember)
- description and source-code
```javascript
srandmember = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.srem"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>srem ()](#apidoc.element.redis.RedisClient.prototype.srem)
- description and source-code
```javascript
srem = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sscan"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sscan ()](#apidoc.element.redis.RedisClient.prototype.sscan)
- description and source-code
```javascript
sscan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.strlen"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>strlen ()](#apidoc.element.redis.RedisClient.prototype.strlen)
- description and source-code
```javascript
strlen = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.subscribe"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>subscribe ()](#apidoc.element.redis.RedisClient.prototype.subscribe)
- description and source-code
```javascript
function subscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('subscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
...
    if (msg_count === 3) {
        sub.unsubscribe();
        sub.quit();
        pub.quit();
    }
});

sub.subscribe("a nice channel");
'''

When a client issues a 'SUBSCRIBE' or 'PSUBSCRIBE', that connection is put into a "subscriber" mode.
At that point, only commands that modify the subscription set are valid and quit (and depending on the redis version ping as well
). When the subscription
set is empty, the connection is put back into regular mode.

If you need to send regular commands to Redis while in subscriber mode, just open another connection with a new client (hint: use
 'client.duplicate()').
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.substr"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>substr ()](#apidoc.element.redis.RedisClient.prototype.substr)
- description and source-code
```javascript
substr = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
if (parsed.auth) {
    options.password = parsed.auth.split(':')[1];
}
if (parsed.protocol && parsed.protocol !== 'redis:') {
    console.warn('node_redis: WARNING: You passed "' + parsed.protocol.substring(0, parsed.protocol.length - 1) + '" as protocol
 instead of the "redis" protocol!');
}
if (parsed.pathname && parsed.pathname !== '/') {
    options.db = parsed.pathname.substr(1);
}
if (parsed.hostname) {
    options.host = parsed.hostname;
}
if (parsed.port) {
    options.port = parsed.port;
}
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sunion"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sunion ()](#apidoc.element.redis.RedisClient.prototype.sunion)
- description and source-code
```javascript
sunion = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sunionstore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sunionstore ()](#apidoc.element.redis.RedisClient.prototype.sunionstore)
- description and source-code
```javascript
sunionstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.swapdb"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>swapdb ()](#apidoc.element.redis.RedisClient.prototype.swapdb)
- description and source-code
```javascript
swapdb = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.sync"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>sync ()](#apidoc.element.redis.RedisClient.prototype.sync)
- description and source-code
```javascript
sync = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.time"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>time ()](#apidoc.element.redis.RedisClient.prototype.time)
- description and source-code
```javascript
time = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.touch"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>touch ()](#apidoc.element.redis.RedisClient.prototype.touch)
- description and source-code
```javascript
touch = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.ttl"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>ttl ()](#apidoc.element.redis.RedisClient.prototype.ttl)
- description and source-code
```javascript
ttl = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.type"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>type ()](#apidoc.element.redis.RedisClient.prototype.type)
- description and source-code
```javascript
type = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.uncork"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>uncork ()](#apidoc.element.redis.RedisClient.prototype.uncork)
- description and source-code
```javascript
function noop() {}
```
- example usage
```shell
...
    } else {
        self.write_buffers();
    }
    self.pipeline = false;
    self.fire_strings = true;
    if (self.stream.uncork) {
        // TODO: Consider using next tick here. See https://github.com/NodeRedis/node_redis/issues/1033
        self.stream.uncork();
    }
};

// Restore modal commands from previous connection. The order of the commands is important
if (this.selected_db !== undefined) {
    this.internal_send_command(new Command('select', [this.selected_db]));
}
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.unlink"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unlink ()](#apidoc.element.redis.RedisClient.prototype.unlink)
- description and source-code
```javascript
unlink = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.unref"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unref ()](#apidoc.element.redis.RedisClient.prototype.unref)
- description and source-code
```javascript
unref = function () {
    if (this.connected) {
        debug("Unref'ing the socket connection");
        this.stream.unref();
    } else {
        debug('Not connected yet, will unref later');
        this.once('connect', function () {
            this.unref();
        });
    }
}
```
- example usage
```shell
...
If node_redis emits a library error because of another error, the triggering error is added to the returned error as 'origin' attribute
.

___Error codes___

node_redis returns a 'NR_CLOSED' error code if the clients connection dropped. If a command unresolved command got rejected a 'UNERCTAIN_STATE
' code is returned.
A 'CONNECTION_BROKEN' error code is used in case node_redis gives up to reconnect.

## client.unref()

Call 'unref()' on the underlying socket connection to the Redis server, allowing the program to exit once no more commands are pending
.

This is an **experimental** feature, and only supports a subset of the Redis protocol. Any commands where client state is saved
on the Redis server, e.g. '*SUBSCRIBE' or the blocking 'BL*' commands will *NOT* work with '.unref()'.

'''js
var redis = require("redis")
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.unsubscribe"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unsubscribe ()](#apidoc.element.redis.RedisClient.prototype.unsubscribe)
- description and source-code
```javascript
function unsubscribe() {
    var arr,
        len = arguments.length,
        callback,
        i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        callback = arguments[1];
    } else {
        len = arguments.length;
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    var self = this;
    var call_on_write = function () {
        // Pub sub has to be activated even if not in pub sub mode, as the return value is manipulated in the callback
        self.pub_sub_mode = self.pub_sub_mode || self.command_queue.length + 1;
    };
    return this.internal_send_command(new Command('unsubscribe', arr, callback, call_on_write));
}
```
- example usage
```shell
...
    pub.publish("a nice channel", "I am sending my last message.");
});

sub.on("message", function (channel, message) {
    console.log("sub channel " + channel + ": " + message);
    msg_count += 1;
    if (msg_count === 3) {
        sub.unsubscribe();
        sub.quit();
        pub.quit();
    }
});

sub.subscribe("a nice channel");
'''
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.unwatch"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>unwatch ()](#apidoc.element.redis.RedisClient.prototype.unwatch)
- description and source-code
```javascript
unwatch = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.wait"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>wait ()](#apidoc.element.redis.RedisClient.prototype.wait)
- description and source-code
```javascript
wait = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.warn"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>warn (msg)](#apidoc.element.redis.RedisClient.prototype.warn)
- description and source-code
```javascript
warn = function (msg) {
    var self = this;
    // Warn on the next tick. Otherwise no event listener can be added
    // for warnings that are emitted in the redis client constructor
    process.nextTick(function () {
        if (self.listeners('warning').length !== 0) {
            self.emit('warning', msg);
        } else {
            console.warn('node_redis:', msg);
        }
    });
}
```
- example usage
```shell
...
    cnx_options.host = options.host || '127.0.0.1';
    cnx_options.family = (!options.family && net.isIP(cnx_options.host)) || (options.family === 'IPv6' ? 6 : 4);
    this.address = cnx_options.host + ':' + cnx_options.port;
}
// Warn on misusing deprecated functions
if (typeof options.retry_strategy === 'function') {
    if ('max_attempts' in options) {
        self.warn('WARNING: You activated the retry_strategy and max_attempts at the same time. This is not possible and max_attempts
 will be ignored.');
        // Do not print deprecation warnings twice
        delete options.max_attempts;
    }
    if ('retry_max_delay' in options) {
        self.warn('WARNING: You activated the retry_strategy and retry_max_delay at the same time. This is not possible and retry_max_delay
 will be ignored.');
        // Do not print deprecation warnings twice
        delete options.retry_max_delay;
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.watch"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>watch ()](#apidoc.element.redis.RedisClient.prototype.watch)
- description and source-code
```javascript
watch = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.write"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>write (data)](#apidoc.element.redis.RedisClient.prototype.write)
- description and source-code
```javascript
write = function (data) {
    if (this.pipeline === false) {
        this.should_buffer = !this.stream.write(data);
        return;
    }
    this.pipeline_queue.push(data);
}
```
- example usage
```shell
...

    if (big_data === false) { // Build up a string and send entire command in one write
for (i = 0; i < len; i += 1) {
    arg = args_copy[i];
    command_str += '$' + Buffer.byteLength(arg) + '\r\n' + arg + '\r\n';
}
debug('Send ' + this.address + ' id ' + this.connection_id + ': ' + command_str);
this.write(command_str);
    } else {
debug('Send command (' + command_str + ') has Buffer arguments');
this.fire_strings = false;
this.write(command_str);

for (i = 0; i < len; i += 1) {
    arg = args_copy[i];
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.write_buffers"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>write_buffers ()](#apidoc.element.redis.RedisClient.prototype.write_buffers)
- description and source-code
```javascript
write_buffers = function () {
    for (var command = this.pipeline_queue.shift(); command; command = this.pipeline_queue.shift()) {
        this.should_buffer = !this.stream.write(command);
    }
}
```
- example usage
```shell
...
        self.stream.cork();
    }
};
this.uncork = function () {
    if (self.fire_strings) {
        self.write_strings();
    } else {
        self.write_buffers();
    }
    self.pipeline = false;
    self.fire_strings = true;
    if (self.stream.uncork) {
        // TODO: Consider using next tick here. See https://github.com/NodeRedis/node_redis/issues/1033
        self.stream.uncork();
    }
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.write_strings"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>write_strings ()](#apidoc.element.redis.RedisClient.prototype.write_strings)
- description and source-code
```javascript
write_strings = function () {
    var str = '';
    for (var command = this.pipeline_queue.shift(); command; command = this.pipeline_queue.shift()) {
        // Write to stream if the string is bigger than 4mb. The biggest string may be Math.pow(2, 28) - 15 chars long
        if (str.length + command.length > 4 * 1024 * 1024) {
            this.should_buffer = !this.stream.write(str);
            str = '';
        }
        str += command;
    }
    if (str !== '') {
        this.should_buffer = !this.stream.write(str);
    }
}
```
- example usage
```shell
...
    self.pipeline = true;
    if (self.stream.cork) {
        self.stream.cork();
    }
};
this.uncork = function () {
    if (self.fire_strings) {
        self.write_strings();
    } else {
        self.write_buffers();
    }
    self.pipeline = false;
    self.fire_strings = true;
    if (self.stream.uncork) {
        // TODO: Consider using next tick here. See https://github.com/NodeRedis/node_redis/issues/1033
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zadd"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zadd ()](#apidoc.element.redis.RedisClient.prototype.zadd)
- description and source-code
```javascript
zadd = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
### Commands with Optional and Keyword arguments

This applies to anything that uses an optional '[WITHSCORES]' or '[LIMIT offset count]' in the [redis.io/commands](http://redis.
io/commands) documentation.

Example:
'''js
var args = [ 'myzset', 1, 'one', 2, 'two', 3, 'three', 99, 'ninety-nine' ];
client.zadd(args, function (err, response) {
if (err) throw err;
console.log('added '+response+' items.');

// -Infinity and +Infinity also work
var args1 = [ 'myzset', '+inf', '-inf' ];
client.zrevrangebyscore(args1, function (err, response) {
    if (err) throw err;
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zcard"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zcard ()](#apidoc.element.redis.RedisClient.prototype.zcard)
- description and source-code
```javascript
zcard = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zcount"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zcount ()](#apidoc.element.redis.RedisClient.prototype.zcount)
- description and source-code
```javascript
zcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zincrby"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zincrby ()](#apidoc.element.redis.RedisClient.prototype.zincrby)
- description and source-code
```javascript
zincrby = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zinterstore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zinterstore ()](#apidoc.element.redis.RedisClient.prototype.zinterstore)
- description and source-code
```javascript
zinterstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zlexcount"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zlexcount ()](#apidoc.element.redis.RedisClient.prototype.zlexcount)
- description and source-code
```javascript
zlexcount = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrange"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrange ()](#apidoc.element.redis.RedisClient.prototype.zrange)
- description and source-code
```javascript
zrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrangebylex"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrangebylex ()](#apidoc.element.redis.RedisClient.prototype.zrangebylex)
- description and source-code
```javascript
zrangebylex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrangebyscore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrangebyscore ()](#apidoc.element.redis.RedisClient.prototype.zrangebyscore)
- description and source-code
```javascript
zrangebyscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrank"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrank ()](#apidoc.element.redis.RedisClient.prototype.zrank)
- description and source-code
```javascript
zrank = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrem"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrem ()](#apidoc.element.redis.RedisClient.prototype.zrem)
- description and source-code
```javascript
zrem = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zremrangebylex"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zremrangebylex ()](#apidoc.element.redis.RedisClient.prototype.zremrangebylex)
- description and source-code
```javascript
zremrangebylex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zremrangebyrank"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zremrangebyrank ()](#apidoc.element.redis.RedisClient.prototype.zremrangebyrank)
- description and source-code
```javascript
zremrangebyrank = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zremrangebyscore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zremrangebyscore ()](#apidoc.element.redis.RedisClient.prototype.zremrangebyscore)
- description and source-code
```javascript
zremrangebyscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrevrange"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrange ()](#apidoc.element.redis.RedisClient.prototype.zrevrange)
- description and source-code
```javascript
zrevrange = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrevrangebylex"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrangebylex ()](#apidoc.element.redis.RedisClient.prototype.zrevrangebylex)
- description and source-code
```javascript
zrevrangebylex = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrevrangebyscore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrangebyscore ()](#apidoc.element.redis.RedisClient.prototype.zrevrangebyscore)
- description and source-code
```javascript
zrevrangebyscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
...
var args = [ 'myzset', 1, 'one', 2, 'two', 3, 'three', 99, 'ninety-nine' ];
client.zadd(args, function (err, response) {
if (err) throw err;
console.log('added '+response+' items.');

// -Infinity and +Infinity also work
var args1 = [ 'myzset', '+inf', '-inf' ];
client.zrevrangebyscore(args1, function (err, response) {
    if (err) throw err;
    console.log('example1', response);
    // write your code here
});

var max = 3, min = 1, offset = 1, count = 2;
var args2 = [ 'myzset', max, min, 'WITHSCORES', 'LIMIT', offset, count ];
...
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zrevrank"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zrevrank ()](#apidoc.element.redis.RedisClient.prototype.zrevrank)
- description and source-code
```javascript
zrevrank = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zscan"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zscan ()](#apidoc.element.redis.RedisClient.prototype.zscan)
- description and source-code
```javascript
zscan = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zscore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zscore ()](#apidoc.element.redis.RedisClient.prototype.zscore)
- description and source-code
```javascript
zscore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisClient.prototype.zunionstore"></a>[function <span class="apidocSignatureSpan">redis.RedisClient.prototype.</span>zunionstore ()](#apidoc.element.redis.RedisClient.prototype.zunionstore)
- description and source-code
```javascript
zunionstore = function () {
    var arr;
    var len = arguments.length;
    var callback;
    var i = 0;
    if (Array.isArray(arguments[0])) {
        arr = arguments[0];
        if (len === 2) {
            callback = arguments[1];
        }
    } else if (len > 1 && Array.isArray(arguments[1])) {
        if (len === 3) {
            callback = arguments[2];
        }
        len = arguments[1].length;
        arr = new Array(len + 1);
        arr[0] = arguments[0];
        for (; i < len; i += 1) {
            arr[i + 1] = arguments[1][i];
        }
    } else {
        // The later should not be the average use case
        if (len !== 0 && (typeof arguments[len - 1] === 'function' || typeof arguments[len - 1] === 'undefined')) {
            len--;
            callback = arguments[len];
        }
        arr = new Array(len);
        for (; i < len; i += 1) {
            arr[i] = arguments[i];
        }
    }
    return this.internal_send_command(new Command(command, arr, callback));
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.RedisError"></a>[module redis.RedisError](#apidoc.module.redis.RedisError)

#### <a name="apidoc.element.redis.RedisError.RedisError"></a>[function <span class="apidocSignatureSpan">redis.</span>RedisError (message, stack)](#apidoc.element.redis.RedisError.RedisError)
- description and source-code
```javascript
function RedisError(message, stack) {
  Object.defineProperty(this, 'message', {
    value: message || '',
    configurable: true,
    writable: true
  })
  if (stack || stack === undefined) {
    Error.captureStackTrace(this, RedisError)
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisError.super_"></a>[function <span class="apidocSignatureSpan">redis.RedisError.</span>super_ ()](#apidoc.element.redis.RedisError.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.RedisError.super_"></a>[module redis.RedisError.super_](#apidoc.module.redis.RedisError.super_)

#### <a name="apidoc.element.redis.RedisError.super_.super_"></a>[function <span class="apidocSignatureSpan">redis.RedisError.</span>super_ ()](#apidoc.element.redis.RedisError.super_.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.RedisError.super_.captureStackTrace"></a>[function <span class="apidocSignatureSpan">redis.RedisError.super_.</span>captureStackTrace ()](#apidoc.element.redis.RedisError.super_.captureStackTrace)
- description and source-code
```javascript
function captureStackTrace() { [native code] }
```
- example usage
```shell
...
    RedisError.call(this, obj.message, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    if (stack || stack === undefined) {
        Error.captureStackTrace(this, AbortError);
    }
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}

function AggregateError (obj) {
...
```



# <a name="apidoc.module.redis.ReplyError"></a>[module redis.ReplyError](#apidoc.module.redis.ReplyError)

#### <a name="apidoc.element.redis.ReplyError.ReplyError"></a>[function <span class="apidocSignatureSpan">redis.</span>ReplyError (message)](#apidoc.element.redis.ReplyError.ReplyError)
- description and source-code
```javascript
function ReplyError(message) {
  var tmp = Error.stackTraceLimit
  Error.stackTraceLimit = 2
  RedisError.call(this, message, ADD_STACKTRACE)
  Error.captureStackTrace(this, ReplyError)
  Error.stackTraceLimit = tmp
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.redis.ReplyError.super_"></a>[function <span class="apidocSignatureSpan">redis.ReplyError.</span>super_ (message, stack)](#apidoc.element.redis.ReplyError.super_)
- description and source-code
```javascript
function RedisError(message, stack) {
  Object.defineProperty(this, 'message', {
    value: message || '',
    configurable: true,
    writable: true
  })
  if (stack || stack === undefined) {
    Error.captureStackTrace(this, RedisError)
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.redis.customErrors"></a>[module redis.customErrors](#apidoc.module.redis.customErrors)

#### <a name="apidoc.element.redis.customErrors.AbortError"></a>[function <span class="apidocSignatureSpan">redis.customErrors.</span>AbortError (obj, stack)](#apidoc.element.redis.customErrors.AbortError)
- description and source-code
```javascript
function AbortError(obj, stack) {
    assert(obj, 'The options argument is required');
    assert.strictEqual(typeof obj, 'object', 'The options argument has to be of type object');

    RedisError.call(this, obj.message, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    if (stack || stack === undefined) {
        Error.captureStackTrace(this, AbortError);
    }
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}
```
- example usage
```shell
...
if (queue_names[i] === 'command_queue') {
    error_attributes.message += ' It might have been processed.';
} else { // As the command_queue is flushed first, remove this for the offline queue
    error_attributes.message = error_attributes.message.replace(' It might have been processed.', '');
}
// Don't flush everything from the queue
for (var command_obj = this[queue_names[i]].shift(); command_obj; command_obj = this[queue_names[i]].shift()) {
    var err = new errorClasses.AbortError(error_attributes);
    if (command_obj.error) {
        err.stack = err.stack + command_obj.error.stack.replace(/^Error.*?\n/, '\n');
    }
    err.command = command_obj.command.toUpperCase();
    if (command_obj.args && command_obj.args.length) {
        err.args = command_obj.args;
    }
...
```

#### <a name="apidoc.element.redis.customErrors.AggregateError"></a>[function <span class="apidocSignatureSpan">redis.customErrors.</span>AggregateError (obj)](#apidoc.element.redis.customErrors.AggregateError)
- description and source-code
```javascript
function AggregateError(obj) {
    assert(obj, 'The options argument is required');
    assert.strictEqual(typeof obj, 'object', 'The options argument has to be of type object');

    AbortError.call(this, obj, ADD_STACKTRACE);
    Object.defineProperty(this, 'message', {
        value: obj.message || '',
        configurable: true,
        writable: true
    });
    Error.captureStackTrace(this, AggregateError);
    for (var keys = Object.keys(obj), key = keys.pop(); key; key = keys.pop()) {
        this[key] = obj[key];
    }
}
```
- example usage
```shell
...
    // Currently this would be a breaking change, therefore it's only emitted in debug_mode
    if (exports.debug_mode && aggregated_errors.length) {
        var error;
        if (aggregated_errors.length === 1) {
            error = aggregated_errors[0];
        } else {
            error_attributes.message = error_attributes.message.replace('It', 'They').replace(/command/i, '$&s');
            error = new errorClasses.AggregateError(error_attributes);
            error.errors = aggregated_errors;
        }
        this.emit('error', error);
    }
};

RedisClient.prototype.on_error = function (err) {
...
```



# <a name="apidoc.module.redis.utils"></a>[module redis.utils](#apidoc.module.redis.utils)

#### <a name="apidoc.element.redis.utils.callback_or_emit"></a>[function <span class="apidocSignatureSpan">redis.utils.</span>callback_or_emit (self, callback, err, res)](#apidoc.element.redis.utils.callback_or_emit)
- description and source-code
```javascript
function callbackOrEmit(self, callback, err, res) {
    if (callback) {
        callback(err, res);
    } else if (err) {
        self.emit('error', err);
    }
}
```
- example usage
```shell
...

    var match = err.message.match(utils.err_code);
    // LUA script could return user errors that don't behave like all other errors!
    if (match) {
        err.code = match[1];
    }

    utils.callback_or_emit(this, command_obj.callback, err);
};

RedisClient.prototype.drain = function () {
    this.emit('drain');
    this.should_buffer = false;
};
...
```

#### <a name="apidoc.element.redis.utils.clone"></a>[function <span class="apidocSignatureSpan">redis.utils.</span>clone (obj)](#apidoc.element.redis.utils.clone)
- description and source-code
```javascript
function convenienceClone(obj) {
    camelCase = false;
    obj = clone(obj) || {};
    if (camelCase) {
        obj.camel_case = true;
    }
    return obj;
}
```
- example usage
```shell
...

exports.debug_mode = /\bredis\b/i.test(process.env.NODE_DEBUG);

// Attention: The second parameter might be removed at will and is not officially supported.
// Do not rely on this
function RedisClient (options, stream) {
// Copy the options so they are not mutated
options = utils.clone(options);
EventEmitter.call(this);
var cnx_options = {};
var self = this;
/* istanbul ignore next: travis does not work with stunnel atm. Therefore the tls tests are skipped on travis */
for (var tls_option in options.tls) {
    cnx_options[tls_option] = options.tls[tls_option];
    // Copy the tls options into the general options to make sure the address is set right
...
```

#### <a name="apidoc.element.redis.utils.print"></a>[function <span class="apidocSignatureSpan">redis.utils.</span>print (err, reply)](#apidoc.element.redis.utils.print)
- description and source-code
```javascript
function print(err, reply) {
    if (err) {
        // A error always begins with Error:
        console.log(err.toString());
    } else {
        console.log('Reply: ' + reply);
    }
}
```
- example usage
```shell
...
The 'versions' key contains an array of the elements of the version string for easy comparison.

> client.server_info.redis_version
'2.3.0'
> client.server_info.versions
[ 2, 3, 0 ]

## redis.print()

A handy callback function for displaying return values when testing. Example:

'''js
var redis = require("redis"),
client = redis.createClient();
...
```

#### <a name="apidoc.element.redis.utils.reply_in_order"></a>[function <span class="apidocSignatureSpan">redis.utils.</span>reply_in_order (self, callback, err, res, queue)](#apidoc.element.redis.utils.reply_in_order)
- description and source-code
```javascript
function replyInOrder(self, callback, err, res, queue) {
    // If the queue is explicitly passed, use that, otherwise fall back to the offline queue first,
    // as there might be commands in both queues at the same time
    var command_obj;
<span class="apidocCodeCommentSpan">    /* istanbul ignore if: TODO: Remove this as soon as we test Redis 3.2 on travis */
</span>    if (queue) {
        command_obj = queue.peekBack();
    } else {
        command_obj = self.offline_queue.peekBack() || self.command_queue.peekBack();
    }
    if (!command_obj) {
        process.nextTick(function () {
            callbackOrEmit(self, callback, err, res);
        });
    } else {
        var tmp = command_obj.callback;
        command_obj.callback = tmp ?
            function (e, r) {
                tmp(e, r);
                callbackOrEmit(self, callback, err, res);
            } :
            function (e, r) {
                if (e) {
                    self.emit('error', e);
                }
                callbackOrEmit(self, callback, err, res);
            };
    }
}
```
- example usage
```shell
...
            message: command + " can't be processed. " + msg,
            code: 'NR_CLOSED',
            command: command
        });
        if (command_obj.args.length) {
            err.args = command_obj.args;
        }
        utils.reply_in_order(self, command_obj.callback, err);
    } else {
        debug('Queueing ' + command + ' for next server connection.');
        self.offline_queue.push(command_obj);
    }
    self.should_buffer = true;
}
...
```

#### <a name="apidoc.element.redis.utils.reply_to_object"></a>[function <span class="apidocSignatureSpan">redis.utils.</span>reply_to_object (reply)](#apidoc.element.redis.utils.reply_to_object)
- description and source-code
```javascript
function replyToObject(reply) {
    // The reply might be a string or a buffer if this is called in a transaction (multi)
    if (reply.length === 0 || !(reply instanceof Array)) {
        return null;
    }
    var obj = {};
    for (var i = 0; i < reply.length; i += 2) {
        obj[reply[i].toString('binary')] = reply[i + 1];
    }
    return obj;
}
```
- example usage
```shell
...
    if (buffer_args === false || this.message_buffers) {
        // If detect_buffers option was specified, then the reply from the parser will be a buffer.
        // If this command did not use Buffer arguments, then convert the reply to Strings here.
        reply = utils.reply_to_strings(reply);
    }

    if (command === 'hgetall') {
        reply = utils.reply_to_object(reply);
    }
    return reply;
}

exports.debug_mode = /\bredis\b/i.test(process.env.NODE_DEBUG);

// Attention: The second parameter might be removed at will and is not officially supported.
...
```

#### <a name="apidoc.element.redis.utils.reply_to_strings"></a>[function <span class="apidocSignatureSpan">redis.utils.</span>reply_to_strings (reply)](#apidoc.element.redis.utils.reply_to_strings)
- description and source-code
```javascript
function replyToStrings(reply) {
    if (reply instanceof Buffer) {
        return reply.toString();
    }
    if (reply instanceof Array) {
        var res = new Array(reply.length);
        for (var i = 0; i < reply.length; i++) {
            // Recusivly call the function as slowlog returns deep nested replies
            res[i] = replyToStrings(reply[i]);
        }
        return res;
    }

    return reply;
}
```
- example usage
```shell
...

function noop () {}

function handle_detect_buffers_reply (reply, command, buffer_args) {
    if (buffer_args === false || this.message_buffers) {
        // If detect_buffers option was specified, then the reply from the parser will be a buffer.
        // If this command did not use Buffer arguments, then convert the reply to Strings here.
        reply = utils.reply_to_strings(reply);
    }

    if (command === 'hgetall') {
        reply = utils.reply_to_object(reply);
    }
    return reply;
}
...
```



# misc
- this document was created with [utility2](https://github.com/kaizhu256/node-utility2)
